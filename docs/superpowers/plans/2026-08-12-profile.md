# Profile Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** สร้างระบบโปรไฟล์พื้นฐานที่เก็บชื่อและ bio ไว้ในเครื่อง เข้าถึงได้จากปุ่มบนหน้า Home ที่ยังว่าง

**Architecture:** `ProfileStore` เป็น `@Observable` ที่ถือ state เพียงผู้เดียว ส่งให้ View ผ่าน SwiftUI environment และเป็นตัวเดียวที่แตะไฟล์ `profile.json` ข้อมูลเป็น struct `Profile` ล้วนที่ถือกฎ validation และกฎอักษรย่อไว้ในตัว หน้าแก้ไขทำงานบน draft copy แล้วส่งผลลัพธ์กลับผ่าน closure โดยไม่รู้จัก store เลย

**Tech Stack:** SwiftUI, Observation (`@Observable`), Foundation (`JSONEncoder` / `JSONDecoder`), ไม่มี dependency ภายนอก

**Spec:** [2026-08-12-profile-design.md](../specs/2026-08-12-profile-design.md)

## Global Constraints

ทุก task ต้องอยู่ภายใต้ข้อกำหนดเหล่านี้:

- **ห้ามแก้ `project.pbxproj`** — โปรเจกต์ใช้ `PBXFileSystemSynchronizedRootGroup` ไฟล์ใหม่ใต้ `null-app/` ถูกเก็บเข้าโปรเจกต์อัตโนมัติ สร้างไฟล์ด้วย Write tool เท่านั้น
- **ไม่มี test target และไม่สร้างในรอบนี้** — การเพิ่ม target ต้องแก้ `project.pbxproj` ซึ่งข้อบนห้ามไว้ การตรวจสอบใช้วิธีที่ระบุในแต่ละ task
- **หนึ่ง target สี่แพลตฟอร์ม** — iOS, iPadOS, macOS, visionOS โค้ดใช้ร่วมกันทั้งหมด แยกด้วย `#if os(...)` เมื่อจำเป็นเท่านั้น
- **`SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor`** — type และ function ที่ไม่ประกาศอะไรเป็น main-actor อยู่แล้ว งานที่ต้องออกจาก main actor ต้องระบุ `nonisolated` ชัดเจน
- **Deployment target 26.5** — ไม่ต้องเขียน `if #available` สำหรับ API ที่มาก่อนหรือเท่ากับ 26.5
- **`displayName`** — ตัดช่องว่างหัวท้ายแล้วต้องไม่ว่าง และยาวไม่เกิน **50** ตัวอักษร
- **`bio`** — ไม่บังคับกรอก ยาวไม่เกิน **160** ตัวอักษร
- **นับความยาวด้วยจำนวน `Character`** ไม่ใช่จำนวน byte เพื่อให้ภาษาไทยและ emoji นับตรงกับที่ผู้ใช้เห็น
- **ไฟล์ข้อมูล** — `profile.json` ใน Application Support directory เขียนแบบ `.atomic`
- **UI strings เป็นภาษาอังกฤษ** ให้เข้ากับโค้ดเดิมในโปรเจกต์ (ยังไม่ทำ localization ในรอบนี้)

## เรื่องการตรวจสอบ

โปรเจกต์ไม่มี test target วงจร TDD ปกติจึงใช้ไม่ได้ แผนนี้ใช้การตรวจสอบที่รันได้จริงแทน:

| ชั้น | วิธีตรวจ |
|---|---|
| Logic ล้วน (Task 1, 2) | compile ไฟล์จริงรวมกับ harness ด้วย `swiftc` แล้วรัน assertion จริง |
| UI (Task 3, 4) | build → launch บน simulator → screenshot ดูผล |

harness เป็นไฟล์ชั่วคราวใน scratchpad ไม่เข้า repo แต่ assertion ที่เขียนไว้จะย้ายไปเป็น test จริงได้ทันทีเมื่อเพิ่ม test target ภายหลัง

ค่าคงที่ที่ใช้ทุก task:

```bash
PROJ="/Users/purintae/Documents/WROKSPACE/App Project/null-app"
SCRATCH="/private/tmp/claude-501/-Users-purintae-Documents-WROKSPACE-App-Project-null-app/42d7aa55-485e-4feb-89c6-9becd6c5d585/scratchpad"
```

---

### Task 1: Profile model

โครงสร้างข้อมูลและกฎทั้งหมดที่ไม่ผูกกับ UI — อักษรย่อ, การตัดช่องว่าง, ความถูกต้องของความยาว

**Files:**
- Create: `null-app/Models/Profile.swift`
- Verify (ชั่วคราว ไม่เข้า repo): `$SCRATCH/profile-check/main.swift`

**Interfaces:**
- Consumes: ไม่มี — เป็น task แรก
- Produces:
  - `struct Profile: Codable, Equatable, Sendable` มี `var displayName: String` และ `var bio: String`
  - `static let Profile.empty: Profile`
  - `static let Profile.displayNameLimit: Int` = 50
  - `static let Profile.bioLimit: Int` = 160
  - `var Profile.initials: String`
  - `var Profile.trimmedDisplayName: String`
  - `var Profile.isValid: Bool`

- [ ] **Step 1: เขียน harness ที่ยัง fail**

สร้าง `$SCRATCH/profile-check/main.swift` (ต้องชื่อ `main.swift` เพราะ Swift อนุญาต top-level code เฉพาะไฟล์ชื่อนี้):

```swift
import Foundation

var failures = 0

func expect<T: Equatable>(_ actual: T, _ expected: T, _ label: String) {
    if actual == expected {
        print("PASS  \(label)")
    } else {
        print("FAIL  \(label): got \(actual), want \(expected)")
        failures += 1
    }
}

// อักษรย่อ
expect(Profile(displayName: "Purin Tae", bio: "").initials, "PT", "สองคำ")
expect(Profile(displayName: "purin", bio: "").initials, "P", "คำเดียว แปลงเป็นตัวใหญ่")
expect(Profile(displayName: "Ann Marie Smith", bio: "").initials, "AM", "สามคำ เอาสองตัวแรก")
expect(Profile(displayName: "  Purin   Tae  ", bio: "").initials, "PT", "ช่องว่างเกินไม่กวน")
expect(Profile(displayName: "", bio: "").initials, "?", "ชื่อว่าง")
expect(Profile(displayName: "   ", bio: "").initials, "?", "มีแต่ช่องว่าง")
expect(Profile(displayName: "ปุริน แต้", bio: "").initials, "ปแ", "ภาษาไทย")

// ตัดช่องว่าง
expect(Profile(displayName: "  Purin  ", bio: "").trimmedDisplayName, "Purin", "ตัดหัวท้าย")

// ความถูกต้อง
expect(Profile(displayName: "Purin", bio: "").isValid, true, "ชื่อปกติ ผ่าน")
expect(Profile(displayName: "", bio: "").isValid, false, "ชื่อว่าง ไม่ผ่าน")
expect(Profile(displayName: "   ", bio: "").isValid, false, "ชื่อมีแต่ช่องว่าง ไม่ผ่าน")
expect(Profile(displayName: String(repeating: "a", count: 50), bio: "").isValid, true, "ชื่อ 50 ตัว ผ่าน")
expect(Profile(displayName: String(repeating: "a", count: 51), bio: "").isValid, false, "ชื่อ 51 ตัว ไม่ผ่าน")
expect(Profile(displayName: "Purin", bio: String(repeating: "b", count: 160)).isValid, true, "bio 160 ตัว ผ่าน")
expect(Profile(displayName: "Purin", bio: String(repeating: "b", count: 161)).isValid, false, "bio 161 ตัว ไม่ผ่าน")

// นับเป็น Character ไม่ใช่ byte
expect(Profile(displayName: "Purin", bio: String(repeating: "🇹🇭", count: 160)).isValid, true, "emoji 160 ตัว ผ่าน")

// เข้ารหัสกลับมาได้เท่าเดิม
let original = Profile(displayName: "Purin Tae", bio: "สวัสดี")
let data = try JSONEncoder().encode(original)
let decoded = try JSONDecoder().decode(Profile.self, from: data)
expect(decoded, original, "encode แล้ว decode ได้ค่าเดิม")

print(failures == 0 ? "\nALL PASS" : "\n\(failures) FAILED")
exit(failures == 0 ? 0 : 1)
```

- [ ] **Step 2: รัน harness ให้เห็นว่า fail**

```bash
mkdir -p "$SCRATCH/profile-check" && swiftc -o "$SCRATCH/profile-check/run" "$PROJ/null-app/Models/Profile.swift" "$SCRATCH/profile-check/main.swift"
```

คาดหวัง: compile ไม่ผ่าน เพราะยังไม่มีไฟล์ `Profile.swift` — error ประมาณ `no such file or directory`

- [ ] **Step 3: เขียน `Profile.swift`**

```swift
//
//  Profile.swift
//  null-app
//

import Foundation

/// โปรไฟล์ของผู้ใช้ มีอยู่ชุดเดียวในแอป
struct Profile: Codable, Equatable, Sendable {
    var displayName: String
    var bio: String

    static let empty = Profile(displayName: "", bio: "")

    static let displayNameLimit = 50
    static let bioLimit = 160

    /// ชื่อที่ตัดช่องว่างหัวท้ายแล้ว ใช้ทั้งตอนแสดงผลและตอนตรวจความถูกต้อง
    var trimmedDisplayName: String {
        displayName.trimmingCharacters(in: .whitespacesAndNewlines)
    }

    /// อักษรย่อสำหรับวงกลมแทนรูปโปรไฟล์
    /// เอาอักษรตัวแรกของสองคำแรก ถ้าไม่มีคำใดเลยคืน "?"
    var initials: String {
        let letters = trimmedDisplayName
            .split(whereSeparator: \.isWhitespace)
            .prefix(2)
            .compactMap(\.first)

        guard !letters.isEmpty else { return "?" }
        return String(letters).uppercased()
    }

    var isValid: Bool {
        let name = trimmedDisplayName
        return !name.isEmpty
            && name.count <= Self.displayNameLimit
            && bio.count <= Self.bioLimit
    }
}
```

- [ ] **Step 4: รัน harness ให้ผ่าน**

```bash
swiftc -o "$SCRATCH/profile-check/run" "$PROJ/null-app/Models/Profile.swift" "$SCRATCH/profile-check/main.swift" && "$SCRATCH/profile-check/run"
```

คาดหวัง: ทุกบรรทัดขึ้น `PASS` และปิดท้ายด้วย `ALL PASS`

หมายเหตุ: `"ปแ"` มาจาก `"ปุริน แต้"` เพราะ `.first` คืน `Character` ตัวแรกซึ่งในภาษาไทยคือพยัญชนะต้นพร้อมสระที่ผูกกับมัน หากผลจริงต่างจากนี้ ให้แก้ค่าที่คาดหวังใน harness ให้ตรงกับพฤติกรรมจริงของ Swift แทนการดัดโค้ด — กฎที่ต้องการคือ "อักษรตัวแรกของคำ" ไม่ใช่ค่าเฉพาะตัวนี้

- [ ] **Step 5: ยืนยันว่าโปรเจกต์ยัง build ผ่าน**

```bash
xcodebuild -scheme null-app -project "$PROJ/null-app.xcodeproj" -destination 'generic/platform=iOS Simulator' build 2>&1 | tail -3
```

คาดหวัง: `** BUILD SUCCEEDED **`

- [ ] **Step 6: Commit**

```bash
cd "$PROJ" && git add null-app/Models/Profile.swift && git commit -m "Add Profile model with initials and validation rules"
```

---

### Task 2: ProfileStore

ตัวเดียวในระบบที่แตะดิสก์ อ่านตอนสร้าง เขียนเมื่อถูกสั่ง

**Files:**
- Create: `null-app/Storage/ProfileStore.swift`
- Verify (ชั่วคราว ไม่เข้า repo): `$SCRATCH/store-check/main.swift`

**Interfaces:**
- Consumes: `Profile`, `Profile.empty` จาก Task 1
- Produces:
  - `@Observable final class ProfileStore`
  - `init(fileURL: URL = ProfileStore.defaultFileURL)` — อ่านไฟล์แบบ synchronous ทันที
  - `private(set) var profile: Profile`
  - `func update(_ newProfile: Profile) async throws` — ตั้งค่าใน memory แล้วเขียนดิสก์
  - `static var defaultFileURL: URL`
  - `nonisolated static func read(from url: URL) -> Profile`
  - `nonisolated static func write(_ profile: Profile, to url: URL) throws`

`read` และ `write` เป็น `internal` ไม่ใช่ `private` โดยตั้งใจ เพื่อให้ตรวจสอบได้จากภายนอกโดยไม่ต้องสร้าง instance

- [ ] **Step 1: เขียน harness ที่ยัง fail**

สร้าง `$SCRATCH/store-check/main.swift`:

```swift
import Foundation

var failures = 0

func expect<T: Equatable>(_ actual: T, _ expected: T, _ label: String) {
    if actual == expected {
        print("PASS  \(label)")
    } else {
        print("FAIL  \(label): got \(actual), want \(expected)")
        failures += 1
    }
}

let dir = URL(fileURLWithPath: NSTemporaryDirectory())
    .appending(path: "store-check-\(UUID().uuidString)")
let file = dir.appending(path: "profile.json")

// ยังไม่มีไฟล์ ต้องได้ค่าว่าง ไม่ crash
expect(ProfileStore.read(from: file), Profile.empty, "ไม่มีไฟล์ คืนค่าว่าง")

// เขียนแล้วอ่านกลับได้เท่าเดิม พร้อมสร้างโฟลเดอร์ให้เอง
let saved = Profile(displayName: "Purin Tae", bio: "สวัสดีครับ")
try ProfileStore.write(saved, to: file)
expect(ProfileStore.read(from: file), saved, "เขียนแล้วอ่านกลับได้ค่าเดิม")

// ไฟล์เสีย ต้องถอยไปค่าว่าง ไม่ crash
try Data("ไม่ใช่ JSON".utf8).write(to: file)
expect(ProfileStore.read(from: file), Profile.empty, "ไฟล์เสีย คืนค่าว่าง")

// เขียนทับได้
try ProfileStore.write(saved, to: file)
let second = Profile(displayName: "Someone Else", bio: "")
try ProfileStore.write(second, to: file)
expect(ProfileStore.read(from: file), second, "เขียนทับได้")

try? FileManager.default.removeItem(at: dir)

print(failures == 0 ? "\nALL PASS" : "\n\(failures) FAILED")
exit(failures == 0 ? 0 : 1)
```

- [ ] **Step 2: รัน harness ให้เห็นว่า fail**

```bash
mkdir -p "$SCRATCH/store-check" && swiftc -o "$SCRATCH/store-check/run" "$PROJ/null-app/Models/Profile.swift" "$PROJ/null-app/Storage/ProfileStore.swift" "$SCRATCH/store-check/main.swift"
```

คาดหวัง: compile ไม่ผ่าน เพราะยังไม่มี `ProfileStore.swift`

- [ ] **Step 3: เขียน `ProfileStore.swift`**

```swift
//
//  ProfileStore.swift
//  null-app
//

import Foundation
import Observation

/// เจ้าของ state ของโปรไฟล์เพียงผู้เดียว และเป็นจุดเดียวในแอปที่แตะไฟล์
@Observable
final class ProfileStore {
    private(set) var profile: Profile

    private let fileURL: URL

    /// อ่านไฟล์แบบ synchronous โดยตั้งใจ — ไฟล์เล็กมาก การอ่านเร็วกว่าหนึ่งเฟรม
    /// และการทำแบบ async จะทำให้เห็นจอว่างแวบหนึ่งก่อนข้อมูลมา
    init(fileURL: URL = ProfileStore.defaultFileURL) {
        self.fileURL = fileURL
        self.profile = ProfileStore.read(from: fileURL)
    }

    /// ตั้งค่าใน memory ก่อน แล้วค่อยเขียนดิสก์
    /// ถ้าเขียนพลาด ค่าใน memory ยังอยู่ ผู้ใช้ไม่เสียสิ่งที่พิมพ์ไป
    func update(_ newProfile: Profile) async throws {
        profile = newProfile

        let url = fileURL
        try await Task.detached(priority: .utility) {
            try ProfileStore.write(newProfile, to: url)
        }.value
    }

    static var defaultFileURL: URL {
        URL.applicationSupportDirectory.appending(path: "profile.json")
    }

    /// ทุกความล้มเหลวของการอ่านแปลงเป็นค่าว่าง — ไม่มีไฟล์คือเรื่องปกติของการเปิดครั้งแรก
    /// ส่วนไฟล์เสียก็ไม่ควรทำให้แอปเปิดไม่ขึ้น
    nonisolated static func read(from url: URL) -> Profile {
        guard
            let data = try? Data(contentsOf: url),
            let decoded = try? JSONDecoder().decode(Profile.self, from: data)
        else {
            return .empty
        }
        return decoded
    }

    /// throw ออกไปให้ผู้เรียกจัดการ เพราะผู้ใช้ต้องรู้ว่าบันทึกไม่สำเร็จ
    nonisolated static func write(_ profile: Profile, to url: URL) throws {
        try FileManager.default.createDirectory(
            at: url.deletingLastPathComponent(),
            withIntermediateDirectories: true
        )
        let data = try JSONEncoder().encode(profile)
        try data.write(to: url, options: .atomic)
    }
}
```

- [ ] **Step 4: รัน harness ให้ผ่าน**

```bash
swiftc -o "$SCRATCH/store-check/run" "$PROJ/null-app/Models/Profile.swift" "$PROJ/null-app/Storage/ProfileStore.swift" "$SCRATCH/store-check/main.swift" && "$SCRATCH/store-check/run"
```

คาดหวัง: ทุกบรรทัด `PASS` และ `ALL PASS`

หากขั้นนี้ล้มเหลวเพราะ `swiftc` ขยาย macro `@Observable` ไม่ได้ ให้ตรวจสอบเฉพาะ `read` / `write` แทน โดยลบบรรทัด `@Observable` ออกชั่วคราวระหว่างรัน harness แล้วใส่กลับก่อน commit — logic ที่ต้องการตรวจอยู่ใน static function ทั้งคู่ ไม่ได้อยู่ที่ตัว macro

- [ ] **Step 5: ยืนยันว่าโปรเจกต์ยัง build ผ่าน**

```bash
xcodebuild -scheme null-app -project "$PROJ/null-app.xcodeproj" -destination 'generic/platform=iOS Simulator' build 2>&1 | tail -3
```

คาดหวัง: `** BUILD SUCCEEDED **`

- [ ] **Step 6: Commit**

```bash
cd "$PROJ" && git add null-app/Storage/ProfileStore.swift && git commit -m "Add ProfileStore backed by a JSON file"
```

---

### Task 3: หน้า Home และหน้า Profile แบบอ่านอย่างเดียว

จบ task นี้แล้วแอปต้องเปิดขึ้นมาเป็นหน้า Home กดปุ่มเข้าหน้า Profile ได้ และเห็นวงกลมอักษรย่อ

**Files:**
- Create: `null-app/Views/InitialsAvatar.swift`
- Create: `null-app/Views/ProfileView.swift`
- Create: `null-app/Views/HomeView.swift`
- Modify: `null-app/null_appApp.swift` (แทนที่ทั้งไฟล์)
- Delete: `null-app/ContentView.swift`

**Interfaces:**
- Consumes: `Profile`, `Profile.initials`, `Profile.trimmedDisplayName` จาก Task 1 · `ProfileStore`, `ProfileStore.profile` จาก Task 2
- Produces:
  - `struct InitialsAvatar: View` — `init(initials: String, diameter: CGFloat = 96)`
  - `struct ProfileView: View` — ไม่มี parameter อ่าน `ProfileStore` จาก environment
  - `struct HomeView: View` — ไม่มี parameter

ยังไม่มีปุ่ม Edit ใน task นี้ — เพิ่มใน Task 4

- [ ] **Step 1: สร้าง `InitialsAvatar.swift`**

```swift
//
//  InitialsAvatar.swift
//  null-app
//

import SwiftUI

/// วงกลมแสดงอักษรย่อแทนรูปโปรไฟล์
/// เป็นจุดเดียวที่ต้องแก้เมื่อวันหนึ่งเปลี่ยนไปใช้รูปจริง
struct InitialsAvatar: View {
    let initials: String
    var diameter: CGFloat = 96

    var body: some View {
        Circle()
            .fill(Color.accentColor.opacity(0.15))
            .frame(width: diameter, height: diameter)
            .overlay {
                Text(initials)
                    .font(.system(size: diameter * 0.36, weight: .semibold, design: .rounded))
                    .foregroundStyle(Color.accentColor)
            }
            // ชื่อถูกอ่านออกเสียงจาก Text ที่อยู่ข้าง ๆ อยู่แล้ว
            .accessibilityHidden(true)
    }
}

#Preview {
    VStack(spacing: 20) {
        InitialsAvatar(initials: "PT")
        InitialsAvatar(initials: "?")
        InitialsAvatar(initials: "A", diameter: 48)
    }
    .padding()
}
```

- [ ] **Step 2: สร้าง `ProfileView.swift`**

```swift
//
//  ProfileView.swift
//  null-app
//

import SwiftUI

struct ProfileView: View {
    @Environment(ProfileStore.self) private var store

    private var name: String {
        let trimmed = store.profile.trimmedDisplayName
        return trimmed.isEmpty ? "No name yet" : trimmed
    }

    var body: some View {
        VStack(spacing: 16) {
            InitialsAvatar(initials: store.profile.initials)

            Text(name)
                .font(.title2.weight(.semibold))
                .foregroundStyle(store.profile.trimmedDisplayName.isEmpty ? .secondary : .primary)

            if !store.profile.bio.isEmpty {
                Text(store.profile.bio)
                    .multilineTextAlignment(.center)
                    .foregroundStyle(.secondary)
            }

            Spacer()
        }
        .padding()
        .frame(maxWidth: .infinity)
        .navigationTitle("Profile")
    }
}

#Preview {
    NavigationStack {
        ProfileView()
    }
    .environment(ProfileStore(fileURL: URL.temporaryDirectory.appending(path: "preview.json")))
}
```

- [ ] **Step 3: สร้าง `HomeView.swift`**

```swift
//
//  HomeView.swift
//  null-app
//

import SwiftUI

/// หน้าแรกยังว่างโดยตั้งใจ — มีแค่ทางเข้าไปหน้าโปรไฟล์
struct HomeView: View {
    var body: some View {
        VStack(spacing: 12) {
            Spacer()

            Image(systemName: "square.dashed")
                .font(.system(size: 44))
                .foregroundStyle(.tertiary)

            Text("Nothing here yet")
                .foregroundStyle(.secondary)

            Spacer()

            NavigationLink("View Profile") {
                ProfileView()
            }
            .buttonStyle(.borderedProminent)
            .controlSize(.large)
        }
        .padding()
        .frame(maxWidth: .infinity)
        .navigationTitle("Home")
    }
}

#Preview {
    NavigationStack {
        HomeView()
    }
    .environment(ProfileStore(fileURL: URL.temporaryDirectory.appending(path: "preview.json")))
}
```

- [ ] **Step 4: แทนที่ `null_appApp.swift` ทั้งไฟล์**

```swift
//
//  null_appApp.swift
//  null-app
//
//  Created by Purin Tae on 12/8/2569 BE.
//

import SwiftUI

@main
struct null_appApp: App {
    /// สร้างครั้งเดียวตอนแอปเริ่ม การอ่านไฟล์เกิดขึ้นใน initializer
    @State private var store = ProfileStore()

    var body: some Scene {
        WindowGroup {
            NavigationStack {
                HomeView()
            }
            .environment(store)
        }
    }
}
```

- [ ] **Step 5: ลบ `ContentView.swift`**

```bash
cd "$PROJ" && git rm null-app/ContentView.swift
```

`HomeView` มาแทนที่แล้ว ถ้าปล่อยไว้จะเป็นโค้ดตายที่ทำให้คนอ่านสับสนว่าหน้าแรกคือไฟล์ไหน

- [ ] **Step 6: Build**

```bash
xcodebuild -scheme null-app -project "$PROJ/null-app.xcodeproj" -destination 'generic/platform=iOS Simulator' build 2>&1 | tail -3
```

คาดหวัง: `** BUILD SUCCEEDED **`

- [ ] **Step 7: รันบน simulator แล้วดูผล**

หา path ของ `.app` ที่เพิ่ง build ก่อน:

```bash
xcodebuild -scheme null-app -project "$PROJ/null-app.xcodeproj" -destination 'generic/platform=iOS Simulator' -showBuildSettings 2>/dev/null | awk -F' = ' '/ BUILT_PRODUCTS_DIR /{print $2"/null-app.app"}'
```

จากนั้น:

- `mcp__Claude_Code_iOS_Simulator__control` action `attach` — เปิด panel ให้ผู้ใช้เห็นก่อนเสมอ
- `mcp__Claude_Code_iOS_Simulator__control` action `launch` โดย `bundle_id` = `purin.null-app` และ `app_path` = ผลลัพธ์จากคำสั่งข้างบน
- `mcp__Claude_Code_iOS_Simulator__control` action `screenshot`

คาดหวังบนหน้าแรก: หัวข้อ `Home` ไอคอนสี่เหลี่ยมประ ข้อความ `Nothing here yet` และปุ่ม `View Profile` สีทึบด้านล่าง

- [ ] **Step 8: กดปุ่มเข้าหน้า Profile แล้วดูผล**

- `tap` ที่ปุ่ม `View Profile`
- `screenshot`

คาดหวัง: หัวข้อ `Profile` วงกลมมี `?` อยู่ตรงกลาง ข้อความ `No name yet` สีจาง ไม่มี bio และมีปุ่มย้อนกลับไป Home

- [ ] **Step 9: Commit**

```bash
cd "$PROJ" && git add -A null-app && git commit -m "Add Home and read-only Profile screens"
```

---

### Task 4: แก้ไขและบันทึกโปรไฟล์

จบ task นี้แล้วผู้ใช้ต้องแก้ชื่อกับ bio ได้ กด Save แล้วค่าอยู่ถาวรแม้ปิดแอปแล้วเปิดใหม่

**Files:**
- Create: `null-app/Views/ProfileEditView.swift`
- Modify: `null-app/Views/ProfileView.swift` (เพิ่มปุ่ม Edit, sheet, alert และ logic การบันทึก)

**Interfaces:**
- Consumes: `Profile`, `Profile.isValid`, `Profile.trimmedDisplayName`, `Profile.bioLimit` จาก Task 1 · `ProfileStore.update(_:)` จาก Task 2 · `ProfileView` จาก Task 3
- Produces: `struct ProfileEditView: View` — `init(profile: Profile, onSave: @escaping (Profile) -> Void)`

`ProfileEditView` ไม่รู้จัก `ProfileStore` เลย รับ `Profile` เข้าไปหนึ่งตัวและส่งออกมาหนึ่งตัว ทำให้ preview ได้โดยไม่ต้องเตรียม store และทำให้การเขียนดิสก์เกิดที่เดียวคือ `ProfileView`

- [ ] **Step 1: สร้าง `ProfileEditView.swift`**

```swift
//
//  ProfileEditView.swift
//  null-app
//

import SwiftUI

/// ฟอร์มแก้ไขที่ทำงานบนสำเนา ไม่แตะ store โดยตรง
/// กด Cancel แล้วทุกอย่างที่พิมพ์ไปถูกทิ้ง
struct ProfileEditView: View {
    @Environment(\.dismiss) private var dismiss

    @State private var draft: Profile
    private let onSave: (Profile) -> Void

    init(profile: Profile, onSave: @escaping (Profile) -> Void) {
        _draft = State(initialValue: profile)
        self.onSave = onSave
    }

    private var bioIsOverLimit: Bool {
        draft.bio.count > Profile.bioLimit
    }

    var body: some View {
        NavigationStack {
            Form {
                Section("Name") {
                    TextField("Display name", text: $draft.displayName)
                }

                Section("Bio") {
                    TextField("Something about you", text: $draft.bio, axis: .vertical)
                        .lineLimit(3...6)

                    HStack {
                        Spacer()
                        Text("\(draft.bio.count)/\(Profile.bioLimit)")
                            .font(.caption)
                            .foregroundStyle(bioIsOverLimit ? Color.red : Color.secondary)
                            .monospacedDigit()
                    }
                }
            }
            .formStyle(.grouped)
            .navigationTitle("Edit Profile")
            .toolbar {
                ToolbarItem(placement: .cancellationAction) {
                    Button("Cancel") { dismiss() }
                }
                ToolbarItem(placement: .confirmationAction) {
                    Button("Save") {
                        // เก็บชื่อที่ตัดช่องว่างแล้ว จะได้ไม่มีช่องว่างค้างในไฟล์
                        var cleaned = draft
                        cleaned.displayName = draft.trimmedDisplayName
                        onSave(cleaned)
                        dismiss()
                    }
                    .disabled(!draft.isValid)
                }
            }
        }
        #if os(macOS)
        // sheet บน macOS ไม่มีขนาดเริ่มต้นที่ใช้งานได้ ต้องกำหนดเอง
        .frame(minWidth: 420, minHeight: 320)
        #endif
    }
}

#Preview("ว่าง") {
    ProfileEditView(profile: .empty) { _ in }
}

#Preview("มีข้อมูลแล้ว") {
    ProfileEditView(profile: Profile(displayName: "Purin Tae", bio: "สวัสดีครับ")) { _ in }
}
```

- [ ] **Step 2: แทนที่ `ProfileView.swift` ทั้งไฟล์**

```swift
//
//  ProfileView.swift
//  null-app
//

import SwiftUI

struct ProfileView: View {
    @Environment(ProfileStore.self) private var store

    @State private var isEditing = false
    @State private var saveError: String?

    private var name: String {
        let trimmed = store.profile.trimmedDisplayName
        return trimmed.isEmpty ? "No name yet" : trimmed
    }

    var body: some View {
        VStack(spacing: 16) {
            InitialsAvatar(initials: store.profile.initials)

            Text(name)
                .font(.title2.weight(.semibold))
                .foregroundStyle(store.profile.trimmedDisplayName.isEmpty ? .secondary : .primary)

            if !store.profile.bio.isEmpty {
                Text(store.profile.bio)
                    .multilineTextAlignment(.center)
                    .foregroundStyle(.secondary)
            }

            Spacer()
        }
        .padding()
        .frame(maxWidth: .infinity)
        .navigationTitle("Profile")
        .toolbar {
            Button("Edit") { isEditing = true }
        }
        .sheet(isPresented: $isEditing) {
            ProfileEditView(profile: store.profile) { updated in
                Task { await save(updated) }
            }
        }
        .alert(
            "Couldn't save",
            isPresented: Binding(
                get: { saveError != nil },
                set: { if !$0 { saveError = nil } }
            ),
            presenting: saveError
        ) { _ in
            Button("OK", role: .cancel) { saveError = nil }
        } message: { message in
            Text(message)
        }
    }

    /// ค่าใน store ถูกตั้งไปแล้วแม้เขียนดิสก์พลาด ผู้ใช้จึงยังเห็นสิ่งที่เพิ่งพิมพ์
    private func save(_ updated: Profile) async {
        do {
            try await store.update(updated)
        } catch {
            saveError = error.localizedDescription
        }
    }
}

#Preview {
    NavigationStack {
        ProfileView()
    }
    .environment(ProfileStore(fileURL: URL.temporaryDirectory.appending(path: "preview.json")))
}
```

- [ ] **Step 3: Build**

```bash
xcodebuild -scheme null-app -project "$PROJ/null-app.xcodeproj" -destination 'generic/platform=iOS Simulator' build 2>&1 | tail -3
```

คาดหวัง: `** BUILD SUCCEEDED **`

- [ ] **Step 4: ตรวจว่าปุ่ม Save ถูก disable ตอนชื่อว่าง**

- `launch` แอปใหม่ แล้ว `tap` ปุ่ม `View Profile`
- `tap` ปุ่ม `Edit` มุมขวาบน
- `screenshot`

คาดหวัง: sheet เปิดขึ้น หัวข้อ `Edit Profile` ช่อง Name ว่าง ตัวนับแสดง `0/160` และปุ่ม `Save` เป็นสีจางกดไม่ได้

- [ ] **Step 5: กรอกข้อมูลแล้วบันทึก**

- `tap` ที่ช่อง Display name
- `text` พิมพ์ `Purin Tae`
- `tap` ที่ช่อง bio แล้ว `text` พิมพ์ `Building null-app`
- `screenshot` — ยืนยันว่าปุ่ม `Save` กดได้แล้วและตัวนับขึ้น `17/160`
- `tap` ปุ่ม `Save`
- `screenshot`

คาดหวัง: sheet ปิด กลับมาหน้า Profile เห็นวงกลม `PT` ชื่อ `Purin Tae` และ bio `Building null-app`

- [ ] **Step 6: ตรวจว่าข้อมูลอยู่ถาวรจริง**

ปิดแอปแล้วเปิดใหม่ด้วย `launch` ซ้ำ จากนั้น `tap` ปุ่ม `View Profile` แล้ว `screenshot`

คาดหวัง: ยังเห็น `PT`, `Purin Tae` และ `Building null-app` เหมือนเดิม — พิสูจน์ว่าอ่านจากไฟล์ได้จริงไม่ใช่ค้างใน memory

- [ ] **Step 7: ตรวจว่า Cancel ทิ้งการแก้ไขจริง**

- `tap` ปุ่ม `Edit`
- `tap` ช่อง Display name แล้วพิมพ์ทับเป็น `Changed`
- `tap` ปุ่ม `Cancel`
- `screenshot`

คาดหวัง: ยังเป็น `Purin Tae` เหมือนเดิม การแก้ไขถูกทิ้งทั้งหมด

- [ ] **Step 8: Commit**

```bash
cd "$PROJ" && git add null-app/Views && git commit -m "Add profile editing with draft copy and save"
```

---

## หลังทำครบทุก task

- [ ] ลบไฟล์ harness ชั่วคราว

```bash
rm -rf "$SCRATCH/profile-check" "$SCRATCH/store-check"
```

- [ ] ยืนยันว่า working tree สะอาดและ build ผ่านทั้ง iOS และ macOS

```bash
cd "$PROJ" && git status --short && xcodebuild -scheme null-app -destination 'platform=macOS' build 2>&1 | tail -3
```

การ build macOS ที่นี่สำคัญ เพราะทั้งแผนตรวจบน iOS simulator อย่างเดียว โค้ดที่มี `#if os(macOS)` จึงยังไม่เคยถูก compile เลยจนถึงขั้นนี้
