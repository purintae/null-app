# Work tracker รอบ 4 — task เป็นตัวปิด stage

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** เพิ่มตาราง `task` แล้วให้สถานะของ stage ทั้งหมด — ปิดแล้วหรือยัง ช้ากี่วัน คืบหน้าเท่าไหร่ — อ่านจาก task แทนการกดสวิตช์ พร้อมเลื่อนแผนที่เหลือให้เองเมื่อ stage ปิดช้ากว่ากำหนด

**Architecture:** `stage.actual_start` / `actual_end` ถูกลบทิ้งทั้งคู่ สถานะกลายเป็นค่าที่**คำนวณ**จาก `task.done_at` ทำให้ไม่มีคอลัมน์ที่รอวันไม่ตรงกับความจริง กติกาทั้งหมดอยู่ในไฟล์ Foundation ล้วนที่ทดสอบด้วย `swiftc` ได้โดยไม่ต้องมี Xcode — `WorkRows.swift` (สถานะรายแถว), `WorkFilter.swift` (กติการะดับรายการ รวมถึง Overdue ที่มีได้ทีละอัน) และ `WorkSchedule.swift` (การเลื่อนแผน) ส่วน `WorkStore` เป็นที่เดียวที่คุยกับเซิร์ฟเวอร์เหมือนเดิม

**Tech Stack:** Swift 5 / SwiftUI (`SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor`), supabase-swift 2.x, Postgres 15 + RLS, deployment target 26.5

**Spec:** `docs/superpowers/specs/2026-08-19-work-round-4-design.md` (ต่อจาก `2026-08-14-work-tracker-design.md`)

## Global Constraints

- feature id คือ `work` schema คือ `f_work` — ตรง `^[a-z][a-z0-9_]*$`
- ชื่อ type ทุกตัวขึ้นต้นด้วย `Work` (ทุกฟีเจอร์อยู่โมดูลเดียวกัน) — **ห้ามเพิ่ม extension บน type ของ stdlib**
- แตะตารางผ่าน `Backend.client.schema("f_work").from(...)` เท่านั้น
- **`apply_migration` ผ่าน MCP ถูกบล็อกในสภาพแวดล้อมนี้** — เขียน SQL เป็นไฟล์ให้เจ้าของโปรเจกต์รันเอง แล้ว**รอการยืนยันด้วยการอ่านแถวกลับมา** ห้ามเดินต่อโดยเดาว่าสำเร็จ
- **ห้าม `import Supabase`** ใน `WorkRows.swift`, `WorkFilter.swift`, `WorkSchedule.swift`, `WorkDraft.swift`, `WorkPayload.swift` — ความบริสุทธิ์นี้คือสิ่งเดียวที่ทำให้ harness ด้วย `swiftc` เป็นไปได้
- **PATCH ที่มีฟิลด์ Optional ต้องเขียน `encode(to:)` เอง** — ตัวสังเคราะห์ใช้ `encodeIfPresent` ฟิลด์ nil จะหายไปจาก JSON และ PostgREST อ่าน "ไม่มีคีย์" ว่า "อย่าแตะคอลัมน์นี้"
- วันของ stage เดินทางเป็น `String` `yyyy-MM-dd` ผ่าน `WorkStageRow.dayFormatter` · `task.done_at` เป็น `timestamptz` เดินทางเป็น `String` ISO8601 ผ่าน `WorkTaskRow.instantFormatter` — **ห้ามให้ `Date` ตรง ๆ ไปโดน encoder ของ library ทั้งสองกรณี**
- `navigationDestination(for:)` **ไม่ทำงานในสแตกนี้** เพราะ Home push ฟีเจอร์ด้วย `NavigationLink(destination:)` แบบเก่า — ใช้ `navigationDestination(item:)` ผูกกับ `@State` เท่านั้น
- **ห้ามผูก view กับ `$array[index]`** — `dismiss()` เคลียร์ state ตั้งแต่เริ่มแอนิเมชัน หน้าที่กำลังถอยจะอ่าน index ที่หลุดขอบแล้วแอปดับ ใช้ binding ที่หาจาก id แบบ `WorkStageEditorView.binding(for:)`
- ป้ายกำกับของแอปเป็นอังกฤษ ข้อความที่ผู้ใช้พิมพ์เป็นไทยได้ — **ทดสอบด้วยข้อความไทยจริงเสมอ**
- สร้างไฟล์ด้วย Write ตรง ๆ (synchronized file group) **ห้ามแก้ `project.pbxproj`**
- build ผ่านทั้ง iOS และ macOS ก่อนถือว่าเสร็จ

## ไม่อยู่ในรอบนี้โดยตั้งใจ

subtask · วันครบกำหนดรายตัวของ task · การมอบหมาย task ให้คนอื่น · หน้า Timeline เต็ม ·
การแสดงตัวเลขเทียบ baseline บนหน้าจอ (รอบนี้**เก็บข้อมูล**อย่างเดียว ยังไม่มีที่ดู) ·
task ที่ยกเลิก และ stage ที่ข้ามไป — สองข้อหลังเป็นคำถามค้างในสเปก ยังไม่มีคำตอบ จึงยังไม่ทำ

## File Structure

| ไฟล์ | หน้าที่ |
|---|---|
| `null-app/Features/work/WorkRows.swift` | **แก้** — เพิ่ม `WorkTaskRow`, `WorkStageRow` รับ `task` และคำนวณสถานะจากมัน |
| `null-app/Features/work/WorkSchedule.swift` | **สร้างใหม่** — การเลื่อนแผนเมื่อปิด stage ช้า Foundation ล้วน |
| `null-app/Features/work/WorkFilter.swift` | **แก้** — Overdue ทีละอัน และเลิกอ้าง `actual_start` |
| `null-app/Features/work/WorkDraft.swift` | **แก้** — `WorkStageDraft` ตัดวันจริงออก |
| `null-app/Features/work/WorkPayload.swift` | **แก้** — เพิ่ม `WorkTaskPayload`, stage payload ตัดวันจริง เพิ่ม baseline ตอน insert |
| `null-app/Features/work/WorkStore.swift` | **แก้** — ดึง task, เขียน task, เลื่อนแผนเมื่อปิด |
| `null-app/Features/work/WorkTaskListView.swift` | **สร้างใหม่** — รายการ task ของ stage ที่เลือก |
| `null-app/Features/work/WorkStageEditorView.swift` | **แก้** — ตัวเลขจริงในการ์ด Progress, ปุ่ม New Task ใช้งานได้, หน้าแก้ stage ตัดสวิตช์ |
| `null-app/Features/work/UNINSTALL.md` | **แก้** — เพิ่มตาราง `task` ในรายการที่ต้อง export |

---

### Task 1: ฐานข้อมูล — ตาราง `task`, baseline, และการย้ายข้อมูลก่อนลบ

**MCP เขียนไม่ได้** งานนี้จึงผลิต**ไฟล์ SQL** ให้เจ้าของโปรเจกต์รัน แล้วยืนยันด้วยการอ่านกลับ
ลำดับสำคัญ: ต้องย้าย `actual_end` เป็น task **ก่อน** drop ไม่งั้นประวัติการปิด stage หายถาวร

**Files:**
- Create: `docs/superpowers/sql/round-4/01_f_work_task.sql`
- Create: `docs/superpowers/sql/round-4/02_f_work_baseline.sql`
- Create: `docs/superpowers/sql/round-4/03_f_work_actual_to_task.sql`
- Create: `docs/superpowers/sql/round-4/04_f_work_drop_actual.sql`

**Interfaces:**
- Consumes: schema จากรอบ 3 (`f_work.work`, `f_work.stage`, `f_work.stage_type`)
- Produces: `f_work.task(id, stage_id, title, done_at, position, created_at, updated_at)` · `f_work.stage.baseline_start` / `baseline_end` · `f_work.stage` ไม่มี `actual_start` / `actual_end`

- [ ] **Step 1: เขียน SQL ตาราง `task`**

`docs/superpowers/sql/round-4/01_f_work_task.sql`:

```sql
-- migration name: f_work_task
create table f_work.task (
    id uuid primary key default gen_random_uuid(),
    stage_id uuid not null references f_work.stage(id) on delete cascade,
    title text not null,
    done_at timestamptz,
    position int not null,
    created_at timestamptz not null default now(),
    updated_at timestamptz not null default now(),
    constraint task_title_not_blank check (btrim(title) <> '')
);

create index task_stage_id_idx on f_work.task(stage_id);

alter table f_work.task enable row level security;

-- เจ้าของสืบผ่าน stage → work สองชั้น
-- with check ต้องมีทั้ง insert และ update ไม่งั้นย้าย task ไปแขวนใต้ stage ของคนอื่นได้
create policy "own task select" on f_work.task
    for select using (
        exists (
            select 1 from f_work.stage s join f_work.work w on w.id = s.work_id
            where s.id = task.stage_id and w.user_id = auth.uid()
        )
    );

create policy "own task insert" on f_work.task
    for insert with check (
        exists (
            select 1 from f_work.stage s join f_work.work w on w.id = s.work_id
            where s.id = task.stage_id and w.user_id = auth.uid()
        )
    );

create policy "own task update" on f_work.task
    for update using (
        exists (
            select 1 from f_work.stage s join f_work.work w on w.id = s.work_id
            where s.id = task.stage_id and w.user_id = auth.uid()
        )
    ) with check (
        exists (
            select 1 from f_work.stage s join f_work.work w on w.id = s.work_id
            where s.id = task.stage_id and w.user_id = auth.uid()
        )
    );

create policy "own task delete" on f_work.task
    for delete using (
        exists (
            select 1 from f_work.stage s join f_work.work w on w.id = s.work_id
            where s.id = task.stage_id and w.user_id = auth.uid()
        )
    );

grant select, insert, update, delete on f_work.task to authenticated;

-- แก้ task คือการแก้ Work การ์ดบอก "อัปเดตล่าสุด" จากคอลัมน์ของ work
-- ไม่ใช่ security definer โดยตั้งใจ — authenticated มี policy own work update อยู่แล้ว
create or replace function f_work.touch_work_from_task()
returns trigger
language plpgsql
set search_path to ''
as $$
declare
    target uuid;
begin
    select s.work_id into target from f_work.stage s
    where s.id = coalesce(new.stage_id, old.stage_id);

    update f_work.work set updated_at = now() where id = target;

    if tg_op = 'DELETE' then return old; else return new; end if;
end;
$$;

create trigger task_touch_work
    after insert or update or delete on f_work.task
    for each row execute function f_work.touch_work_from_task();
```

- [ ] **Step 2: เขียน SQL คอลัมน์ baseline**

`docs/superpowers/sql/round-4/02_f_work_baseline.sql`:

```sql
-- migration name: f_work_stage_baseline
-- แผนแรกที่ตั้งไว้ เขียนครั้งเดียวตอน insert ไม่แตะอีกตลอดชีวิตของแถว
-- planned_* จะถูกเขียนทับเมื่อ stage ปิดช้าแล้วแผนที่เหลือเลื่อน ตัวนี้จึงเป็น
-- ที่เดียวที่ยังตอบได้ว่าหลุดจาก Action Plan ต้นปีไปเท่าไหร่
alter table f_work.stage
    add column baseline_start date,
    add column baseline_end date;

-- แถวเดิมยังไม่มีแผนแรกบันทึกไว้ ใช้แผนปัจจุบัน ณ วันย้ายเป็น baseline
update f_work.stage
set baseline_start = planned_start,
    baseline_end = planned_end
where baseline_start is null;

alter table f_work.stage
    alter column baseline_start set not null,
    alter column baseline_end set not null,
    add constraint stage_baseline_dates_ordered check (baseline_end >= baseline_start);
```

- [ ] **Step 3: เขียน SQL ย้าย `actual_end` เป็น task**

`docs/superpowers/sql/round-4/03_f_work_actual_to_task.sql`:

```sql
-- ต้องรันหลัง 01 และ **ก่อน** 04 เสมอ
-- stage ที่ปิดไปแล้วจริงมีวันปิดอยู่ใน actual_end ถ้า drop ตรง ๆ ประวัติหายทั้งหมด
-- แปลงเป็น task หนึ่งอันที่เสร็จแล้ว เพื่อให้กติกาใหม่ยังอ่านว่า stage นั้นปิดแล้วเหมือนเดิม
insert into f_work.task (stage_id, title, done_at, position)
select s.id,
       'ปิด ' || s.name,
       s.actual_end::timestamptz,
       1
from f_work.stage s
where s.actual_end is not null;
```

- [ ] **Step 4: เขียน SQL ลบคอลัมน์วันจริง**

`docs/superpowers/sql/round-4/04_f_work_drop_actual.sql`:

```sql
-- migration name: f_work_drop_actual_dates
-- actual_start ไม่มีทางต่างจาก planned_start ได้ในกระบวนการจริง
-- actual_end คำนวณจาก max(task.done_at) ได้ ทั้งคู่จึงเป็นคอลัมน์ที่รอวันไม่ตรงกับความจริง
alter table f_work.stage drop constraint stage_actual_dates_ordered;
alter table f_work.stage drop column actual_start;
alter table f_work.stage drop column actual_end;
```

- [ ] **Step 5: ส่งไฟล์ให้เจ้าของโปรเจกต์รันตามลำดับ แล้วรอ**

ส่งทั้งสี่ไฟล์พร้อมบอกว่า **ต้องรันเรียงเลข 01 → 02 → 03 → 04** และข้อ 03 ห้ามข้าม
**หยุดรอจนกว่าจะยืนยันว่ารันแล้ว** ห้ามเดินต่อไป Task 2 โดยเดา

- [ ] **Step 6: ยืนยันด้วยการอ่านกลับ**

```sql
select column_name from information_schema.columns
where table_schema = 'f_work' and table_name = 'stage' order by ordinal_position;
```

Expected: มี `baseline_start`, `baseline_end` และ **ไม่มี** `actual_start`, `actual_end`

```sql
select s.code, s.position, count(t.id) as tasks, count(t.done_at) as done
from f_work.stage s
left join f_work.task t on t.stage_id = s.id
join f_work.work w on w.id = s.work_id
where w.name like '26-BP-07-02%'
group by s.code, s.position order by s.position;
```

Expected: stage ที่เคยมี `actual_end` มี `tasks = 1` และ `done = 1` ส่วนที่ยังไม่ปิดมี `tasks = 0`

- [ ] **Step 7: พิสูจน์ว่า RLS ปฏิเสธ task ของคนอื่น**

หา `USER_B` (คนที่ไม่ได้เป็นเจ้าของ) จาก `select user_id, display_name from public.profiles order by created_at;` แล้ว:

```sql
begin;
set local role authenticated;
set local request.jwt.claims to '{"sub":"USER_B","role":"authenticated"}';
select count(*) as tasks_visible_to_user_b from f_work.task;
rollback;
```

Expected: `0` — **ถ้าได้มากกว่า 0 แปลว่า policy หลวม หยุดทันที**

- [ ] **Step 8: พิสูจน์ว่าชื่อ task ว่างถูกปฏิเสธ**

```sql
begin;
insert into f_work.task (stage_id, title, position)
values ((select id from f_work.stage limit 1), '   ', 1);
rollback;
```

Expected: `ERROR ... violates check constraint "task_title_not_blank"`

- [ ] **Step 9: Commit ไฟล์ SQL**

```bash
git add docs/superpowers/sql/round-4/
git commit -m "Write the SQL that lets tasks exist"
```

---

### Task 2: `WorkTaskRow` และสถานะของ stage ที่อ่านจาก task

**Files:**
- Modify: `null-app/Features/work/WorkRows.swift`
- Test: harness ชั่วคราวนอก repo (`mktemp -d`)

**Interfaces:**
- Consumes: ไม่มี
- Produces:
  - `WorkTaskRow` — `id: UUID`, `title: String`, `doneAt: Date?`, `position: Int`, `isDone: Bool`, `static instantFormatter: ISO8601DateFormatter`
  - `WorkStageRow.task: [WorkTaskRow]`, `.baselineStart` / `.baselineEnd: String`, `.isClosed: Bool`, `.closedOn: Date?`, `.doneCount: Int`, `.taskCount: Int`, `.progress: Double`
  - `WorkStageRow.State` เหลือ `.completed` / `.current` / `.ahead` เท่าเดิม แต่ `state(today:calendar:)` รับพารามิเตอร์แล้ว

- [ ] **Step 1: เพิ่ม `WorkTaskRow` ท้าย `WorkRows.swift`**

```swift
/// task หนึ่งอันใน stage
///
/// `done_at` เป็น `timestamptz` ไม่ใช่ `date` — ต่างจากวันของ stage โดยตั้งใจ
/// เพราะมันบันทึก**ช่วงเวลาที่ติ๊ก** ไม่ใช่วันที่คนเลือกเอง การเก็บเวลาไว้ด้วย
/// ทำให้เรียงลำดับการปิดภายในวันเดียวกันได้ถูกต้อง
nonisolated struct WorkTaskRow: Codable, Sendable, Identifiable {
    let id: UUID
    let title: String
    let doneAt: Date?
    let position: Int

    enum CodingKeys: String, CodingKey {
        case id, title, position
        case doneAt = "done_at"
    }

    var isDone: Bool { doneAt != nil }

    /// ตัวแปลงขาเขียนของ `done_at` — ขาอ่าน decoder ของ library จัดการเอง
    /// ห้ามส่ง `Date` ตรง ๆ ให้ encoder ด้วยเหตุผลเดียวกับที่วันของ stage ห้ามส่ง
    static let instantFormatter: ISO8601DateFormatter = {
        let f = ISO8601DateFormatter()
        f.formatOptions = [.withInternetDateTime]
        f.timeZone = TimeZone(secondsFromGMT: 0)
        return f
    }()
}
```

- [ ] **Step 2: ให้ `WorkStageRow` รับ task และ baseline**

ใน `WorkStageRow` ลบ `let actualStart: String?` และ `let actualEnd: String?` แล้วเพิ่ม:

```swift
    let baselineStart: String
    let baselineEnd: String
    var task: [WorkTaskRow]
```

แก้ `CodingKeys` เป็น:

```swift
    enum CodingKeys: String, CodingKey {
        case id, code, name, position, task
        case plannedStart = "planned_start"
        case plannedEnd = "planned_end"
        case baselineStart = "baseline_start"
        case baselineEnd = "baseline_end"
    }
```

ลบ `actualStartDate` / `actualEndDate` แล้วเพิ่ม:

```swift
    var baselineStartDate: Date? { Self.dayFormatter.date(from: baselineStart) }
    var baselineEndDate: Date? { Self.dayFormatter.date(from: baselineEnd) }
```

- [ ] **Step 3: เขียนกติกาการปิดและความคืบหน้า**

แทนที่ `var state: State` เดิมทั้งก้อนด้วย:

```swift
    /// **stage ที่ไม่มี task เลยปิดไม่ได้ตลอดกาล — ตั้งใจให้เป็นแบบนี้**
    ///
    /// ถ้าใช้กติกา "ไม่มี task ที่ค้าง" เฉย ๆ stage ที่เพิ่งสร้างจะกลายเป็นเสร็จแล้ว
    /// ตั้งแต่วินาทีแรก เพราะยังไม่มีอะไรให้ค้าง ผลที่ยอมรับคือทุก stage ต้องมี task
    /// อย่างน้อยหนึ่งอัน ซึ่งบังคับให้เขียนออกมาว่าอะไรคือของที่ต้องได้จาก stage นี้
    var isClosed: Bool {
        !task.isEmpty && task.allSatisfy(\.isDone)
    }

    /// วันที่ปิด stage คือวันที่ติ๊ก task ตัวสุดท้าย — ไม่มีคอลัมน์เก็บ
    var closedOn: Date? {
        guard isClosed else { return nil }
        return task.compactMap(\.doneAt).max()
    }

    var taskCount: Int { task.count }
    var doneCount: Int { task.count { $0.isDone } }

    /// 0 เมื่อยังไม่มี task — ไม่ใช่ 1 ดูเหตุผลที่ `isClosed`
    var progress: Double {
        guard !task.isEmpty else { return 0 }
        return Double(doneCount) / Double(task.count)
    }

    enum State: Sendable {
        case completed
        case current
        case ahead
    }

    /// สามสถานะที่อนุมานจาก task และปฏิทิน ไม่มีคอลัมน์เก็บ
    ///
    /// รับ `today` เข้ามาแทนการเรียก `Date()` เอง เพราะหน้าจอตรึงวันนี้ไว้ตอนปรากฏ
    /// ถ้าฟังก์ชันนี้อ่านนาฬิกาเอง การนับกับสิ่งที่วาดอาจคนละวินาทีกันข้ามเที่ยงคืน
    func state(today: Date, calendar: Calendar) -> State {
        if isClosed { return .completed }
        guard let start = plannedStartDate else { return .ahead }
        return calendar.startOfDay(for: start) <= calendar.startOfDay(for: today)
            ? .current
            : .ahead
    }
```

- [ ] **Step 4: เขียน harness**

```bash
SCRATCH=$(mktemp -d)
echo "$SCRATCH"
```

เขียน `$SCRATCH/main.swift`:

```swift
import Foundation

var failures = 0
func check(_ name: String, _ condition: Bool) {
    if condition { print("ok   \(name)") } else { print("FAIL \(name)"); failures += 1 }
}

let cal = WorkFilter.calendar
func day(_ text: String) -> Date { WorkStageRow.dayFormatter.date(from: text)! }
func instant(_ text: String) -> Date { WorkTaskRow.instantFormatter.date(from: text)! }

func task(_ title: String, done: String?) -> WorkTaskRow {
    WorkTaskRow(id: UUID(), title: title, doneAt: done.map(instant), position: 1)
}

func stage(_ code: String, _ start: String, _ end: String, _ tasks: [WorkTaskRow]) -> WorkStageRow {
    WorkStageRow(
        id: UUID(), code: code, name: code, position: 1,
        plannedStart: start, plannedEnd: end,
        baselineStart: start, baselineEnd: end,
        task: tasks
    )
}

let today = day("2026-09-20")

// ── การปิด stage ─────────────────────────────────────────────────

let empty = stage("RU", "2026-08-01", "2026-09-01", [])
check("stage ที่ไม่มี task เลยยังไม่ปิด", empty.isClosed == false)
check("stage ที่ไม่มี task ได้ progress 0 ไม่ใช่ 1", empty.progress == 0)

let half = stage("RU", "2026-08-01", "2026-09-01", [
    task("user ออก proposal", done: "2026-08-20T03:00:00Z"),
    task("ทวนขอบเขตกับทีม", done: nil),
])
check("task เสร็จบางส่วนยังไม่ปิด", half.isClosed == false)
check("progress ครึ่งหนึ่ง", half.progress == 0.5)
check("นับ task ที่เสร็จได้ถูก", half.doneCount == 1 && half.taskCount == 2)

let closed = stage("RU", "2026-08-01", "2026-09-01", [
    task("user ออก proposal", done: "2026-08-20T03:00:00Z"),
    task("ทวนขอบเขตกับทีม", done: "2026-09-05T08:30:00Z"),
])
check("task ครบทุกอันแปลว่าปิด", closed.isClosed)
check("progress เต็ม", closed.progress == 1)
check("วันปิดคือวันที่ติ๊กตัวสุดท้าย", closed.closedOn == instant("2026-09-05T08:30:00Z"))
check("stage ที่ยังไม่ปิดไม่มีวันปิด", half.closedOn == nil)

// ── สามสถานะ ─────────────────────────────────────────────────────

check("ปิดแล้วอ่านว่า completed", closed.state(today: today, calendar: cal) == .completed)
check("ถึงวันเริ่มแล้วยังไม่ปิด อ่านว่า current",
      half.state(today: today, calendar: cal) == .current)

let future = stage("PVT", "2027-01-01", "2027-02-01", [task("เตรียมสคริปต์", done: nil)])
check("ยังไม่ถึงวันเริ่ม อ่านว่า ahead", future.state(today: today, calendar: cal) == .ahead)

let startsToday = stage("DEV", "2026-09-20", "2026-10-20", [task("ตั้งโครง", done: nil)])
check("วันเริ่มตรงกับวันนี้ อ่านว่า current แล้ว",
      startsToday.state(today: today, calendar: cal) == .current)

// ── ข้อความไทย ───────────────────────────────────────────────────

let thai = task("อบรมผู้ใช้ก่อนเปิดระบบ", done: nil)
check("ชื่อ task ภาษาไทยเดินทางครบ", thai.title == "อบรมผู้ใช้ก่อนเปิดระบบ")

print(failures == 0 ? "ALL PASS" : "\(failures) FAILED")
exit(failures == 0 ? 0 : 1)
```

- [ ] **Step 5: รัน harness ให้ผ่านทุกข้อ**

```bash
swiftc -o "$SCRATCH/run" \
  null-app/Features/work/WorkRows.swift \
  null-app/Features/work/WorkFilter.swift \
  "$SCRATCH/main.swift" && "$SCRATCH/run"
```

Expected: ทุกบรรทัดขึ้นต้นด้วย `ok` และปิดท้ายด้วย `ALL PASS`

จะยังคอมไพล์ไม่ผ่านถ้า `WorkFilter` ยังอ้าง `actualStart` — **นี่คือสิ่งที่คาดไว้** ไปแก้ใน Task 3 แล้วกลับมารันใหม่

- [ ] **Step 6: Commit**

```bash
git add null-app/Features/work/WorkRows.swift
git commit -m "Let a stage read its own state off its tasks"
```

---

### Task 3: Overdue มีได้ทีละอัน และตัวกรองที่เลิกอ้างวันจริง

**Files:**
- Modify: `null-app/Features/work/WorkFilter.swift`
- Test: harness เดิม (`$SCRATCH/main.swift`)

**Interfaces:**
- Consumes: `WorkStageRow.isClosed`, `.state(today:calendar:)` จาก Task 2
- Produces:
  - `WorkFilter.blockingStage(_:today:calendar:) -> WorkStageRow?` — stage แรกที่ยังไม่ปิดและเลยกำหนด มีได้ไม่เกินหนึ่ง
  - `WorkFilter.daysLate(_:today:calendar:) -> Int?` — นับจาก `blockingStage` ตัวเดียว
  - `WorkFilter.isFinished(_:) -> Bool` — ทุก stage ปิดครบ
  - `WorkFilter.calendar` เท่าเดิม

- [ ] **Step 1: เขียน `blockingStage` แทน `overdueStages`**

แทนที่ฟังก์ชัน `overdueStages` เดิมด้วย:

```swift
    /// stage ที่บล็อกงานอยู่ — ตัวแรกตามลำดับที่ยังไม่ปิดและเลย `planned_end` มาแล้ว
    ///
    /// **มีได้ไม่เกินหนึ่งอันต่องานหนึ่งชิ้น และนี่คือทั้งหมดของ Freeze**
    /// ถ้า RU ค้างเพราะ user ยังไม่ออก proposal อีกสี่ stage ข้างหลังก็เลยวันตามแผนไปด้วย
    /// การนับทั้งห้าเป็น overdue คือการรายงานว่ามีปัญหาห้าจุดทั้งที่มีจุดเดียว —
    /// ตัวเลขที่เกินความจริงบ่อย ๆ คือตัวเลขที่คนเลิกเชื่อทั้งใบ
    /// ส่วนอีกสี่อันไม่ได้ช้า มันรอ ซึ่งไม่ใช่ความผิดของใคร
    static func blockingStage(
        _ stages: [WorkStageRow],
        today: Date,
        calendar: Calendar
    ) -> WorkStageRow? {
        stages
            .sorted { $0.position < $1.position }
            .first { stage in
                guard !stage.isClosed, let end = stage.plannedEndDate else { return false }
                return calendar.startOfDay(for: end) < calendar.startOfDay(for: today)
            }
    }
```

- [ ] **Step 2: ให้ `daysLate` นับจากตัวที่บล็อกตัวเดียว**

แทนที่ `daysLate` เดิมด้วย:

```swift
    /// ช้ากี่วัน นับจาก stage ที่บล็อกอยู่ตัวเดียว ไม่ใช่ตัวที่เลยกำหนดมานานที่สุด
    /// คืน nil เมื่อไม่มีอะไรบล็อก — ต่างจาก 0 ซึ่งจะแปลว่า "เลยกำหนดวันนี้พอดี"
    static func daysLate(_ stages: [WorkStageRow], today: Date, calendar: Calendar) -> Int? {
        guard let blocking = blockingStage(stages, today: today, calendar: calendar),
              let end = blocking.plannedEndDate
        else { return nil }

        return calendar.dateComponents(
            [.day],
            from: calendar.startOfDay(for: end),
            to: calendar.startOfDay(for: today)
        ).day
    }
```

- [ ] **Step 3: แก้ `isFinished` และตัวกรองที่อ้างวันจริง**

```swift
    /// ทุก stage ปิดครบ = งานเสร็จ ไม่ต้องมีคอลัมน์ status
    static func isFinished(_ stages: [WorkStageRow]) -> Bool {
        !stages.isEmpty && stages.allSatisfy(\.isClosed)
    }
```

ใน `matches(_:today:calendar:)` แก้สองสาขา:

```swift
        case .active:
            // เริ่มแล้ว = ถึงวันเริ่มของ stage แรกแล้ว ไม่ใช่ = มีใครกดปุ่ม
            // สเปกรอบ 4 ตัด actual_start ทิ้งเพราะงานเริ่มตามแผนเสมอ
            // พอเริ่มไม่ได้ตามแผน สิ่งที่เกิดคือเลื่อน timeline ไม่ใช่บันทึกส่วนต่าง
            guard !Self.isFinished(stages) else { return false }
            guard let first = stages.compactMap(\.plannedStartDate).min() else { return false }
            return calendar.startOfDay(for: first) <= calendar.startOfDay(for: today)

        case .overdue:
            return Self.blockingStage(stages, today: today, calendar: calendar) != nil
```

และ:

```swift
        case .startingSoon:
            guard !Self.isFinished(stages) else { return false }
            guard let first = stages.compactMap(\.plannedStartDate).min() else { return false }
            // ยังไม่ถึงวันเริ่ม — งานที่เลยวันเริ่มมาแล้วอยู่ใน Active ไม่ใช่ที่นี่
            guard calendar.startOfDay(for: first) > calendar.startOfDay(for: today) else { return false }
            guard let limit = calendar.date(byAdding: .day, value: Self.startingSoonWindow, to: today)
            else { return false }
            return first <= limit
```

- [ ] **Step 4: เติม harness**

เติมท้าย `$SCRATCH/main.swift` ก่อนบรรทัด `print(failures == 0 ...`:

```swift
// ── Overdue ทีละอัน ──────────────────────────────────────────────

func at(_ position: Int, _ s: WorkStageRow) -> WorkStageRow {
    WorkStageRow(
        id: s.id, code: s.code, name: s.name, position: position,
        plannedStart: s.plannedStart, plannedEnd: s.plannedEnd,
        baselineStart: s.baselineStart, baselineEnd: s.baselineEnd,
        task: s.task
    )
}

let blocked = [
    at(1, stage("RU", "2026-08-01", "2026-09-01", [task("user ออก proposal", done: nil)])),
    at(2, stage("RI", "2026-09-02", "2026-09-10", [task("สรุป requirement", done: nil)])),
    at(3, stage("DEV", "2026-09-11", "2026-09-18", [task("เขียนโค้ด", done: nil)])),
]

let blocking = WorkFilter.blockingStage(blocked, today: today, calendar: cal)
check("stage ที่บล็อกคือตัวแรกที่ยังไม่ปิดและเลยกำหนด", blocking?.code == "RU")
check("ช้านับจากตัวที่บล็อก ไม่ใช่ตัวที่เลยมานานที่สุด",
      WorkFilter.daysLate(blocked, today: today, calendar: cal) == 19)
check("Overdue เข้าเกณฑ์เมื่อมีตัวบล็อก",
      WorkFilter.overdue.matches(blocked, today: today, calendar: cal))

let firstClosed = [
    at(1, stage("RU", "2026-08-01", "2026-09-01", [task("user ออก proposal", done: "2026-09-03T02:00:00Z")])),
    at(2, stage("RI", "2026-09-02", "2026-09-10", [task("สรุป requirement", done: nil)])),
]
check("ปิดตัวแรกแล้ว ตัวบล็อกขยับมาเป็นตัวถัดไป",
      WorkFilter.blockingStage(firstClosed, today: today, calendar: cal)?.code == "RI")

let allClosed = [
    at(1, stage("RU", "2026-08-01", "2026-09-01", [task("a", done: "2026-08-15T00:00:00Z")])),
    at(2, stage("RI", "2026-09-02", "2026-09-10", [task("b", done: "2026-09-09T00:00:00Z")])),
]
check("ปิดครบแล้วไม่มีอะไรบล็อก",
      WorkFilter.blockingStage(allClosed, today: today, calendar: cal) == nil)
check("ปิดครบแล้วงานเสร็จ", WorkFilter.isFinished(allClosed))
check("งานที่เสร็จแล้วไม่อยู่ใน Active",
      WorkFilter.active.matches(allClosed, today: today, calendar: cal) == false)

let notStarted = [at(1, stage("RU", "2026-10-01", "2026-11-01", [task("a", done: nil)]))]
check("ยังไม่ถึงวันเริ่ม อยู่ใน Starting soon",
      WorkFilter.startingSoon.matches(notStarted, today: today, calendar: cal))
check("ยังไม่ถึงวันเริ่ม ไม่อยู่ใน Active",
      WorkFilter.active.matches(notStarted, today: today, calendar: cal) == false)
check("เลยวันเริ่มแล้วอยู่ใน Active",
      WorkFilter.active.matches(blocked, today: today, calendar: cal))
check("เลยวันเริ่มแล้วไม่อยู่ใน Starting soon",
      WorkFilter.startingSoon.matches(blocked, today: today, calendar: cal) == false)
check("stage ที่ไม่มี task เลยไม่ทำให้งานนับว่าเสร็จ",
      WorkFilter.isFinished([at(1, empty)]) == false)
```

- [ ] **Step 5: รัน harness ให้ผ่านทุกข้อ**

```bash
swiftc -o "$SCRATCH/run" \
  null-app/Features/work/WorkRows.swift \
  null-app/Features/work/WorkFilter.swift \
  "$SCRATCH/main.swift" && "$SCRATCH/run"
```

Expected: `ALL PASS`

- [ ] **Step 6: Commit**

```bash
git add null-app/Features/work/WorkFilter.swift
git commit -m "Let one stage be late at a time"
```

---

### Task 4: การเลื่อนแผนเมื่อปิด stage ช้า

**Files:**
- Create: `null-app/Features/work/WorkSchedule.swift`
- Test: harness เดิม

**Interfaces:**
- Consumes: `WorkStageRow` จาก Task 2
- Produces: `WorkSchedule.shifts(after:in:closedOn:calendar:) -> [WorkSchedule.Shift]` และ `WorkSchedule.Shift { stageID: UUID, plannedStart: Date, plannedEnd: Date }`

- [ ] **Step 1: เขียน `WorkSchedule.swift`**

```swift
//
//  WorkSchedule.swift
//  null-app
//

import Foundation

/// การเลื่อนแผนที่เหลือเมื่อ stage หนึ่งปิดช้ากว่ากำหนด
///
/// **ห้าม import Supabase ในไฟล์นี้** — ดูเหตุผลใน WorkRows.swift
///
/// แยกออกมาจาก `WorkStore` เพราะเป็นเลขล้วน ๆ ที่ทดสอบได้โดยไม่ต้องมีเซิร์ฟเวอร์
/// และเพราะการคำนวณผิดตรงนี้จะเขียนทับวันของ stage หลายอันพร้อมกันโดยไม่มีทางย้อน
nonisolated enum WorkSchedule {
    /// วันชุดใหม่ของ stage หนึ่งอัน
    nonisolated struct Shift: Sendable, Equatable {
        let stageID: UUID
        let plannedStart: Date
        let plannedEnd: Date
    }

    /// stage ที่อยู่หลัง `closed` และยังไม่ปิด ต้องเลื่อนไปกี่วันและเป็นวันอะไร
    ///
    /// **เลื่อนทุกอันด้วยจำนวนวันเท่ากัน ไม่ใช่ไล่ต่อท้ายตัวก่อนหน้าทีละอัน** —
    /// สเปกระบุว่า stage ทับกันได้ (PT คาบ SIT & UAT, IMP คาบ PVT) การไล่ต่อท้าย
    /// จะดึงของที่ตั้งใจให้ทับมาเรียงเป็นแถวเดี่ยว ผลลัพธ์เหมือนกันเป๊ะเมื่อ stage เรียงต่อกันอยู่แล้ว
    ///
    /// **ปิดเร็วกว่าแผนไม่ดึงอะไรขึ้นมา** — การที่เราเสร็จก่อนไม่ได้แปลว่าทีมถัดไป
    /// ว่างรับงานเร็วขึ้น การดึงแผนขึ้นเองจะสร้างวันที่ไม่มีใครตกลงด้วย
    static func shifts(
        after closed: WorkStageRow,
        in stages: [WorkStageRow],
        closedOn: Date,
        calendar: Calendar
    ) -> [Shift] {
        guard let plannedEnd = closed.plannedEndDate else { return [] }

        let delta = calendar.dateComponents(
            [.day],
            from: calendar.startOfDay(for: plannedEnd),
            to: calendar.startOfDay(for: closedOn)
        ).day ?? 0

        guard delta > 0 else { return [] }

        return stages
            .filter { $0.position > closed.position && !$0.isClosed }
            .compactMap { stage in
                guard let start = stage.plannedStartDate,
                      let end = stage.plannedEndDate,
                      let newStart = calendar.date(byAdding: .day, value: delta, to: start),
                      let newEnd = calendar.date(byAdding: .day, value: delta, to: end)
                else { return nil }

                return Shift(stageID: stage.id, plannedStart: newStart, plannedEnd: newEnd)
            }
    }
}
```

- [ ] **Step 2: เติม harness**

เติมท้าย `$SCRATCH/main.swift` ก่อน `print(failures == 0 ...`:

```swift
// ── การเลื่อนแผน ─────────────────────────────────────────────────

let ru = at(1, stage("RU", "2026-08-01", "2026-09-01", [task("a", done: "2026-09-30T04:00:00Z")]))
let ri = at(2, stage("RI", "2026-09-02", "2026-10-02", [task("b", done: nil)]))
let dev = at(3, stage("DEV", "2026-09-15", "2026-11-15", [task("c", done: nil)]))
let done = at(4, stage("PT", "2026-11-16", "2026-12-16", [task("d", done: "2026-08-01T00:00:00Z")]))
let chain = [ru, ri, dev, done]

let late = WorkSchedule.shifts(after: ru, in: chain, closedOn: instant("2026-09-30T04:00:00Z"), calendar: cal)
check("เลื่อนเฉพาะ stage ที่อยู่ข้างหลังและยังไม่ปิด", late.count == 2)
check("เลื่อนด้วยจำนวนวันเท่ากันทุกอัน — RI",
      late.first { $0.stageID == ri.id }?.plannedStart == day("2026-10-01"))
check("ปลายทางของ RI ก็ขยับเท่ากัน",
      late.first { $0.stageID == ri.id }?.plannedEnd == day("2026-10-31"))
check("DEV ที่ทับกับ RI ยังทับเท่าเดิม ไม่ถูกดึงมาต่อท้าย",
      late.first { $0.stageID == dev.id }?.plannedStart == day("2026-10-14"))
check("stage ที่ปิดไปแล้วไม่ขยับ", late.contains { $0.stageID == done.id } == false)

let onTime = at(1, stage("RU", "2026-08-01", "2026-09-01", [task("a", done: "2026-09-01T09:00:00Z")]))
check("ปิดตรงวันไม่เลื่อนอะไร",
      WorkSchedule.shifts(after: onTime, in: chain, closedOn: instant("2026-09-01T09:00:00Z"), calendar: cal).isEmpty)

check("ปิดเร็วกว่าแผนไม่ดึงอะไรขึ้นมา",
      WorkSchedule.shifts(after: onTime, in: chain, closedOn: instant("2026-08-20T09:00:00Z"), calendar: cal).isEmpty)

let lastOne = at(9, stage("IMP", "2027-01-01", "2027-02-01", [task("z", done: "2027-03-01T00:00:00Z")]))
check("ปิดตัวสุดท้ายช้าก็ไม่มีอะไรให้เลื่อน",
      WorkSchedule.shifts(after: lastOne, in: [lastOne], closedOn: instant("2027-03-01T00:00:00Z"), calendar: cal).isEmpty)
```

- [ ] **Step 3: รัน harness ให้ผ่านทุกข้อ**

```bash
swiftc -o "$SCRATCH/run" \
  null-app/Features/work/WorkRows.swift \
  null-app/Features/work/WorkFilter.swift \
  null-app/Features/work/WorkSchedule.swift \
  "$SCRATCH/main.swift" && "$SCRATCH/run"
```

Expected: `ALL PASS`

- [ ] **Step 4: Commit**

```bash
git add null-app/Features/work/WorkSchedule.swift
git commit -m "Work out which dates move when a stage closes late"
```

---

### Task 5: draft และ payload ที่เลิกรู้จักวันจริง

**Files:**
- Modify: `null-app/Features/work/WorkDraft.swift`
- Modify: `null-app/Features/work/WorkPayload.swift`
- Test: harness เดิม

**Interfaces:**
- Consumes: `WorkTaskRow`, `WorkStageRow` จาก Task 2
- Produces:
  - `WorkStageDraft` ไม่มี `actualStart` / `actualEnd` อีกต่อไป
  - `WorkStagePayload.init(insertFor:draft:position:)` ส่ง `baseline_start` / `baseline_end` เท่ากับ planned
  - `WorkStagePayload.init(updateFor:draft:position:)` **ไม่แตะ** baseline
  - `WorkStagePayload.init(shift:)` สำหรับการเลื่อน — ส่งเฉพาะ `id` กับวันตามแผนใหม่
  - `WorkTaskPayload.init(insertFor stageID:title:position:)` และ `.init(doneFor id:doneAt:)`

- [ ] **Step 1: ตัดวันจริงออกจาก `WorkStageDraft`**

ใน `WorkDraft.swift` ลบ `var actualStart: Date?` และ `var actualEnd: Date?` ลบการกำหนดค่าทั้งสองใน `init(_ row:knownCodes:)` และลบสองบล็อกนี้ออกจาก `validationError(calendar:)`:

```swift
        if actualEnd != nil && actualStart == nil { ... }
        if let start = actualStart, let end = actualEnd, ... { ... }
```

เติม doc comment บน struct:

```swift
/// stage หนึ่งแถวที่กำลังถูกแก้อยู่
///
/// **ไม่มีวันจริงในนี้แล้ว** — รอบ 4 ตัด `actual_start` / `actual_end` ทิ้งทั้งคู่
/// เพราะ stage เริ่มตามแผนเสมอ (พอไม่ตามก็แก้แผนแทน) และวันปิดคำนวณจาก task ได้
```

- [ ] **Step 2: แก้ `WorkStagePayload`**

ลบ `actualStart` / `actualEnd` ทั้งจาก property, `CodingKeys` และ `encode(to:)` แล้วเพิ่ม baseline:

```swift
    let id: UUID?
    let workID: UUID?
    let code: String
    let name: String
    let position: Int
    let plannedStart: String
    let plannedEnd: String
    /// ส่งเฉพาะตอน insert — แผนแรกเขียนครั้งเดียวแล้วห้ามแตะอีก
    /// **นี่คือฟิลด์ที่ "ไม่ส่ง" เป็นพฤติกรรมที่ถูกต้องตอน update**
    let baselineStart: String?
    let baselineEnd: String?

    enum CodingKeys: String, CodingKey {
        case id
        case workID = "work_id"
        case code, name, position
        case plannedStart = "planned_start"
        case plannedEnd = "planned_end"
        case baselineStart = "baseline_start"
        case baselineEnd = "baseline_end"
    }
```

`private init` เดิมเพิ่มพารามิเตอร์ `baseline: Bool` แล้วตั้งค่า:

```swift
        baselineStart = baseline ? WorkStageRow.dayFormatter.string(from: draft.plannedStart) : nil
        baselineEnd = baseline ? WorkStageRow.dayFormatter.string(from: draft.plannedEnd) : nil
```

`init(insertFor:draft:position:)` เรียกด้วย `baseline: true` · `init(updateFor:draft:position:)` เรียกด้วย `baseline: false`

`encode(to:)` เอาสองบรรทัดของ actual ออก แล้วใส่:

```swift
        // baseline ส่งเฉพาะตอน insert — ตอน update ต้อง**ไม่มีคีย์** เพื่อให้ PostgREST
        // ไม่แตะคอลัมน์ ต่างจากฟิลด์อื่นในไฟล์นี้ที่ต้องส่งเสมอแม้เป็น null
        if let baselineStart { try container.encode(baselineStart, forKey: .baselineStart) }
        if let baselineEnd { try container.encode(baselineEnd, forKey: .baselineEnd) }
```

- [ ] **Step 3: เพิ่ม payload ของการเลื่อน**

ท้าย `WorkStagePayload` เพิ่ม:

```swift
    /// payload ของการเลื่อนแผน — ส่งเฉพาะ id กับวันใหม่ ไม่แตะชื่อ รหัส ลำดับ หรือ baseline
    init(shift: WorkSchedule.Shift, position: Int) {
        id = shift.stageID
        workID = nil
        code = ""
        name = ""
        self.position = position
        plannedStart = WorkStageRow.dayFormatter.string(from: shift.plannedStart)
        plannedEnd = WorkStageRow.dayFormatter.string(from: shift.plannedEnd)
        baselineStart = nil
        baselineEnd = nil
    }
```

**เตือน:** `init(shift:position:)` ตั้ง `code` และ `name` เป็นสตริงว่างซึ่ง `encode(to:)`
จะส่งขึ้นไปและชน `stage_code_not_blank` — ห้ามใช้ payload ตัวนี้กับ `.update()` ทั้งก้อน
`WorkStore` ใน Task 6 ต้องส่งเฉพาะสองคอลัมน์ของวันด้วย dictionary แทน ดูขั้นตอนที่นั่น
**ถ้าอ่านถึงตรงนี้แล้วรู้สึกว่าเสี่ยง ให้ลบ `init(shift:position:)` ทิ้งแล้วใช้ dictionary อย่างเดียว** —
ทางนั้นสั้นกว่าและไม่มีกับดัก

- [ ] **Step 4: เพิ่ม `WorkTaskPayload`**

ท้าย `WorkPayload.swift`:

```swift
/// task หนึ่งอันที่กำลังจะถูกส่งขึ้น
///
/// `done_at` เป็น `timestamptz` จึงส่งเป็น ISO8601 ผ่าน `WorkTaskRow.instantFormatter`
/// ไม่ใช่ `yyyy-MM-dd` แบบวันของ stage — คนละชนิดคอลัมน์ คนละตัวแปลง
nonisolated struct WorkTaskPayload: Encodable, Sendable {
    let stageID: UUID?
    let title: String?
    let position: Int?
    /// `.some(nil)` = ติ๊กออก ต้องส่ง null · `.none` = ไม่แตะคอลัมน์นี้
    let doneAt: Date??

    enum CodingKeys: String, CodingKey {
        case title, position
        case stageID = "stage_id"
        case doneAt = "done_at"
    }

    init(insertFor stageID: UUID, title: String, position: Int) {
        self.stageID = stageID
        self.title = title.trimmingCharacters(in: .whitespacesAndNewlines)
        self.position = position
        doneAt = nil
    }

    /// ติ๊กเสร็จหรือติ๊กออก — แตะคอลัมน์เดียว
    init(done: Date?) {
        stageID = nil
        title = nil
        position = nil
        doneAt = .some(done)
    }

    func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        if let stageID { try container.encode(stageID, forKey: .stageID) }
        if let title { try container.encode(title, forKey: .title) }
        if let position { try container.encode(position, forKey: .position) }

        // หัวใจของไฟล์นี้ — ติ๊ก task ออกต้องส่ง null ไม่ใช่ปล่อยคีย์หายไป
        // ถ้าคีย์หาย PostgREST จะไม่แตะคอลัมน์ แล้ว task ที่ติ๊กออกจะยังเสร็จอยู่
        // และ stage ก็จะยังปิดอยู่ทั้งที่ผู้ใช้เพิ่งเปิดมันกลับมา
        if let doneAt {
            if let value = doneAt {
                try container.encode(WorkTaskRow.instantFormatter.string(from: value), forKey: .doneAt)
            } else {
                try container.encodeNil(forKey: .doneAt)
            }
        }
    }
}
```

- [ ] **Step 5: เขียน harness ที่อ่าน JSON จริง**

เขียนทับ `$SCRATCH/main.swift` (เก็บ helper `check`, `json`, `keys`, `isNull` แบบเดียวกับ harness ของรอบ 3) แล้วใส่:

```swift
import Foundation

var failures = 0
func check(_ name: String, _ condition: Bool) {
    if condition { print("ok   \(name)") } else { print("FAIL \(name)"); failures += 1 }
}

let cal = WorkFilter.calendar
func day(_ text: String) -> Date { WorkStageRow.dayFormatter.date(from: text)! }
func instant(_ text: String) -> Date { WorkTaskRow.instantFormatter.date(from: text)! }

func json<T: Encodable>(_ value: T) -> String {
    let e = JSONEncoder(); e.outputFormatting = [.sortedKeys]
    return String(data: try! e.encode(value), encoding: .utf8)!
}
func keys<T: Encodable>(_ value: T) -> Set<String> {
    let data = try! JSONEncoder().encode(value)
    return Set((try! JSONSerialization.jsonObject(with: data) as! [String: Any]).keys)
}
func isNull<T: Encodable>(_ value: T, _ key: String) -> Bool {
    let data = try! JSONEncoder().encode(value)
    return (try! JSONSerialization.jsonObject(with: data) as! [String: Any])[key] is NSNull
}

let type = WorkStageTypeRow(code: "RU", label: "Requirement · User", position: 1)
var draft = WorkStageDraft(type: type, startingFrom: day("2026-09-01"), calendar: cal)
draft.plannedEnd = day("2026-09-30")

let insert = WorkStagePayload(insertFor: UUID(), draft: draft, position: 1)
check("INSERT ส่ง baseline เท่ากับแผน ณ วันสร้าง",
      json(insert).contains("\"baseline_start\":\"2026-09-01\""))
check("INSERT ส่ง baseline_end ด้วย", json(insert).contains("\"baseline_end\":\"2026-09-30\""))
check("INSERT ไม่มีคอลัมน์วันจริงอีกแล้ว",
      !keys(insert).contains("actual_start") && !keys(insert).contains("actual_end"))

let update = WorkStagePayload(updateFor: UUID(), draft: draft, position: 2)
check("UPDATE ไม่เอ่ยถึง baseline เลย — แผนแรกห้ามถูกเขียนทับ",
      !keys(update).contains("baseline_start") && !keys(update).contains("baseline_end"))

let stageID = UUID()
let add = WorkTaskPayload(insertFor: stageID, title: "  user ออก proposal  ", position: 1)
check("INSERT task ผูกกับ stage", keys(add).contains("stage_id"))
check("ชื่อ task ไทยถูกตัดช่องว่างหัวท้าย", json(add).contains("\"title\":\"user ออก proposal\""))
check("INSERT task ไม่เอ่ยถึง done_at — ปล่อยให้เป็น null ตาม default",
      !keys(add).contains("done_at"))

let tick = WorkTaskPayload(done: instant("2026-09-20T04:30:00Z"))
check("ติ๊กเสร็จส่ง done_at เป็น ISO8601", json(tick).contains("2026-09-20T04:30:00Z"))
check("ติ๊กเสร็จแตะคอลัมน์เดียว", keys(tick) == ["done_at"])

let untick = WorkTaskPayload(done: nil)
check("ติ๊กออกส่ง null ไม่ใช่คีย์ที่หายไป", isNull(untick, "done_at"))
check("ติ๊กออกก็ยังแตะคอลัมน์เดียว", keys(untick) == ["done_at"])

print(failures == 0 ? "ALL PASS" : "\(failures) FAILED")
exit(failures == 0 ? 0 : 1)
```

- [ ] **Step 6: รัน harness ให้ผ่านทุกข้อ**

```bash
swiftc -o "$SCRATCH/run" \
  null-app/Features/work/WorkRows.swift \
  null-app/Features/work/WorkFilter.swift \
  null-app/Features/work/WorkSchedule.swift \
  null-app/Features/work/WorkDraft.swift \
  null-app/Features/work/WorkPayload.swift \
  "$SCRATCH/main.swift" && "$SCRATCH/run"
```

Expected: `ALL PASS`

- [ ] **Step 7: Commit**

```bash
git add null-app/Features/work/WorkDraft.swift null-app/Features/work/WorkPayload.swift
git commit -m "Send tasks up, and stop sending dates that no longer exist"
```

---

### Task 6: `WorkStore` อ่าน task เขียน task และเลื่อนแผนให้เอง

**Files:**
- Modify: `null-app/Features/work/WorkStore.swift`

**Interfaces:**
- Consumes: `WorkTaskPayload`, `WorkStagePayload`, `WorkSchedule` จาก Task 4–5
- Produces:
  - `WorkStore.addTask(_ title: String, to stageID: UUID) async -> Bool`
  - `WorkStore.setTask(_ id: UUID, done: Bool, in workID: UUID) async -> Bool`
  - `WorkStore.deleteTask(_ id: UUID) async -> Bool`
  - `WorkStore.lastShiftCount: Int` — จำนวน stage ที่เพิ่งถูกเลื่อน ไว้ขึ้นแถบบอกผู้ใช้ · `clearLastShift()`

- [ ] **Step 1: ดึง task มาพร้อมกับที่เหลือ**

แก้ `workSelect`:

```swift
    private static let workSelect = """
        id,type_code,name,description,requested_by,updated_at,\
        stage(id,code,name,position,planned_start,planned_end,baseline_start,baseline_end,\
        task(id,title,done_at,position))
        """
```

ใน `load()` หลังเรียง stage เดิม ให้เรียง task ด้วย — เซิร์ฟเวอร์ไม่รับประกันลำดับของ embed:

```swift
            works = loadedWorks.map { work in
                var sorted = work
                sorted.stage.sort { $0.position < $1.position }
                sorted.stage = sorted.stage.map { stage in
                    var s = stage
                    s.task.sort { $0.position < $1.position }
                    return s
                }
                return sorted
            }
```

- [ ] **Step 2: เพิ่ม state ของการเลื่อน**

ใต้ `private(set) var saveError: String?`:

```swift
    /// จำนวน stage ที่เพิ่งถูกเลื่อนจากการปิด stage ช้า — 0 = ไม่มีอะไรเลื่อน
    /// หน้าจอเอาไปขึ้นแถบบอก เพราะการเขียนทับวันของหลาย stage ในคลิกเดียว
    /// ที่ไม่บอกอะไรเลยคือการเปลี่ยนแผนลับหลังผู้ใช้
    private(set) var lastShiftCount = 0

    func clearLastShift() { lastShiftCount = 0 }
```

- [ ] **Step 3: เขียนสามฟังก์ชันของ task**

เติมท้ายคลาส:

```swift
    /// เพิ่ม task ต่อท้ายรายการของ stage นั้น
    /// `position` มาจากจำนวนที่มีอยู่ ไม่ใช่ช่องให้พิมพ์เลขเอง
    func addTask(_ title: String, to stageID: UUID) async -> Bool {
        let trimmed = title.trimmingCharacters(in: .whitespacesAndNewlines)
        guard !trimmed.isEmpty else { return false }

        let existing = works
            .flatMap(\.stage)
            .first { $0.id == stageID }?
            .task.count ?? 0

        do {
            try await Backend.client
                .schema("f_work").from("task")
                .insert(WorkTaskPayload(insertFor: stageID, title: trimmed, position: existing + 1))
                .execute()
            saveError = nil
            await load()
            return true
        } catch {
            fail(error)
            return false
        }
    }

    /// ติ๊กเสร็จหรือติ๊กออก แล้วถ้าการติ๊กนั้นทำให้ stage ปิดช้ากว่ากำหนด
    /// ให้เลื่อนแผนของ stage ที่เหลือตามไปด้วยในคำขอเดียวกัน
    func setTask(_ id: UUID, done: Bool, in workID: UUID) async -> Bool {
        do {
            try await Backend.client
                .schema("f_work").from("task")
                .update(WorkTaskPayload(done: done ? Date() : nil))
                .eq("id", value: id)
                .execute()

            saveError = nil
            await load()
            await shiftIfClosedLate(workID: workID)
            return true
        } catch {
            fail(error)
            return false
        }
    }

    func deleteTask(_ id: UUID) async -> Bool {
        do {
            try await Backend.client
                .schema("f_work").from("task")
                .delete()
                .eq("id", value: id)
                .execute()
            saveError = nil
            await load()
            return true
        } catch {
            fail(error)
            return false
        }
    }
```

- [ ] **Step 4: เขียนการเลื่อนแผน**

```swift
    /// หลังโหลดใหม่แล้ว ดูว่ามี stage ไหนเพิ่งปิดช้ากว่ากำหนดหรือไม่ ถ้ามีก็เลื่อนที่เหลือ
    ///
    /// **ส่งขึ้นเป็น dictionary ไม่ใช่ `WorkStagePayload`** — payload ตัวนั้นส่งทุกคอลัมน์
    /// เสมอตามกติกาของไฟล์ ซึ่งจะเขียนทับ `code`/`name` ด้วยค่าที่เราไม่ได้ตั้งใจแก้
    /// ที่นี่ต้องแตะแค่สองคอลัมน์ของวันเท่านั้น
    private func shiftIfClosedLate(workID: UUID) async {
        guard let work = works.first(where: { $0.id == workID }) else { return }

        let calendar = WorkFilter.calendar
        let candidates = work.stage.filter { $0.isClosed }

        for closed in candidates {
            guard let closedOn = closed.closedOn else { continue }

            let shifts = WorkSchedule.shifts(
                after: closed,
                in: work.stage,
                closedOn: closedOn,
                calendar: calendar
            )
            guard !shifts.isEmpty else { continue }

            do {
                for shift in shifts {
                    try await Backend.client
                        .schema("f_work").from("stage")
                        .update([
                            "planned_start": WorkStageRow.dayFormatter.string(from: shift.plannedStart),
                            "planned_end": WorkStageRow.dayFormatter.string(from: shift.plannedEnd),
                        ])
                        .eq("id", value: shift.stageID)
                        .execute()
                }
                lastShiftCount = shifts.count
                await load()
            } catch {
                fail(error)
            }
            return
        }
    }
```

**หมายเหตุที่ต้องอ่านก่อนเขียน:** ลูปนี้เลื่อนได้ครั้งละหนึ่ง stage ที่ปิดช้า (มี `return` ท้ายบล็อก)
เพราะหลังเลื่อนแล้ววันของทุกอันเปลี่ยน การเลื่อนต่อในรอบเดียวกันจะคำนวณจากข้อมูลเก่า
กรณีปิดหลาย stage รวดเดียวจึงต้องให้ผู้ใช้ติ๊กทีละอัน ซึ่งเป็นสิ่งที่เกิดขึ้นจริงอยู่แล้ว

- [ ] **Step 5: build ทั้งสองแพลตฟอร์ม แล้ว commit**

```bash
xcodebuild -scheme null-app -destination 'generic/platform=iOS Simulator' build 2>&1 | tail -3
```

Expected: `** BUILD SUCCEEDED **`

```bash
xcodebuild -scheme null-app -destination 'platform=macOS' build 2>&1 | tail -3
```

Expected: `** BUILD SUCCEEDED **`

```bash
git add null-app/Features/work/WorkStore.swift
git commit -m "Let the store tick tasks, and move the plan when one closes late"
```

---

### Task 7: รายการ task บนหน้างาน

**Files:**
- Create: `null-app/Features/work/WorkTaskListView.swift`
- Modify: `null-app/Features/work/WorkStageEditorView.swift`

**Interfaces:**
- Consumes: `WorkStore.addTask/setTask/deleteTask/lastShiftCount` จาก Task 6
- Produces: `WorkTaskListView(stage:workID:store:)`

- [ ] **Step 1: เขียน `WorkTaskListView.swift`**

```swift
//
//  WorkTaskListView.swift
//  null-app
//

import SwiftUI

/// รายการ task ของ stage ที่เลือกอยู่
///
/// ติ๊กแล้วบันทึกทันที ไม่มีปุ่ม Save — แบบเดียวกับที่หน้า stage ทำ
/// การติ๊กคือการรายงานว่าทำอะไรเสร็จ ถ้าต้องกดยืนยันอีกทีมันจะกลายเป็นงานเอกสาร
struct WorkTaskListView: View {
    let stage: WorkStageRow
    let workID: UUID
    let store: WorkStore

    @State private var newTitle = ""
    @State private var isAdding = false
    @FocusState private var isTypingNew: Bool

    var body: some View {
        List {
            ForEach(stage.task) { task in
                Button {
                    Task { _ = await store.setTask(task.id, done: !task.isDone, in: workID) }
                } label: {
                    HStack(spacing: 12) {
                        Image(systemName: task.isDone ? "checkmark.circle.fill" : "circle")
                            .foregroundStyle(task.isDone ? AnyShapeStyle(Color.accentColor) : AnyShapeStyle(.tertiary))
                            .font(.title3)

                        Text(task.title)
                            .strikethrough(task.isDone, color: .secondary)
                            .foregroundStyle(task.isDone ? AnyShapeStyle(.secondary) : AnyShapeStyle(.primary))
                            // ชื่อ task ภาษาไทยไม่มีช่องว่าง ปล่อยให้ระบบตัดบรรทัดเอง
                            .fixedSize(horizontal: false, vertical: true)

                        Spacer(minLength: 0)
                    }
                    .contentShape(Rectangle())
                }
                .buttonStyle(.plain)
                .swipeActions(edge: .trailing) {
                    Button("Delete", role: .destructive) {
                        Task { _ = await store.deleteTask(task.id) }
                    }
                }
            }

            if isAdding {
                HStack(spacing: 12) {
                    Image(systemName: "circle")
                        .foregroundStyle(.tertiary)
                        .font(.title3)

                    TextField("What has to happen", text: $newTitle)
                        .focused($isTypingNew)
                        .onSubmit(commit)
                }
            }
        }
        .listStyle(.plain)
        .onChange(of: isTypingNew) { _, focused in
            // เลิกพิมพ์แล้วยังไม่ได้ใส่อะไร ให้แถวหายไปเอง ไม่ต้องมีปุ่มยกเลิก
            if !focused { commit() }
        }
    }

    /// เรียกจากหน้าแม่ผ่านปุ่ม New Task
    func startAdding() {
        isAdding = true
        isTypingNew = true
    }

    private func commit() {
        let title = newTitle.trimmingCharacters(in: .whitespacesAndNewlines)
        newTitle = ""
        isAdding = false

        guard !title.isEmpty else { return }
        Task { _ = await store.addTask(title, to: stage.id) }
    }
}
```

**ปัญหาที่ต้องแก้ตอนต่อสาย:** `startAdding()` เรียกจากภายนอกไม่ได้เพราะ `@State` เป็นของ view
ให้เปลี่ยนเป็นรับ `@Binding var isAdding: Bool` จากหน้าแม่แทน แล้วปุ่ม New Task บนหน้าแม่
ตั้งค่าตัวนั้นเป็น true — ทำในขั้นตอนถัดไป

- [ ] **Step 2: เปลี่ยน `isAdding` เป็น Binding แล้วต่อสายกับปุ่ม New Task**

ใน `WorkTaskListView` เปลี่ยน `@State private var isAdding = false` เป็น `@Binding var isAdding: Bool`
และลบเมธอด `startAdding()` ทิ้ง

ใน `WorkStageEditorView`:

1. เติม state: `@State private var isAddingTask = false`
2. ในการ์ด Progress เปลี่ยนตัวเลขให้อ่านจริง — แทนที่ `let done = 0` / `let total = 0` ด้วย:

```swift
            let row = work?.stage.first { $0.id == selectedDraft?.id }
            let done = row?.doneCount ?? 0
            let total = row?.taskCount ?? 0
```

3. ปุ่ม New Task เปิดใช้งาน:

```swift
                Button("New Task") { isAddingTask = true }
                    .buttonStyle(.bordered)
                    .controlSize(.small)
```

4. แทน `tasks` เดิมด้วยรายการจริง โดยคง gesture ปัดเปลี่ยน stage ไว้ที่เดิม:

```swift
    @ViewBuilder
    private var tasks: some View {
        if let row = work?.stage.first(where: { $0.id == selectedDraft?.id }) {
            if row.task.isEmpty && !isAddingTask {
                VStack(spacing: 8) {
                    Spacer()
                    Text("No tasks yet")
                        .font(.subheadline)
                        .foregroundStyle(.secondary)
                    Text("A stage closes when all of its tasks are done.")
                        .font(.footnote)
                        .foregroundStyle(.tertiary)
                        .multilineTextAlignment(.center)
                    Spacer()
                }
                .frame(maxWidth: .infinity)
                .padding(.horizontal, 32)
            } else {
                WorkTaskListView(
                    stage: row,
                    workID: workID,
                    store: store,
                    isAdding: $isAddingTask
                )
            }
        }
    }
```

- [ ] **Step 3: ขึ้นแถบบอกเมื่อแผนถูกเลื่อน**

ใน `WorkStageEditorView` เติมใต้บล็อก error ที่มีอยู่:

```swift
            if store.lastShiftCount > 0 {
                HStack(spacing: 8) {
                    Image(systemName: "calendar.badge.clock")
                    Text("Moved \(store.lastShiftCount) later stage\(store.lastShiftCount == 1 ? "" : "s") to keep the gap.")
                        .font(.footnote)
                    Spacer(minLength: 0)
                    Button("OK") { store.clearLastShift() }
                        .font(.footnote)
                }
                .padding()
                .background(.quaternary.opacity(0.4))
            }
```

การเขียนทับวันของหลาย stage ในคลิกเดียวโดยไม่บอกอะไรเลย คือการเปลี่ยนแผนลับหลังผู้ใช้

- [ ] **Step 4: ตัดสวิตช์ออกจากหน้าแก้ stage**

ใน `WorkStageDetailView` ลบ `Section { Toggle("Started"...) }`, `Section { Toggle("Finished"...) }`,
ทั้งสองบล็อก `if draft.actualStart != nil` / `actualEnd != nil`, เมธอด `startedToggle`,
`finishedToggle` และ `dateRow(_:date:)` ทิ้งทั้งหมด เหลือ Planned / Order / Remove

เติม doc comment บน struct:

```swift
/// แก้ stage หนึ่งอัน
///
/// **ไม่มีสวิตช์ Started / Finished แล้ว** — stage ปิดเมื่อ task ครบ ไม่ใช่เมื่อกดปุ่ม
/// ดู `docs/superpowers/specs/2026-08-19-work-round-4-design.md`
```

- [ ] **Step 5: build ทั้งสองแพลตฟอร์ม แล้ว commit**

```bash
xcodebuild -scheme null-app -destination 'generic/platform=iOS Simulator' build 2>&1 | tail -3
```

Expected: `** BUILD SUCCEEDED **`

```bash
xcodebuild -scheme null-app -destination 'platform=macOS' build 2>&1 | tail -3
```

Expected: `** BUILD SUCCEEDED **`

```bash
git add null-app/Features/work/
git commit -m "Tick a task, close a stage"
```

---

### Task 8: อัปเดต UNINSTALL.md

**Files:**
- Modify: `null-app/Features/work/UNINSTALL.md`

**Interfaces:**
- Consumes: ตาราง `task` จาก Task 1
- Produces: เอกสารถอนที่ตรงกับของจริง

- [ ] **Step 1: แก้รายการ export**

อ่านทั้งไฟล์แล้วแก้: ระบุตารางเป็นห้าตัว (`work_type`, `stage_type`, `work`, `stage`, `task`)
และเพิ่ม query export ของ task ต่อจาก query เดิม:

```sql
select w.name as work_name, s.code as stage_code, t.title, t.done_at, t.position
from f_work.task t
join f_work.stage s on s.id = t.stage_id
join f_work.work w on w.id = s.work_id
order by w.created_at, s.position, t.position;
```

พร้อมบรรทัดอธิบายว่า **task คือที่เดียวที่บันทึกว่างานอะไรเสร็จเมื่อไหร่** ตั้งแต่รอบ 4
เป็นต้นไป — คอลัมน์ `actual_end` ที่เคยเก็บวันปิดไม่มีอีกแล้ว

- [ ] **Step 2: Commit**

```bash
git add null-app/Features/work/UNINSTALL.md
git commit -m "Tell the uninstall doc that tasks hold the history now"
```

---

### Task 9: พิสูจน์กับของจริง

CLAUDE.md เขียนไว้ว่า **"หน้าจอที่ดูถูกต้องไม่ใช่หลักฐาน"** ทุกข้อในงานนี้จึงอ่านแถวกลับมาเทียบ

**Files:** ไม่มีไฟล์ใน repo

- [ ] **Step 1: ติดตั้งและเปิดแอป**

```bash
xcrun simctl list devices booted
```

จด UDID เป็น `SIM` แล้ว:

```bash
xcrun simctl install "$SIM" "$(xcodebuild -scheme null-app -destination 'generic/platform=iOS Simulator' -showBuildSettings 2>/dev/null | awk -F' = ' '/ BUILT_PRODUCTS_DIR /{print $2"/null-app.app"}')"
xcrun simctl launch "$SIM" purin.null-app
```

**ห้าม** `simctl keychain reset` — ต้องเข้าเป็นเจ้าของข้อมูลเดิม

- [ ] **Step 2: เพิ่ม task ด้วยข้อความไทยจริง**

เข้างานทดสอบ → เลือก stage แรก → กด New Task → พิมพ์ `user ออก proposal` → Enter

```sql
select t.title, t.done_at, t.position, s.code
from f_work.task t join f_work.stage s on s.id = t.stage_id
order by t.created_at desc limit 3;
```

Expected: แถวบนสุด `title = user ออก proposal` ตรงตัวอักษร · `done_at` เป็น **null** · `position = 1`

- [ ] **Step 3: ติ๊กเสร็จแล้วดูว่า stage ปิด**

ติ๊ก task นั้น แล้ว:

```sql
select s.code, count(t.id) as tasks, count(t.done_at) as done, max(t.done_at) as closed_on
from f_work.stage s left join f_work.task t on t.stage_id = s.id
where s.id = 'STAGE_ID' group by s.code;
```

Expected: `tasks = 1`, `done = 1`, `closed_on` เป็นเวลาไม่กี่วินาทีที่แล้ว

บนจอ: การ์ด Progress ขึ้น `100%` และ `Task 1 / 1`

- [ ] **Step 4: พิสูจน์ว่าติ๊กออกได้จริง**

ติ๊ก task เดิมออก แล้วรัน query เดิม

Expected: `done = 0` และ `closed_on` เป็น **null**
ถ้ายังเป็นเวลาเดิม แปลว่าคีย์ `done_at` หายไปจาก PATCH — กลับไปดู `encode(to:)` ใน `WorkTaskPayload`
**นี่คือขั้นที่กับดักเดิมของโปรเจกต์จะโผล่ ถ้าโผล่จริงห้ามเดินต่อ**

- [ ] **Step 5: พิสูจน์การเลื่อนแผน**

เลือก stage ที่ `planned_end` เป็นอดีตไปแล้ว ใส่ task หนึ่งอันแล้วติ๊กเสร็จ

```sql
select code, position, planned_start, planned_end, baseline_start, baseline_end
from f_work.stage where work_id = 'TEST_WORK' order by position;
```

Expected:
- stage ที่อยู่**หลัง**ตัวที่เพิ่งปิด และยังไม่ปิด → `planned_*` ขยับไปเท่ากันทุกอัน
- stage ที่ปิดไปแล้ว → ไม่ขยับ
- **`baseline_*` ของทุกแถวไม่ขยับเลยสักแถว** — ถ้าขยับแปลว่า `encode(to:)` ส่ง baseline ตอน update
- บนจอขึ้นแถบ `Moved N later stages to keep the gap.`

- [ ] **Step 6: พิสูจน์ว่า Overdue นับทีละอัน**

```sql
select w.name, s.code, s.position, s.planned_end,
       (select count(*) from f_work.task t where t.stage_id = s.id and t.done_at is null) as open_tasks
from f_work.work w join f_work.stage s on s.work_id = w.id
where w.id = 'TEST_WORK' order by s.position;
```

หา stage แรกที่ยังมี `open_tasks > 0` และ `planned_end` เป็นอดีต แล้วเทียบกับหน้าจอ

Expected: การ์ดบนหน้ารายการขึ้นตัวเลขช้าที่นับจาก stage นั้น**ตัวเดียว** และตัวนับ Overdue
บนหัวหน้าจอนับงานชิ้นนี้หนึ่งครั้ง ไม่ใช่หลายครั้งตามจำนวน stage ที่เลยกำหนด

- [ ] **Step 7: ตรวจว่าข้อมูลจริงไม่ถูกกระทบ**

```sql
select w.name, count(distinct s.id) as stages, count(t.id) as tasks
from f_work.work w
left join f_work.stage s on s.work_id = w.id
left join f_work.task t on t.stage_id = s.id
group by w.name order by w.name;
```

Expected: `26-BP-07-02` ยังมี 7 stage และมี task เท่ากับจำนวน stage ที่เคยปิดไว้ (จาก Task 1 Step 3)

- [ ] **Step 8: build ปิดท้ายทั้งสองแพลตฟอร์ม**

```bash
xcodebuild -scheme null-app -destination 'generic/platform=iOS Simulator' build 2>&1 | tail -3
```

Expected: `** BUILD SUCCEEDED **`

```bash
xcodebuild -scheme null-app -destination 'platform=macOS' build 2>&1 | tail -3
```

Expected: `** BUILD SUCCEEDED **`

- [ ] **Step 9: บันทึกสิ่งที่พิสูจน์แล้ว**

เติมผลลง `.superpowers/sdd/2026-08-19-work-round-4/progress.md` (สร้างโฟลเดอร์ถ้ายังไม่มี)
อย่างน้อยต้องมี: ข้อไหนผ่าน ข้อไหนต้องแก้ก่อนผ่าน และอะไรที่ยังไม่ได้พิสูจน์

---

## Self-Review

**Spec coverage**

| สเปกกำหนด | งานที่ครอบคลุม |
|---|---|
| ตาราง `task` พร้อม RLS สืบสองชั้น | Task 1 Step 1, พิสูจน์ Step 7 |
| `title` ห้ามว่าง | Task 1 Step 1, พิสูจน์ Step 8 |
| ตัด `actual_start` / `actual_end` | Task 1 Step 4 (DB) + Task 2 Step 2 + Task 5 Step 1–2 (โค้ด) |
| ย้าย `actual_end` เป็น task ก่อน drop | Task 1 Step 3, ยืนยัน Step 6 |
| `baseline_start` / `baseline_end` เขียนครั้งเดียว | Task 1 Step 2, Task 5 Step 2, พิสูจน์ Task 9 Step 5 |
| stage ปิดเมื่อ task ครบ และ stage ที่ไม่มี task ปิดไม่ได้ | Task 2 Step 3, harness Step 4 |
| วันปิด = `max(done_at)` | Task 2 Step 3 |
| Overdue ทีละอัน (Freeze) | Task 3 Step 1–2, harness Step 4, พิสูจน์ Task 9 Step 6 |
| เลื่อนเท่ากันทุกอัน ไม่ไล่ต่อท้าย | Task 4, harness Step 2 |
| ปิดเร็วกว่าแผนไม่ดึงขึ้น | Task 4 harness |
| ติ๊กออกต้องส่ง null | Task 5 Step 4, พิสูจน์ Task 9 Step 4 |
| progress ของ stage = task เสร็จ / ทั้งหมด | Task 2 Step 3, แสดงผล Task 7 Step 2 |
| ไม่มีสวิตช์ Started / Finished | Task 7 Step 4 |
| บอกผู้ใช้เมื่อแผนถูกเลื่อน | Task 7 Step 3 |
| ทดสอบด้วยข้อความไทยจริง | Task 2 harness, Task 5 harness, Task 9 Step 2 |

**ยังไม่อยู่ในแผนนี้โดยตั้งใจ:** subtask · วันครบกำหนดของ task · การมอบหมาย ·
หน้า Timeline เต็ม · การแสดง baseline บนจอ · task ที่ยกเลิก · stage ที่ข้ามไป

**Type consistency:** `WorkTaskRow` ประกาศ Task 2 ใช้ใน Task 3 (ผ่าน `isClosed`), Task 5
(`instantFormatter`), Task 6 (`select`), Task 7 (`task.isDone`, `task.title`) ·
`WorkStageRow.state(today:calendar:)` เปลี่ยนลายเซ็นใน Task 2 — **ผู้เรียกทุกที่ต้องแก้ตาม**
คือ `WorkStageBar` และ `WorkCard` ซึ่ง Task 3 Step 5 จะจับได้ตอน build ·
`WorkFilter.blockingStage` ประกาศ Task 3 ใช้ใน `daysLate` และ `.overdue` ·
`WorkSchedule.Shift` ประกาศ Task 4 ใช้ใน Task 5 (`init(shift:position:)` ซึ่งมีคำเตือนให้ลบทิ้งได้)
และ Task 6 Step 4 ซึ่งเลือกใช้ dictionary แทน ·
`WorkStore.lastShiftCount` ประกาศ Task 6 Step 2 ใช้ Task 7 Step 3

**หนี้ที่แผนนี้ทิ้งไว้:**

1. `shiftIfClosedLate` เลื่อนได้ครั้งละหนึ่ง stage ที่ปิดช้า ถ้าผู้ใช้ปิดหลาย stage รวดเดียว
   (เช่นเปิดแอปหลังกลับจากลาพักร้อน) จะต้องติ๊กทีละอันให้แผนไล่ตาม
2. `setTask` ยิงสองคำขอ (update แล้ว load) แล้วอาจตามด้วยการเลื่อนอีกหลายคำขอ —
   ที่จำนวน task ระดับนี้ยังไม่คุ้มกับการเขียน Postgres function แต่ถ้าติ๊กรัว ๆ แล้วรู้สึกหน่วง
   ให้ย้ายทั้งก้อนไปเป็น function เดียวฝั่งเซิร์ฟเวอร์
3. การเลื่อนแผนไม่มี Undo จริง มีแต่แถบบอกว่าเกิดอะไรขึ้น — Undo ต้องเก็บวันเดิมไว้ที่ไหนสักที่
   ซึ่งจะกลายเป็น baseline ตัวที่สอง ยังไม่คุ้ม
4. `WorkTaskListView` ใช้ `List` ซ้อนอยู่ใน `VStack` ของหน้าแม่ ถ้าเลย์เอาต์เพี้ยนบน macOS
   ให้เปลี่ยนเป็น `ScrollView` + `LazyVStack` ซึ่งคุมความสูงได้ตรงกว่า
