# Work tracker รอบ 3 — สร้าง Work และแบ่ง Stage ได้ในแอป

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** สร้าง Work ใหม่ เลือก Work Type แบ่ง Stage จากรายการมาตรฐาน และกำหนด Timeline ของแต่ละ Stage ได้จากในแอป โดยไม่ต้องเปิด Supabase Dashboard อีก

**Architecture:** Stage เลิกเป็นช่องพิมพ์อิสระ กลายเป็นเมนูให้ติ๊กจากตารางอ้างอิง `f_work.stage_type` ที่คุมจากหลังบ้านแบบเดียวกับ `work_type` — รายการนี้เป็น**เมนู ไม่ใช่ลำดับบังคับ** งานที่ข้าม SIT ก็ไม่ติ๊ก งานที่รวม SIT กับ UAT ก็ติ๊กตัวรวม และยังเพิ่ม stage นอกรายการเองได้ ฟอร์มไม่คุยกับเซิร์ฟเวอร์เอง มันแก้ `WorkDraft` / `WorkStageDraft` ซึ่งเป็น struct ธรรมดาที่รู้กติกาของตัวเอง แล้ว `WorkStore` เป็นคนเดียวที่แปลง draft เป็น payload ทุก payload เขียน `encode(to:)` เองเพราะตัวที่ compiler สังเคราะห์ทำให้ "ล้างค่าทิ้ง" กลายเป็น "ไม่ทำอะไร" อย่างเงียบ ๆ

**Tech Stack:** Swift 5 / SwiftUI (`SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor`), supabase-swift 2.x, Postgres 15 + RLS, deployment target 26.5

**Spec:** `docs/superpowers/specs/2026-08-14-work-tracker-design.md`

## คำที่ตกลงกันแล้ว

ใช้คำเหล่านี้ให้ตรงกันทั้งบนจอ ในโค้ด และในฐานข้อมูล — รอบนี้มีงานเปลี่ยนชื่อ `item` → `work` อยู่ด้วย เพราะคำว่า item ไม่เคยถูกใช้ตอนคุยกันเลย

| คำ | หมายถึง |
|---|---|
| **Work** | หนึ่งงาน — เดิมเรียก `item` ทั้งในโค้ดและในฐานข้อมูล |
| **Work Type** | `Project` (อยู่ใน Action Plan ประจำปี) หรือ `Request` (งานแทรก) — คุมจากหลังบ้าน |
| **Stage** | ช่วงหนึ่งของ Work มีวันเริ่ม–วันจบตามแผน และวันเริ่ม–วันจบจริง |
| **Stage Type** | รายการ stage มาตรฐานที่ติ๊กเลือกได้ — **ใหม่ในรอบนี้** |
| **Task** | งานย่อยใน Stage — **ยังไม่มีในรอบนี้** (รอบ 4) |

รายการ Stage Type ตั้งต้นเก้าแถว:

| code | label | position | หมายเหตุ |
|---|---|---|---|
| `RU` | Requirement · User | 1 | |
| `RI` | Requirement · IT | 2 | |
| `DEV` | Development | 3 | |
| `SIT` | System Integration Test | 4 | บาง Work ข้ามขั้นนี้ไปเลย |
| `UAT` | User Acceptance Test | 5 | |
| `SU` | SIT & UAT | 6 | สำหรับ Work ที่รวมสองขั้นบนเป็นช่วงเดียว |
| `PT` | Penetration Test | 7 | |
| `PVT` | Production Verification Test | 8 | |
| `IMP` | Implement | 9 | |

**รายการนี้เป็นเมนู ไม่ใช่ลำดับบังคับ** — ติ๊กเฉพาะอันที่ Work ชิ้นนั้นมีจริง สเปกปัดตก
"stage template ตายตัวทุกโปรเจกต์" ไว้ด้วยเหตุผลว่าการ**บังคับ** template จะฝืนของจริง
เมนูที่เลือกได้ไม่ใช่การบังคับ และยังมีตัวเลือก "Other" ให้ตั้ง stage นอกรายการเองได้

## Global Constraints

- feature id คือ `work` schema คือ `f_work` — ตรง `^[a-z][a-z0-9_]*$`
- ชื่อ type ทุกตัวขึ้นต้นด้วย `Work` (ทุกฟีเจอร์อยู่โมดูลเดียวกัน ชื่อไม่มี prefix จะชนกัน) — **รวมถึงห้ามเพิ่ม extension บน type ของ stdlib เช่น `String`** เพราะมันจะโผล่ทั้งโมดูล
- แตะตารางผ่าน `Backend.client.schema("f_work").from(...)` เท่านั้น
- **ห้ามเขียนชื่อ Work Type หรือ Stage Type ตายตัวใน view** — ทั้งสองรายการวนจากตารางที่ `WorkStore` ดึงมา วันที่มีขั้นที่สิบจึงไม่ต้องแก้แอปเลยสักบรรทัด
- ป้ายกำกับของแอปเป็นอังกฤษ ข้อความที่ผู้ใช้พิมพ์เป็นไทยได้ — **ทดสอบด้วยข้อความไทยจริงเสมอ ไม่ใช่ placeholder อังกฤษ**
- **ช่องบังคับตอนสร้าง Work มีสองช่องเท่านั้น: Work Type และชื่อ** — คำอธิบาย ผู้ขอ และ Stage ทั้งชุดไม่บังคับ สเปกจดไว้ว่าช่องบังคับคือเหตุผลอันดับหนึ่งที่คนกดยกเลิกกลางฟอร์ม
- **PATCH ที่มีฟิลด์ Optional ต้องเขียน `encode(to:)` เอง** — ตัวสังเคราะห์ใช้ `encodeIfPresent` ฟิลด์ nil จะหายไปจาก JSON และ PostgREST อ่าน "ไม่มีคีย์" ว่า "อย่าแตะคอลัมน์นี้"
- วันที่ของ stage เดินทางเป็น `String` รูปแบบ `yyyy-MM-dd` ผ่าน `WorkStageRow.dayFormatter` ทั้งขาขึ้นและขาลง — ห้ามให้ `Date` ตรง ๆ ไปโดน encoder ของ library
- **ห้าม `import Supabase`** ใน `WorkRows.swift`, `WorkFilter.swift`, `WorkDraft.swift`, `WorkPayload.swift` — ความบริสุทธิ์นี้คือสิ่งเดียวที่ทำให้ harness ด้วย `swiftc` เป็นไปได้
- `NavigationLink(destination:label:)` สร้างปลายทางทันทีที่วาด **ห้ามใช้กับรายการการ์ด** — ใช้ `NavigationLink(value:)` คู่กับ `.navigationDestination(for:)`
- สร้างไฟล์ด้วย Write ตรง ๆ (synchronized file group) **ห้ามแก้ `project.pbxproj`** — การเปลี่ยนชื่อไฟล์ทำด้วย `git mv` ได้เลย
- build ผ่านทั้ง iOS และ macOS ก่อนถือว่าเสร็จ
- ใช้ `apply_migration` สำหรับทุกการเปลี่ยน schema ห้ามพิมพ์ใน Dashboard — **ถ้าเครื่องมือตอบว่าไม่มีสิทธิ์เขียน ให้ส่ง SQL ให้เจ้าของโปรเจกต์รันเอง แล้วรอการยืนยัน ห้ามเดินต่อโดยเดาว่าสำเร็จ**

## ไม่อยู่ในรอบนี้โดยตั้งใจ

ตาราง `task` และ progress % (รอบ 4) · หน้า Timeline เต็ม (รอบ 5) · บล็อก "ช่วงนี้ควรโฟกัส" และ "เพิ่งขยับ" (ต้องมี `task` ก่อน) · การเข้ากรุ (archive) และหน้ารายการที่เข้ากรุ — สองอย่างหลังต้องมาคู่กัน ปล่อยให้เข้ากรุได้โดยยังไม่มีที่ดูคือการสร้างประตูทางเดียว ทางออกของ Work ที่สร้างผิดในรอบนี้คือ **ลบถาวรพร้อมกล่องยืนยัน** ตามที่สเปกบอกว่าการลบต้องเป็นการกระทำแยกที่ตั้งใจ

## File Structure

| ไฟล์ | หน้าที่ |
|---|---|
| `null-app/Features/work/WorkRows.swift` | **เปลี่ยนชื่อจาก `WorkRow.swift`** — รูปร่างของทุกแถวใน `f_work.*` Foundation ล้วน |
| `null-app/Features/work/WorkDraft.swift` | **สร้างใหม่** — ค่าที่ฟอร์มถืออยู่ และกติกาว่าอะไรถูกอะไรผิด Foundation ล้วน |
| `null-app/Features/work/WorkPayload.swift` | **สร้างใหม่** — รูปร่างของ JSON ขาขึ้น `encode(to:)` เขียนมือทุกตัว Foundation ล้วน |
| `null-app/Features/work/WorkFormView.swift` | **สร้างใหม่** — หน้าสร้าง Work และหน้าแก้ Work (view เดียว สองโหมด) |
| `null-app/Features/work/WorkStageEditorView.swift` | **สร้างใหม่** — ติ๊กเลือก Stage แล้วใส่ Timeline ของอันที่ติ๊ก |
| `null-app/Features/work/WorkStore.swift` | **แก้** — ดึง `stage_type`, เพิ่มทางเขียน, เปลี่ยนชื่อตาราง |
| `null-app/Features/work/WorkFilter.swift` | **แก้** — เพิ่ม `calendar` ตัวเดียวของทั้งฟีเจอร์ |
| `null-app/Features/work/WorkCard.swift` | **แก้** — ตัด badge chip, เปลี่ยนชื่อ type |
| `null-app/Features/work/WorkStageBar.swift` | **แก้** — เปลี่ยนชื่อ type เท่านั้น |
| `null-app/Features/work/WorkRootView.swift` | **แก้** — ปุ่ม `+` ใช้งานได้ การ์ดกดเข้าหน้าแก้ได้ |
| `null-app/Features/work/UNINSTALL.md` | **แก้** — ชื่อตารางและคอลัมน์ชุดใหม่ |

---

### Task 1: แอปเลิกอ่าน `badge`

สเปกบอกไปแล้วว่าไม่มีคอลัมน์นี้ แต่โค้ดยังอ่านและยังวาดมันอยู่ ทำก่อนลบคอลัมน์เสมอ —
ลบคอลัมน์ก่อนแล้วแอปที่ยัง `select` มันอยู่จะพังทั้งหน้าจอในช่วงระหว่างสองงาน

**Files:**
- Modify: `null-app/Features/work/WorkRow.swift`
- Modify: `null-app/Features/work/WorkStore.swift`
- Modify: `null-app/Features/work/WorkCard.swift`

**Interfaces:**
- Consumes: ไม่มี
- Produces: `WorkItemRow` ไม่มีฟิลด์ `badge` (ชื่อ type จะถูกเปลี่ยนในงานที่ 2)

- [ ] **Step 1: ตัดฟิลด์ออกจาก `WorkItemRow`**

ใน `null-app/Features/work/WorkRow.swift` ลบ `let badge: String?` และลบ `badge` ออกจาก `CodingKeys`:

```swift
nonisolated struct WorkItemRow: Codable, Sendable, Identifiable {
    let id: UUID
    let typeCode: String
    let name: String
    let description: String?
    let requestedBy: String?
    let updatedAt: Date
    var stage: [WorkStageRow]

    enum CodingKeys: String, CodingKey {
        case id, name, description, stage
        case typeCode = "type_code"
        case requestedBy = "requested_by"
        case updatedAt = "updated_at"
    }
}
```

- [ ] **Step 2: เลิกขอคอลัมน์นั้นจากเซิร์ฟเวอร์**

ใน `null-app/Features/work/WorkStore.swift` แก้สตริง `select` เอา `badge,` ออก:

```swift
.select("id,type_code,name,description,requested_by,updated_at,stage(id,code,name,position,planned_start,planned_end,actual_start,actual_end)")
```

- [ ] **Step 3: เอา badge chip ออกจากการ์ด**

ใน `null-app/Features/work/WorkCard.swift`:

1. ลบ `private var badgeText: String?` ทั้งก้อน
2. ลบ `private var stageAndBadgeText: some View` ทั้งก้อน
3. ลบ `private func badgeChip(_:lineLimit:)` ทั้งก้อน
4. แก้บล็อกที่เคยเรียกสองตัวนั้นให้เหลือชื่อ stage อย่างเดียว:

```swift
            if let stageText = currentStageText {
                HStack(alignment: .firstTextBaseline, spacing: 8) {
                    Text(stageText)
                        .font(.caption)
                        .foregroundStyle(.secondary)
                        // ไทยไม่มีช่องว่างระหว่างคำ ปล่อยให้ระบบตัดบรรทัดเองได้เต็มที่
                        .fixedSize(horizontal: false, vertical: true)

                    Spacer(minLength: 8)

                    if let late = WorkFilter.daysLate(item.stage, today: today, calendar: calendar) {
                        Text("\(late)d late")
                            .font(.caption)
                            .foregroundStyle(.red)
                            .layoutPriority(1)
                            .accessibilityLabel(Text("\(late) day\(late == 1 ? "" : "s") late"))
                    }
                }
            }
```

5. แก้ doc comment บนหัว struct ให้ตรงความจริง:

```swift
/// การ์ด Work หนึ่งใบบนหน้า Overview
///
/// ไม่มีตัวเลข progress % และไม่มี Task x/y — สเปกปัดทั้งสองทิ้ง
/// เพราะแถบ stage บอกได้ละเอียดกว่าและตีความง่ายกว่าตัวเลขโดด ๆ
///
/// บรรทัดใต้แถบเคยมี badge ที่ผู้ใช้พิมพ์เองต่อท้ายชื่อ stage — ตัดทิ้งแล้ว
/// รอบ 4 จะเอาบรรทัดนั้นกลับมาในรูปที่คำนวณจาก `task` แทนการพิมพ์มือ
```

6. แก้ `#Preview` ให้เลิกส่ง `badge:` และเปลี่ยนชื่อ preview ให้ตรงกับสิ่งที่มันทดสอบจริง:

```swift
#Preview("Long Thai name wraps, doesn't truncate") {
    WorkCard(
        item: WorkItemRow(
            id: UUID(),
            typeCode: "project",
            name: "26-BP-07-02 | ปรับปรุงแอปพลิเคชัน Umay+ ระยะที่หนึ่ง สำหรับกลุ่มผู้ใช้ภายในองค์กร",
            description: nil,
            requestedBy: nil,
            updatedAt: Date(),
            stage: [
                WorkStageRow(
                    id: UUID(), code: "RU", name: "Requirement · User", position: 1,
                    plannedStart: "2026-01-01", plannedEnd: "2026-02-01",
                    actualStart: "2026-01-01", actualEnd: "2026-02-01"
                ),
                WorkStageRow(
                    id: UUID(), code: "RI", name: "Requirement · IT", position: 2,
                    plannedStart: "2026-02-01", plannedEnd: "2026-03-01",
                    actualStart: "2026-02-01", actualEnd: nil
                ),
            ]
        ),
        today: Date()
    )
    .padding()
}
```

- [ ] **Step 4: พิสูจน์ว่าไม่มีอะไรเอ่ยถึง badge อีก**

```bash
grep -rn "badge" null-app --include="*.swift"
```

Expected: เหลือบรรทัดเดียวคือ `calendar.badge.plus` ซึ่งเป็นชื่อ SF Symbol ไม่ใช่คอลัมน์

- [ ] **Step 5: build ทั้งสองแพลตฟอร์ม**

```bash
xcodebuild -scheme null-app -destination 'generic/platform=iOS Simulator' build 2>&1 | tail -3
```

Expected: `** BUILD SUCCEEDED **`

```bash
xcodebuild -scheme null-app -destination 'platform=macOS' build 2>&1 | tail -3
```

Expected: `** BUILD SUCCEEDED **`

- [ ] **Step 6: Commit**

```bash
git add null-app/Features/work/
git commit -m "Stop reading a column the spec already deleted"
```

---

### Task 2: เปลี่ยนชื่อ item เป็น work ทั้งฐานข้อมูลและโค้ด

คำว่า `item` ไม่เคยถูกใช้ตอนคุยกันเลย ทุกคนพูดว่า Work การแปลคำไปมาทุกครั้งที่อ่านโค้ด
คือภาษีที่จ่ายตลอดไป และตอนนี้คือจุดที่โค้ดน้อยที่สุดที่จะเปลี่ยนได้

**การเปลี่ยนชื่อตารางกับการเปลี่ยนชื่อ type ต้องอยู่ใน commit เดียวกัน** — commit ที่ทำครึ่งเดียว
คือ commit ที่แอปเปิดไม่ได้

**Files:**
- Migration: `f_work_rename_item_to_work`
- Rename: `null-app/Features/work/WorkRow.swift` → `null-app/Features/work/WorkRows.swift`
- Modify: ทุกไฟล์ใน `null-app/Features/work/`

**Interfaces:**
- Consumes: ผลของงานที่ 1
- Produces:
  - ตาราง `f_work.work` (เดิม `f_work.item`) และคอลัมน์ `f_work.stage.work_id` (เดิม `item_id`)
  - `WorkRow` (เดิม `WorkItemRow`), `WorkTypeRow` เท่าเดิม, `WorkStageRow` เท่าเดิม
  - `WorkStore.works` (เดิม `items`)
  - งานที่ 3 เป็นต้นไปใช้ชื่อชุดนี้ทั้งหมด

- [ ] **Step 1: migration เปลี่ยนชื่อฝั่งฐานข้อมูล**

`apply_migration` ชื่อ `f_work_rename_item_to_work`:

```sql
alter table f_work.item rename to work;
alter table f_work.stage rename column item_id to work_id;

-- ชื่อ constraint และ index ไม่เปลี่ยนตามชื่อตาราง ต้องเปลี่ยนเอง
-- ปล่อยไว้จะได้ error ในอนาคตที่พูดถึงตารางที่ไม่มีอยู่แล้ว
alter table f_work.work rename constraint item_user_id_fkey to work_user_id_fkey;
alter table f_work.work rename constraint item_type_code_fkey to work_type_code_fkey;
alter table f_work.stage rename constraint stage_item_id_fkey to stage_work_id_fkey;
alter index f_work.item_user_id_idx rename to work_user_id_idx;
alter index f_work.stage_item_id_idx rename to stage_work_id_idx;

-- policy บน stage อ้างชื่อตารางเก่าอยู่ในตัว expression ต้องเขียนใหม่ทั้งสี่
drop policy "own stage select" on f_work.stage;
drop policy "own stage insert" on f_work.stage;
drop policy "own stage update" on f_work.stage;
drop policy "own stage delete" on f_work.stage;

create policy "own stage select" on f_work.stage
    for select using (
        exists (select 1 from f_work.work w where w.id = stage.work_id and w.user_id = auth.uid())
    );

create policy "own stage insert" on f_work.stage
    for insert with check (
        exists (select 1 from f_work.work w where w.id = stage.work_id and w.user_id = auth.uid())
    );

-- with check ต้องมีด้วย ไม่งั้นย้าย stage ไปแขวนใต้ work ของคนอื่นได้
create policy "own stage update" on f_work.stage
    for update using (
        exists (select 1 from f_work.work w where w.id = stage.work_id and w.user_id = auth.uid())
    ) with check (
        exists (select 1 from f_work.work w where w.id = stage.work_id and w.user_id = auth.uid())
    );

create policy "own stage delete" on f_work.stage
    for delete using (
        exists (select 1 from f_work.work w where w.id = stage.work_id and w.user_id = auth.uid())
    );

-- policy บน work เองอ้างแค่คอลัมน์ user_id เปลี่ยนแค่ชื่อให้อ่านรู้เรื่อง
alter policy "own item select" on f_work.work rename to "own work select";
alter policy "own item insert" on f_work.work rename to "own work insert";
alter policy "own item update" on f_work.work rename to "own work update";
alter policy "own item delete" on f_work.work rename to "own work delete";
```

- [ ] **Step 2: ยืนยันว่าเปลี่ยนครบและ RLS ยังปฏิเสธ**

```sql
select tablename, policyname, cmd from pg_policies
where schemaname = 'f_work' order by tablename, policyname;

select conname from pg_constraint
where conrelid in ('f_work.work'::regclass, 'f_work.stage'::regclass)
order by conname;
```

Expected: ไม่มีคำว่า `item` โผล่ในผลลัพธ์ทั้งสองชุด

หา `USER_B` (คนที่ไม่ได้เป็นเจ้าของข้อมูล) จาก:

```sql
select user_id, display_name from public.profiles order by created_at;
```

แล้วพิสูจน์ว่า policy ที่เขียนใหม่ยังปฏิเสธจริง:

```sql
set local role authenticated;
set local request.jwt.claims to '{"sub":"USER_B","role":"authenticated"}';
select count(*) from f_work.stage;
```

Expected: `0` — **ถ้าได้ 9 แปลว่า policy ที่เขียนใหม่หลวม หยุดทันที**

- [ ] **Step 3: เปลี่ยนชื่อไฟล์**

```bash
git mv null-app/Features/work/WorkRow.swift null-app/Features/work/WorkRows.swift
```

ไฟล์นี้ถือ type สามตัว ชื่อพหูพจน์จึงตรงกับเนื้อในมากกว่า และไม่ชนกับชื่อ type `WorkRow` ที่กำลังจะเกิด

- [ ] **Step 4: เปลี่ยนชื่อ type และ property ทั่วทั้งฟีเจอร์**

```bash
grep -rln "WorkItemRow\|\.items\b\|item_id" null-app/Features/work
```

แก้ทีละไฟล์ตามตารางนี้ (อย่าใช้ `sed` ทั้งโฟลเดอร์ — คำว่า `item` โผล่ในบริบทที่ไม่เกี่ยวด้วย เช่น `ToolbarItem`, `GridItem`, `visibleItems`):

| เดิม | ใหม่ |
|---|---|
| `WorkItemRow` | `WorkRow` |
| `WorkStore.items` | `WorkStore.works` |
| `typeFilteredItems` | `typeFilteredWorks` |
| `visibleItems` | `visibleWorks` |
| `.from("item")` | `.from("work")` |
| `WorkCard(item:today:)` | `WorkCard(work:today:)` และ property `let work: WorkRow` |

`ToolbarItem`, `GridItem`, `ForEach` ที่ตั้งชื่อตัวแปรว่า `item` ในลูป — เปลี่ยนตัวแปรลูปเป็น `work`
ส่วนชื่อ type ของ SwiftUI ห้ามแตะ

- [ ] **Step 5: พิสูจน์ว่าไม่เหลือคำเก่าที่เป็นของเรา**

```bash
grep -rn "WorkItemRow\|from(\"item\")\|item_id" null-app
```

Expected: ไม่มีผลลัพธ์

```bash
grep -rn "\bitem\b" null-app/Features/work --include="*.swift"
```

Expected: เหลือเฉพาะบรรทัดที่เป็น API ของ SwiftUI (`ToolbarItem`, `GridItem`) — อ่านทุกบรรทัดที่เหลือ ไม่ใช่นับจำนวน

- [ ] **Step 6: build ทั้งสองแพลตฟอร์ม**

```bash
xcodebuild -scheme null-app -destination 'generic/platform=iOS Simulator' build 2>&1 | tail -3
```

Expected: `** BUILD SUCCEEDED **`

```bash
xcodebuild -scheme null-app -destination 'platform=macOS' build 2>&1 | tail -3
```

Expected: `** BUILD SUCCEEDED **`

- [ ] **Step 7: รันของจริงหนึ่งรอบก่อน commit**

การเปลี่ยนชื่อที่ compile ผ่านยังพังตอนรันได้ ถ้าสตริงชื่อตารางหลุดไปที่หนึ่ง

```bash
xcrun simctl list devices booted
```

จด UDID เป็น `SIM` แล้ว:

```bash
xcrun simctl install "$SIM" "$(xcodebuild -scheme null-app -destination 'generic/platform=iOS Simulator' -showBuildSettings 2>/dev/null | awk -F' = ' '/ BUILT_PRODUCTS_DIR /{print $2"/null-app.app"}')"
xcrun simctl launch "$SIM" purin.null-app
```

Expected: หน้า Work แสดง Work สามชิ้นเดิมพร้อมแถบ stage ครบ ถ่ายภาพหน้าจอเก็บไว้
ถ้าขึ้น `Couldn't load your work` แปลว่าชื่อตารางหลุด **อย่า commit**

- [ ] **Step 8: Commit**

```bash
git add -A null-app/Features/work/
git commit -m "Call it work, the way everyone already does"
```

---

### Task 3: ฐานข้อมูล — รายการ Stage Type, ลบ badge, และกติกาที่ปฏิเสธได้

**Files:**
- Migration: `f_work_stage_type` และ `f_work_round_3_rules`
- Modify: `null-app/Features/work/UNINSTALL.md`

**Interfaces:**
- Consumes: ตาราง `f_work.work` และ `f_work.stage` จากงานที่ 2
- Produces:
  - `f_work.stage_type(code, label, position, is_active)` เก้าแถว อ่านได้อย่างเดียว
  - constraint `work_name_not_blank`, `stage_code_not_blank`, `stage_name_not_blank`, `stage_dates_ordered`, `stage_actual_dates_ordered`
  - trigger `stage_touch_work`
  - งานที่ 4 เขียนกติกาฝั่งแอปให้ตรงกับชุดนี้ทีละข้อ

- [ ] **Step 1: สร้างตารางรายการ Stage Type**

`apply_migration` ชื่อ `f_work_stage_type`:

```sql
-- รายการ stage มาตรฐาน คุมจากหลังบ้านแบบเดียวกับ work_type
-- ไม่มี user_id เพราะเป็น reference data ไม่ได้เป็นของใครคนใดคนหนึ่ง
create table f_work.stage_type (
    code text primary key,
    label text not null,
    position int not null,
    is_active boolean not null default true
);

insert into f_work.stage_type (code, label, position) values
    ('RU',  'Requirement · User',           1),
    ('RI',  'Requirement · IT',             2),
    ('DEV', 'Development',                  3),
    ('SIT', 'System Integration Test',      4),
    ('UAT', 'User Acceptance Test',         5),
    ('SU',  'SIT & UAT',                    6),
    ('PT',  'Penetration Test',             7),
    ('PVT', 'Production Verification Test', 8),
    ('IMP', 'Implement',                    9);

alter table f_work.stage_type enable row level security;

-- อ่านได้อย่างเดียว ไม่มี policy สำหรับ insert/update/delete เลย
-- ความปลอดภัยมาจากการไม่มี policy ไม่ใช่จากการเช็คในโค้ดแอป
create policy "read stage types" on f_work.stage_type
    for select using (true);

grant select on f_work.stage_type to authenticated;
```

**`f_work.stage.code` จงใจไม่เป็น foreign key ไปหาตารางนี้** — ตัวเลือก "Other" ต้องใส่รหัส
นอกรายการได้ FK จะปิดทางนั้นทันที ตารางนี้เป็น**เมนูให้เลือก ไม่ใช่รายการค่าที่อนุญาต**
ความแตกต่างนี้คือทั้งหมดของเหตุผลที่สเปกยอมรับมันได้ทั้งที่เคยปัดตก "template ตายตัว"

- [ ] **Step 2: ยืนยันว่า `anon` เขียนไม่ได้และแอปก็เขียนไม่ได้**

```sql
select grantee, privilege_type from information_schema.role_table_grants
where table_schema = 'f_work' and table_name = 'stage_type'
order by grantee, privilege_type;
```

Expected: `authenticated` มีแค่ `SELECT` และไม่มีแถวของ `anon` เลย
บันทึกการทำงานของรอบก่อนเคยจดว่า `f_example` แจกสิทธิ์ให้ `anon` โดยไม่ตั้งใจ — ข้อนี้คือกันไม่ให้ซ้ำ

- [ ] **Step 3: แมปรหัสเดิมให้ตรงรายการใหม่**

ข้อมูลเดิมของ `26-BP-07-02` ใช้รหัสคนละชุด แมปได้ตรงตัวโดยไม่ต้องแตกวันใหม่:
`D` → `DEV`, `U` → `SU` (เดิมชื่อ `SIT & UAT` อยู่แล้ว), `I` → `IMP`

```sql
update f_work.stage set code = 'DEV' where code = 'D';
update f_work.stage set code = 'SU'  where code = 'U';
update f_work.stage set code = 'IMP' where code = 'I';

-- ทำให้ชื่อตรงกับ label ของรายการ เป็นการแก้ครั้งเดียวตอนนี้ที่ยังไม่มี stage แบบ Other
-- หลังจากนี้ชื่อของแถวจะไม่ถูกเขียนทับอีก เพราะ stage แบบ Other มีชื่อของตัวเอง
update f_work.stage s
set name = t.label
from f_work.stage_type t
where t.code = s.code;
```

ยืนยัน:

```sql
select s.code, s.name, s.position, s.planned_start, s.planned_end
from f_work.stage s join f_work.work w on w.id = s.work_id
where w.name like '26-BP-07-02%' order by s.position;
```

Expected: เจ็ดแถว รหัส `RU RI DEV SU PT PVT IMP` ตามลำดับ วันที่ไม่เปลี่ยนจากเดิมสักแถว

- [ ] **Step 4: ลบ badge ใส่กติกา และ trigger**

`apply_migration` ชื่อ `f_work_round_3_rules`:

```sql
-- badge เคยเป็นข้อความอิสระว่ากำลังทำอะไรอยู่ สเปกตัดทิ้งแล้ว
-- รอบ 4 จะคำนวณบรรทัดนั้นจากตาราง task แทน
alter table f_work.work drop column badge;

-- ชื่อที่มีแต่ช่องว่างไม่ใช่ชื่อ กติกาเดียวกับที่ public.profiles.display_name ใช้อยู่
alter table f_work.work
    add constraint work_name_not_blank check (btrim(name) <> '');

alter table f_work.stage
    add constraint stage_code_not_blank check (btrim(code) <> ''),
    add constraint stage_name_not_blank check (btrim(name) <> ''),
    add constraint stage_dates_ordered check (planned_end >= planned_start),
    -- ปิด stage ที่ไม่เคยเปิด ทำให้ WorkStageRow.state อ่านว่า completed
    -- ทั้งที่ไม่มีวันเริ่มจริง — สภาพที่ไม่มีทางเกิดจากการทำงานจริง
    add constraint stage_actual_dates_ordered check (
        (actual_end is null) or (actual_start is not null and actual_end >= actual_start)
    );

-- แก้ stage คือการแก้ Work การ์ดบอก "อัปเดตล่าสุด" จากคอลัมน์ของ work
-- ถ้าไม่มีตัวนี้ ย้ายวันของ stage แล้วการ์ดจะยังบอกว่าอัปเดตเมื่อสัปดาห์ที่แล้ว
create or replace function f_work.touch_work_from_stage()
returns trigger
language plpgsql
set search_path to ''
as $$
begin
    if tg_op = 'DELETE' then
        update f_work.work set updated_at = now() where id = old.work_id;
        return old;
    else
        update f_work.work set updated_at = now() where id = new.work_id;
        return new;
    end if;
end;
$$;

create trigger stage_touch_work
    after insert or update or delete on f_work.stage
    for each row execute function f_work.touch_work_from_stage();
```

ฟังก์ชันนี้ **ไม่ใช่** `security definer` โดยตั้งใจ — `authenticated` มี policy `own work update`
อยู่แล้ว การอัปเดต Work ของตัวเองจึงผ่าน RLS ตามปกติ ส่วนการทำให้เป็น `security definer`
จะเปิดทางให้มันอัปเดต Work ของคนอื่นได้ถ้าวันหนึ่ง policy ของ stage หลวมลง

- [ ] **Step 5: ยืนยันว่าคอลัมน์หายและ constraint ติดจริง**

```sql
select conname, pg_get_constraintdef(oid) from pg_constraint
where conrelid in ('f_work.work'::regclass, 'f_work.stage'::regclass) and contype = 'c'
order by conname;

select column_name from information_schema.columns
where table_schema = 'f_work' and table_name = 'work' order by ordinal_position;
```

Expected: เห็น constraint ทั้งห้า และรายชื่อคอลัมน์ของ `work` **ไม่มี** `badge`

- [ ] **Step 6: พิสูจน์ว่าชื่อว่างถูกปฏิเสธ**

หา `USER_A` (เจ้าของข้อมูลจริง) จาก `select user_id, display_name from public.profiles order by created_at;` แล้ว:

```sql
insert into f_work.work (user_id, type_code, name) values ('USER_A', 'project', '   ');
```

Expected: `ERROR ... violates check constraint "work_name_not_blank"`

- [ ] **Step 7: พิสูจน์กติกาของวันที่**

```sql
insert into f_work.work (user_id, type_code, name)
values ('USER_A', 'project', 'ทดสอบ constraint ของวันที่') returning id;
```

จด id เป็น `TEMP_WORK` แล้วรันทีละคำสั่ง:

```sql
insert into f_work.stage (work_id, code, name, position, planned_start, planned_end)
values ('TEMP_WORK', 'SIT', 'System Integration Test', 1, '2026-09-30', '2026-09-01');
```

Expected: `ERROR ... violates check constraint "stage_dates_ordered"`

```sql
insert into f_work.stage (work_id, code, name, position, planned_start, planned_end, actual_start, actual_end)
values ('TEMP_WORK', 'SIT', 'System Integration Test', 1, '2026-09-01', '2026-09-30', null, '2026-09-15');
```

Expected: `ERROR ... violates check constraint "stage_actual_dates_ordered"`

- [ ] **Step 8: พิสูจน์ว่า trigger ดัน `updated_at` ของ Work**

```sql
select updated_at from f_work.work where id = 'TEMP_WORK';

insert into f_work.stage (work_id, code, name, position, planned_start, planned_end)
values ('TEMP_WORK', 'RU', 'Requirement · User', 1, '2026-09-01', '2026-09-30');

select updated_at from f_work.work where id = 'TEMP_WORK';
```

Expected: ค่าที่สองใหม่กว่าค่าแรก ถ้าเท่าเดิม trigger ไม่ทำงาน **อย่าเดินต่อ**

- [ ] **Step 9: พิสูจน์ว่าแอปเขียนตาราง stage_type ไม่ได้**

```sql
set local role authenticated;
set local request.jwt.claims to '{"sub":"USER_A","role":"authenticated"}';
insert into f_work.stage_type (code, label, position) values ('HACK', 'Injected', 99);
```

Expected: `ERROR ... permission denied for table stage_type`
ตารางนี้ไม่มีทั้ง grant และ policy สำหรับ insert — สองชั้นที่ต้องพังพร้อมกันถึงจะเขียนได้

- [ ] **Step 10: เก็บกวาดข้อมูลทดสอบ**

```sql
delete from f_work.work where id = 'TEMP_WORK';
select count(*) as leftover_stages from f_work.stage where work_id = 'TEMP_WORK';
```

Expected: `0` — stage ตามไปด้วยผ่าน `on delete cascade`

- [ ] **Step 11: อัปเดต UNINSTALL.md**

อ่าน `null-app/Features/work/UNINSTALL.md` ทั้งไฟล์ แล้วแก้ให้ตรงกับของจริงรอบนี้:
ชื่อตาราง `item` → `work` ทุกที่ · คอลัมน์ `item_id` → `work_id` · ตัด `badge` ออกจากรายการคอลัมน์ ·
เพิ่ม `stage_type` เข้าไปในรายการตารางที่ต้อง export ก่อนถอน

- [ ] **Step 12: Commit**

```bash
git add null-app/Features/work/UNINSTALL.md
git commit -m "Give stages a menu to be picked from, and rules that refuse the rest"
```

---

### Task 4: ปฏิทินตัวเดียว และกติกาของฟอร์มที่ทดสอบได้จริง

รอบ 2 จดหนี้ไว้ว่า `WorkCard` กับ `WorkRootView` สร้าง `Calendar` คนละที่ "ถ้ามีที่ที่สามเมื่อไหร่
ควรย้ายไปเป็น `static let` ใน `WorkFilter`" — ฟอร์มรอบนี้คือที่ที่สาม และมันไม่ใช่แค่เรื่องความสะอาด:
`DatePicker` อ่านโซนเวลาจาก environment ส่วน `WorkStageRow.dayFormatter` ตรึงที่ UTC
ถ้าสองอย่างนี้อยู่คนละโซน ผู้ใช้ที่กรุงเทพฯ เลือก 1 ก.ย. แล้วเซิร์ฟเวอร์จะได้ `2026-08-31`

**Files:**
- Create: `null-app/Features/work/WorkDraft.swift`
- Modify: `null-app/Features/work/WorkFilter.swift`, `WorkCard.swift`, `WorkRootView.swift`
- Test: harness ชั่วคราวนอก repo (`mktemp -d`)

**Interfaces:**
- Consumes: `WorkRow`, `WorkStageRow` จากงานที่ 2
- Produces:
  - `WorkFilter.calendar: Calendar` — ปฏิทิน UTC ตัวเดียวของทั้งฟีเจอร์
  - `WorkDraft` — `typeCode`, `name`, `detail`, `requestedBy` (`String` ทั้งหมด), `init()`, `init(_ row: WorkRow)`, `isValid: Bool`, `validationError: String?`, `trimmedName: String`, `trimmedDetail: String?`, `trimmedRequestedBy: String?`, `static func emptyToNil(_:) -> String?`
  - `WorkStageDraft` — `id: UUID`, `isNew: Bool`, `isCustom: Bool`, `code`, `name` (`String`), `plannedStart`, `plannedEnd` (`Date`), `actualStart`, `actualEnd` (`Date?`), `init(type:startingFrom:calendar:)`, `init(custom:calendar:)`, `init(_ row: WorkStageRow, knownCodes: Set<String>)`, `func validationError(calendar:) -> String?`, `static func firstError(in:calendar:) -> String?`

- [ ] **Step 1: ย้ายปฏิทินมาไว้ที่เดียว**

เติมท้าย `WorkFilter` ใน `null-app/Features/work/WorkFilter.swift` ก่อน `}` ปิด enum:

```swift
    /// ปฏิทินตัวเดียวของทั้งฟีเจอร์ ตรึงที่ UTC ให้ตรงกับ `WorkStageRow.dayFormatter`
    ///
    /// วันของ stage เป็น Postgres `date` ซึ่งไม่มีโซนเวลาติดมา การอ่านและการเขียนจึงต้อง
    /// ตีความมันด้วยโซนเดียวกันเสมอ ไม่ใช่โซนของเครื่องผู้ใช้ — ไม่งั้นคนที่กรุงเทพฯ (UTC+7)
    /// เลือก `2026-09-01` บน `DatePicker` จะได้ Date ที่เท่ากับ `2026-08-31T17:00Z`
    /// แล้ว formatter ที่เป็น UTC จะเขียนลงฐานข้อมูลว่า `2026-08-31` — ผิดไปหนึ่งวันทุกครั้ง
    /// โดยไม่มี error ใด ๆ
    ///
    /// ผลที่ตามมา: view ที่มี `DatePicker` ต้องใส่ `.environment(\.timeZone, ...)` ให้ตรงกับตัวนี้ด้วย
    static let calendar: Calendar = {
        var c = Calendar(identifier: .gregorian)
        c.timeZone = TimeZone(secondsFromGMT: 0)!
        return c
    }()
```

- [ ] **Step 2: ลบปฏิทินที่ซ้ำอยู่สองที่**

ใน `WorkCard.swift` และ `WorkRootView.swift` ลบก้อนนี้ทั้งสองไฟล์:

```swift
    private var calendar: Calendar {
        var c = Calendar(identifier: .gregorian)
        c.timeZone = TimeZone(secondsFromGMT: 0)!
        return c
    }
```

แล้วเปลี่ยนทุกที่ที่เขียน `calendar: calendar` เป็น `calendar: WorkFilter.calendar`
(`WorkCard` มีหนึ่งที่ · `WorkRootView` มีสองที่ — ใน `visibleWorks` และ `count(_:)`)

พิสูจน์:

```bash
grep -rn "Calendar(identifier:" null-app/Features/work
```

Expected: บรรทัดเดียว อยู่ใน `WorkFilter.swift`

- [ ] **Step 3: เขียน `WorkDraft.swift`**

```swift
//
//  WorkDraft.swift
//  null-app
//

import Foundation

/// ค่าที่ฟอร์มกำลังถืออยู่ และกติกาว่าอะไรถูกอะไรผิด
///
/// **ห้าม import Supabase ในไฟล์นี้** — ดูเหตุผลใน WorkRows.swift
///
/// กติกาทุกข้อในนี้มีคู่แฝดเป็น check constraint ในฐานข้อมูล ไม่ใช่การเขียนซ้ำโดยเปล่าประโยชน์
/// แต่เพราะสองชั้นทำคนละหน้าที่: ชั้นนี้บอกผู้ใช้ว่าอะไรยังไม่ครบ *ก่อน* กด Save
/// ส่วนชั้นฐานข้อมูลรับประกันว่าไม่มีทางใดเลยที่ข้อมูลเสียเข้าไปได้ รวมถึงทางที่ยังไม่ได้เขียน
nonisolated struct WorkDraft: Sendable, Equatable {
    var typeCode = ""
    var name = ""

    /// ตรงกับคอลัมน์ `description` — ตั้งชื่อ property ว่า `detail` เพราะ `description`
    /// เป็นชื่อที่ `CustomStringConvertible` จองไว้ การมี stored property ชื่อนั้นทำให้
    /// string interpolation ของ struct นี้พ่นคำอธิบายงานออกมาแทนที่จะพ่นตัว struct
    var detail = ""
    var requestedBy = ""

    static let nameLimit = 200

    init() {}

    init(_ row: WorkRow) {
        typeCode = row.typeCode
        name = row.name
        detail = row.description ?? ""
        requestedBy = row.requestedBy ?? ""
    }

    var isValid: Bool { validationError == nil }

    /// ช่องบังคับมีสองช่องตามสเปก — Work Type และชื่อ ที่เหลือว่างได้ทั้งหมด
    var validationError: String? {
        if typeCode.isEmpty { return "Pick a type" }
        let trimmed = trimmedName
        if trimmed.isEmpty { return "Name can't be empty" }
        if trimmed.count > Self.nameLimit { return "Name is too long" }
        return nil
    }

    var trimmedName: String { name.trimmingCharacters(in: .whitespacesAndNewlines) }
    var trimmedDetail: String? { Self.emptyToNil(detail) }
    var trimmedRequestedBy: String? { Self.emptyToNil(requestedBy) }

    /// ช่องที่ผู้ใช้ล้างทิ้งต้องกลายเป็น nil ไม่ใช่ `""`
    /// สตริงว่างในคอลัมน์ที่ null ได้ คือค่าที่ไม่มีความหมายและต้องเช็กสองแบบตลอดไป
    static func emptyToNil(_ text: String) -> String? {
        let trimmed = text.trimmingCharacters(in: .whitespacesAndNewlines)
        return trimmed.isEmpty ? nil : trimmed
    }
}

/// stage หนึ่งแถวที่กำลังถูกแก้อยู่
///
/// วันที่เป็น `Date` ที่นี่ (ไม่ใช่ `String` แบบ `WorkStageRow`) เพราะ `DatePicker` ต้องการ `Date`
/// การแปลงกลับเป็น `yyyy-MM-dd` เกิดที่เดียวคือ `WorkPayload.swift`
nonisolated struct WorkStageDraft: Sendable, Equatable, Identifiable {
    /// id ของแถวจริงเมื่อ `isNew == false` — เมื่อเป็นแถวใหม่ ตัวนี้เป็นแค่ identity ให้ `ForEach`
    /// และถูกทิ้งตอนบันทึก เพราะ id จริงเป็นของที่ฐานข้อมูลแจก
    let id: UUID
    let isNew: Bool

    /// true = stage ที่ผู้ใช้ตั้งเอง ไม่ได้มาจากรายการ `stage_type`
    ///
    /// เก็บไว้เพราะหน้าจอต้องรู้ว่าจะโชว์ช่องพิมพ์รหัสกับชื่อหรือไม่ ส่วนฐานข้อมูล**ไม่เก็บ**
    /// ค่านี้ — `stage.code` เป็น text เฉย ๆ ไม่มี FK ไปหารายการ ตอนโหลดกลับมาจึงอนุมานเอา
    /// จากว่ารหัสนั้นอยู่ในรายการหรือเปล่า
    var isCustom: Bool

    var code = ""
    var name = ""
    var plannedStart: Date
    var plannedEnd: Date
    var actualStart: Date?
    var actualEnd: Date?

    static let codeLimit = 8
    static let nameLimit = 80

    /// stage ที่ติ๊กมาจากรายการ — รหัสกับชื่อมาจากรายการ ผู้ใช้เหลือแค่ใส่วัน
    init(type: WorkStageTypeRow, startingFrom start: Date, calendar: Calendar) {
        id = UUID()
        isNew = true
        isCustom = false
        code = type.code
        name = type.label
        plannedStart = calendar.startOfDay(for: start)
        plannedEnd = calendar.date(byAdding: .month, value: 1, to: plannedStart) ?? plannedStart
    }

    /// stage ที่ผู้ใช้ตั้งเอง — เริ่มจากรหัสและชื่อว่าง ฟอร์มจะฟ้องจนกว่าจะกรอก
    init(custom start: Date, calendar: Calendar) {
        id = UUID()
        isNew = true
        isCustom = true
        plannedStart = calendar.startOfDay(for: start)
        plannedEnd = calendar.date(byAdding: .month, value: 1, to: plannedStart) ?? plannedStart
    }

    init(_ row: WorkStageRow, knownCodes: Set<String>) {
        id = row.id
        isNew = false
        isCustom = !knownCodes.contains(row.code)
        code = row.code
        name = row.name
        plannedStart = row.plannedStartDate ?? Date()
        plannedEnd = row.plannedEndDate ?? plannedStart
        actualStart = row.actualStartDate
        actualEnd = row.actualEndDate
    }

    /// เทียบวันที่ระดับวัน ไม่ใช่ระดับวินาที — `DatePicker` คืนเวลาติดมาด้วยเสมอ
    /// stage ที่เริ่มและจบวันเดียวกันจะฟ้องผิดทันทีถ้าเทียบ `Date` ตรง ๆ
    /// เพราะเวลาบ่ายของวันเริ่มมากกว่าเวลาเช้าของวันจบ
    func validationError(calendar: Calendar) -> String? {
        let trimmedCode = code.trimmingCharacters(in: .whitespacesAndNewlines)
        if trimmedCode.isEmpty { return "Code can't be empty" }
        if trimmedCode.count > Self.codeLimit { return "Code is too long" }

        let trimmedName = name.trimmingCharacters(in: .whitespacesAndNewlines)
        if trimmedName.isEmpty { return "Stage name can't be empty" }
        if trimmedName.count > Self.nameLimit { return "Stage name is too long" }

        if calendar.startOfDay(for: plannedEnd) < calendar.startOfDay(for: plannedStart) {
            return "Planned end is before the planned start"
        }

        // ปิด stage ที่ไม่เคยเปิด ทำให้ `WorkStageRow.state` อ่านว่า completed ทั้งที่ไม่มีวันเริ่มจริง
        // ฐานข้อมูลปฏิเสธด้วย constraint เดียวกัน
        if actualEnd != nil && actualStart == nil {
            return "Set the actual start before the actual end"
        }
        if let start = actualStart, let end = actualEnd,
           calendar.startOfDay(for: end) < calendar.startOfDay(for: start) {
            return "Actual end is before the actual start"
        }

        return nil
    }

    /// ข้อผิดพลาดแรกของทั้งรายการ พร้อมบอกว่าเป็นของ stage ไหน
    /// ผู้ใช้ที่มีแปด stage ต้องรู้ว่าต้องไปแก้บรรทัดไหน ไม่ใช่แค่รู้ว่ามีอะไรผิดสักที่
    static func firstError(in drafts: [WorkStageDraft], calendar: Calendar) -> String? {
        for draft in drafts {
            guard let message = draft.validationError(calendar: calendar) else { continue }
            let label = draft.code.trimmingCharacters(in: .whitespacesAndNewlines)
            return label.isEmpty ? message : "\(label): \(message)"
        }
        return nil
    }
}
```

- [ ] **Step 4: เพิ่ม `WorkStageTypeRow` ใน `WorkRows.swift`**

รูปร่างเหมือน `WorkTypeRow` ทุกอย่าง แต่เป็นคนละตารางและคนละความหมาย จึงเป็นคนละ type
การใช้ type เดียวกันสองที่จะทำให้ส่งรายการผิดตัวเข้า `Picker` ได้โดย compiler ไม่ทัก

```swift
nonisolated struct WorkStageTypeRow: Codable, Sendable, Identifiable {
    let code: String
    let label: String
    let position: Int

    var id: String { code }
}
```

- [ ] **Step 5: เขียน harness**

```bash
SCRATCH=$(mktemp -d)
echo "$SCRATCH"
```

เขียน `$SCRATCH/main.swift`:

```swift
import Foundation

var failures = 0

func check(_ name: String, _ condition: Bool) {
    if condition {
        print("ok   \(name)")
    } else {
        print("FAIL \(name)")
        failures += 1
    }
}

let cal = WorkFilter.calendar
func day(_ text: String) -> Date { WorkStageRow.dayFormatter.date(from: text)! }

let types = [
    WorkStageTypeRow(code: "RU",  label: "Requirement · User", position: 1),
    WorkStageTypeRow(code: "SIT", label: "System Integration Test", position: 4),
    WorkStageTypeRow(code: "SU",  label: "SIT & UAT", position: 6),
]
let knownCodes = Set(types.map(\.code))

// ── WorkDraft ────────────────────────────────────────────────────

var draft = WorkDraft()
check("ฟอร์มเปล่าฟ้องว่ายังไม่ได้เลือกประเภท", draft.validationError == "Pick a type")

draft.typeCode = "project"
check("เลือกประเภทแล้วยังฟ้องเรื่องชื่อ", draft.validationError == "Name can't be empty")

draft.name = "   \n  "
check("ชื่อที่มีแต่ช่องว่างไม่ผ่าน", draft.validationError == "Name can't be empty")

draft.name = "26-BP-07-02 | ปรับปรุงแอป Umay+ ระยะที่หนึ่ง"
check("ชื่อภาษาไทยผ่าน", draft.isValid)

draft.name = "  " + String(repeating: "ก", count: WorkDraft.nameLimit) + "  "
check("ชื่อยาวเท่าเพดานพอดีผ่าน หลังตัดช่องว่างหัวท้าย", draft.isValid)

draft.name = String(repeating: "ก", count: WorkDraft.nameLimit + 1)
check("ชื่อเกินเพดานไม่ผ่าน", draft.validationError == "Name is too long")

check("ช่องว่างกลายเป็น nil", WorkDraft.emptyToNil("   ") == nil)
check("ช่องที่มีข้อความไทยถูกตัดหัวท้าย", WorkDraft.emptyToNil("  ทีม IT  ") == "ทีม IT")

let row = WorkRow(
    id: UUID(),
    typeCode: "request",
    name: "26-BP-09-01 | แก้ปุ่มยืนยันบนหน้าชำระเงิน",
    description: nil,
    requestedBy: "ทีมบัญชี",
    updatedAt: Date(),
    stage: []
)
let fromRow = WorkDraft(row)
check("draft จากแถวจริงได้ประเภทมาถูก", fromRow.typeCode == "request")
check("description ที่เป็น null กลายเป็นสตริงว่างในฟอร์ม", fromRow.detail == "")
check("description ที่ว่างในฟอร์มกลายเป็น nil ขากลับ", fromRow.trimmedDetail == nil)
check("requestedBy ภาษาไทยเดินทางครบ", fromRow.trimmedRequestedBy == "ทีมบัญชี")

// ── WorkStageDraft ที่มาจากรายการ ────────────────────────────────

let picked = WorkStageDraft(type: types[0], startingFrom: day("2026-09-01"), calendar: cal)
check("stage ที่ติ๊กจากรายการผ่านได้ทันทีโดยไม่ต้องพิมพ์อะไรเลย",
      picked.validationError(calendar: cal) == nil)
check("รหัสมาจากรายการ", picked.code == "RU")
check("ชื่อมาจาก label ของรายการ", picked.name == "Requirement · User")
check("stage ที่ติ๊กจากรายการไม่ใช่ custom", picked.isCustom == false)

// ── WorkStageDraft ที่ผู้ใช้ตั้งเอง ──────────────────────────────

var custom = WorkStageDraft(custom: day("2026-11-01"), calendar: cal)
check("stage แบบ Other ยังไม่มีรหัส จึงยังไม่ผ่าน",
      custom.validationError(calendar: cal) == "Code can't be empty")

custom.code = "TRN"
check("มีรหัสแล้วแต่ยังไม่มีชื่อ ก็ยังไม่ผ่าน",
      custom.validationError(calendar: cal) == "Stage name can't be empty")

custom.name = "อบรมผู้ใช้ก่อนเปิดระบบ"
check("stage แบบ Other ที่กรอกครบผ่าน", custom.validationError(calendar: cal) == nil)
check("stage แบบ Other ถูกทำเครื่องหมายว่า custom", custom.isCustom)

// ── การอนุมาน isCustom ตอนโหลดกลับมา ─────────────────────────────

let knownRow = WorkStageRow(
    id: UUID(), code: "SU", name: "SIT & UAT", position: 4,
    plannedStart: "2027-01-01", plannedEnd: "2027-02-28",
    actualStart: nil, actualEnd: nil
)
check("รหัสที่อยู่ในรายการอ่านกลับมาเป็นไม่ใช่ custom",
      WorkStageDraft(knownRow, knownCodes: knownCodes).isCustom == false)

let unknownRow = WorkStageRow(
    id: UUID(), code: "TRN", name: "อบรมผู้ใช้ก่อนเปิดระบบ", position: 9,
    plannedStart: "2027-03-01", plannedEnd: "2027-03-15",
    actualStart: nil, actualEnd: nil
)
check("รหัสนอกรายการอ่านกลับมาเป็น custom",
      WorkStageDraft(unknownRow, knownCodes: knownCodes).isCustom)

// ── กติกาของวันที่ ───────────────────────────────────────────────

var sameDay = WorkStageDraft(type: types[1], startingFrom: day("2026-09-01"), calendar: cal)
sameDay.plannedEnd = sameDay.plannedStart.addingTimeInterval(-3600 * 5)
check("stage ที่เริ่มและจบวันเดียวกันผ่าน แม้เวลาของวันจบจะน้อยกว่า",
      sameDay.validationError(calendar: cal) == nil)

var backwards = picked
backwards.plannedEnd = day("2026-08-01")
check("วันจบก่อนวันเริ่มไม่ผ่าน",
      backwards.validationError(calendar: cal) == "Planned end is before the planned start")

var closedNeverOpened = picked
closedNeverOpened.actualEnd = day("2026-09-15")
check("ปิด stage ที่ไม่เคยเปิดไม่ผ่าน",
      closedNeverOpened.validationError(calendar: cal) == "Set the actual start before the actual end")

var actualBackwards = picked
actualBackwards.actualStart = day("2026-09-10")
actualBackwards.actualEnd = day("2026-09-05")
check("วันจบจริงก่อนวันเริ่มจริงไม่ผ่าน",
      actualBackwards.validationError(calendar: cal) == "Actual end is before the actual start")

var tooLongCode = picked
tooLongCode.code = String(repeating: "X", count: WorkStageDraft.codeLimit + 1)
check("รหัสยาวเกินไม่ผ่าน", tooLongCode.validationError(calendar: cal) == "Code is too long")

check("firstError บอกว่าเป็นของ stage ไหน",
      WorkStageDraft.firstError(in: [picked, backwards], calendar: cal)
          == "RU: Planned end is before the planned start")
check("รายการที่ถูกต้องทั้งหมดไม่มี error",
      WorkStageDraft.firstError(in: [picked, custom], calendar: cal) == nil)

print(failures == 0 ? "ALL PASS" : "\(failures) FAILED")
exit(failures == 0 ? 0 : 1)
```

- [ ] **Step 6: รัน harness ให้ผ่านทุกข้อ**

```bash
swiftc -o "$SCRATCH/run" \
  null-app/Features/work/WorkRows.swift \
  null-app/Features/work/WorkFilter.swift \
  null-app/Features/work/WorkDraft.swift \
  "$SCRATCH/main.swift" && "$SCRATCH/run"
```

Expected: ทุกบรรทัดขึ้นต้นด้วย `ok` และปิดท้ายด้วย `ALL PASS`
ถ้ามี `FAIL` ให้แก้ `WorkDraft.swift` ไม่ใช่แก้ harness — harness เขียนจากสเปกและจาก constraint ในงานที่ 3

- [ ] **Step 7: build ทั้งสองแพลตฟอร์ม**

```bash
xcodebuild -scheme null-app -destination 'generic/platform=iOS Simulator' build 2>&1 | tail -3
```

Expected: `** BUILD SUCCEEDED **`

```bash
xcodebuild -scheme null-app -destination 'platform=macOS' build 2>&1 | tail -3
```

Expected: `** BUILD SUCCEEDED **`

- [ ] **Step 8: Commit**

```bash
git add null-app/Features/work/
git commit -m "Give the feature one calendar, and the form one set of rules"
```

---

### Task 5: payload ที่ล้างค่าได้จริง

กับดักที่กินเวลาไปแล้วครั้งหนึ่ง: `Encodable` ที่ compiler สังเคราะห์ใช้ `encodeIfPresent`
ฟิลด์ที่เป็น nil จึงหายไปจาก JSON ทั้งคีย์ แล้ว PostgREST อ่าน "ไม่มีคีย์" ว่า "อย่าแตะคอลัมน์นี้"
ผลคือผู้ใช้ลบคำอธิบายทิ้ง กด Save หน้าจอบอกว่าสำเร็จ แล้วคำอธิบายเดิมยังอยู่ในฐานข้อมูล

**Files:**
- Create: `null-app/Features/work/WorkPayload.swift`
- Test: harness ชั่วคราวนอก repo

**Interfaces:**
- Consumes: `WorkDraft`, `WorkStageDraft` จากงานที่ 4 · `WorkStageRow.dayFormatter` จากรอบ 2
- Produces:
  - `WorkPayload` — `init(insertFor userID: UUID, draft: WorkDraft)`, `init(updateFrom draft: WorkDraft)`
  - `WorkStagePayload` — `init(insertFor workID: UUID, draft: WorkStageDraft, position: Int)`, `init(updateFor id: UUID, draft: WorkStageDraft, position: Int)`

- [ ] **Step 1: เขียน `WorkPayload.swift`**

```swift
//
//  WorkPayload.swift
//  null-app
//

import Foundation

/// รูปร่างของ JSON ที่ส่งขึ้น PostgREST
///
/// **ห้าม import Supabase ในไฟล์นี้** — ดูเหตุผลใน WorkRows.swift
///
/// **ทุก type ในไฟล์นี้เขียน `encode(to:)` เอง ห้ามพึ่งตัวที่ compiler สังเคราะห์ให้**
/// ตัวสังเคราะห์ใช้ `encodeIfPresent` กับ Optional ฟิลด์ที่เป็น nil จึงหายไปจาก JSON ทั้งคีย์
/// แล้ว PostgREST อ่าน "ไม่มีคีย์" ว่า "อย่าแตะคอลัมน์นี้" ผลคือการลบคำอธิบายทิ้ง
/// หรือถอนวันจบจริงออก กลายเป็นการกด Save ที่ไม่เกิดอะไรขึ้นเลย และหน้าจอก็ยังดูถูกต้องทุกอย่าง
/// กับดักนี้กินเวลาไปแล้วครั้งหนึ่ง — ดู CLAUDE.md หัวข้อ "Traps that have already cost time"
nonisolated struct WorkPayload: Encodable, Sendable {
    /// ส่งเฉพาะตอน insert — เจ้าของแถวเปลี่ยนไม่ได้ PATCH จึงไม่ควรเอ่ยถึงคอลัมน์นี้เลย
    /// **นี่คือฟิลด์เดียวในไฟล์ที่ "ไม่ส่ง" เป็นพฤติกรรมที่ถูกต้อง** ที่เหลือต้องส่งเสมอ
    let userID: UUID?
    let typeCode: String

    /// รหัสงาน (`26-BP-07-02`) รวมอยู่ในนี้ ไม่มีคอลัมน์แยก — สเปกตัดสินไว้แล้วว่า
    /// รหัสถูกใช้เป็นส่วนหนึ่งของชื่อในการสื่อสารจริงอยู่แล้ว การแยกออกมาจะได้ช่องบังคับ
    /// เพิ่มมาหนึ่งช่องแลกกับความสามารถที่ยังไม่มีใครขอ
    let name: String
    let detail: String?
    let requestedBy: String?

    enum CodingKeys: String, CodingKey {
        case userID = "user_id"
        case typeCode = "type_code"
        case name
        case detail = "description"
        case requestedBy = "requested_by"
    }

    init(insertFor userID: UUID, draft: WorkDraft) {
        self.userID = userID
        typeCode = draft.typeCode
        name = draft.trimmedName
        detail = draft.trimmedDetail
        requestedBy = draft.trimmedRequestedBy
    }

    init(updateFrom draft: WorkDraft) {
        userID = nil
        typeCode = draft.typeCode
        name = draft.trimmedName
        detail = draft.trimmedDetail
        requestedBy = draft.trimmedRequestedBy
    }

    func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        if let userID { try container.encode(userID, forKey: .userID) }
        try container.encode(typeCode, forKey: .typeCode)
        try container.encode(name, forKey: .name)
        // `encodeNil` เขียนเจตนาให้เห็นชัด การเรียก `encode` ทับ Optional ตรง ๆ ได้ผลเหมือนกัน
        // แต่หน้าตาเหมือน encodeIfPresent จนคนอ่านครั้งหน้าแยกไม่ออกว่าตั้งใจหรือพลาด
        if let detail { try container.encode(detail, forKey: .detail) }
        else { try container.encodeNil(forKey: .detail) }
        if let requestedBy { try container.encode(requestedBy, forKey: .requestedBy) }
        else { try container.encodeNil(forKey: .requestedBy) }
    }
}

/// stage หนึ่งแถวที่กำลังจะถูกส่งขึ้น
///
/// วันที่ถูกแปลงเป็น `yyyy-MM-dd` ที่นี่ที่เดียว ด้วย formatter ตัวเดียวกับที่ขาอ่านใช้
/// การส่ง `Date` ตรง ๆ ให้ encoder ของ library จะได้ ISO8601 พร้อมเวลาและโซน
/// ซึ่งคอลัมน์ `date` รับได้แต่ตัดเวลาทิ้งตามโซนของเซิร์ฟเวอร์ ไม่ใช่ตามที่ผู้ใช้เลือก
nonisolated struct WorkStagePayload: Encodable, Sendable {
    /// ส่งเฉพาะตอน update — บอกว่าจะแก้แถวไหน
    let id: UUID?
    /// ส่งเฉพาะตอน insert — บอกว่าแขวนใต้ Work ไหน แถวที่มีอยู่แล้วย้ายเจ้าของไม่ได้
    let workID: UUID?
    let code: String
    let name: String
    let position: Int
    let plannedStart: String
    let plannedEnd: String
    let actualStart: String?
    let actualEnd: String?

    enum CodingKeys: String, CodingKey {
        case id
        case workID = "work_id"
        case code, name, position
        case plannedStart = "planned_start"
        case plannedEnd = "planned_end"
        case actualStart = "actual_start"
        case actualEnd = "actual_end"
    }

    private init(id: UUID?, workID: UUID?, draft: WorkStageDraft, position: Int) {
        self.id = id
        self.workID = workID
        code = draft.code.trimmingCharacters(in: .whitespacesAndNewlines)
        name = draft.name.trimmingCharacters(in: .whitespacesAndNewlines)
        self.position = position
        plannedStart = WorkStageRow.dayFormatter.string(from: draft.plannedStart)
        plannedEnd = WorkStageRow.dayFormatter.string(from: draft.plannedEnd)
        actualStart = draft.actualStart.map(WorkStageRow.dayFormatter.string(from:))
        actualEnd = draft.actualEnd.map(WorkStageRow.dayFormatter.string(from:))
    }

    init(insertFor workID: UUID, draft: WorkStageDraft, position: Int) {
        self.init(id: nil, workID: workID, draft: draft, position: position)
    }

    init(updateFor id: UUID, draft: WorkStageDraft, position: Int) {
        self.init(id: id, workID: nil, draft: draft, position: position)
    }

    func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        if let id { try container.encode(id, forKey: .id) }
        if let workID { try container.encode(workID, forKey: .workID) }
        try container.encode(code, forKey: .code)
        try container.encode(name, forKey: .name)
        try container.encode(position, forKey: .position)
        try container.encode(plannedStart, forKey: .plannedStart)
        try container.encode(plannedEnd, forKey: .plannedEnd)
        // สองคู่นี้คือหัวใจของไฟล์ — ผู้ใช้ที่ปิด toggle "Finished" กำลังบอกว่า stage นี้ยังไม่จบ
        // ถ้าคีย์หายไป คอลัมน์จะคาวันจบเดิมไว้ตลอดกาล
        if let actualStart { try container.encode(actualStart, forKey: .actualStart) }
        else { try container.encodeNil(forKey: .actualStart) }
        if let actualEnd { try container.encode(actualEnd, forKey: .actualEnd) }
        else { try container.encodeNil(forKey: .actualEnd) }
    }
}
```

- [ ] **Step 2: เขียน harness ที่อ่าน JSON จริง ไม่ใช่เชื่อว่ามันถูก**

เขียนทับ `$SCRATCH/main.swift`:

```swift
import Foundation

var failures = 0

func check(_ name: String, _ condition: Bool) {
    if condition { print("ok   \(name)") } else { print("FAIL \(name)"); failures += 1 }
}

let cal = WorkFilter.calendar
func day(_ text: String) -> Date { WorkStageRow.dayFormatter.date(from: text)! }

func json<T: Encodable>(_ value: T) -> String {
    let encoder = JSONEncoder()
    encoder.outputFormatting = [.sortedKeys]
    return String(data: try! encoder.encode(value), encoding: .utf8)!
}

func keys<T: Encodable>(_ value: T) -> Set<String> {
    let data = try! JSONEncoder().encode(value)
    return Set((try! JSONSerialization.jsonObject(with: data) as! [String: Any]).keys)
}

func isNull<T: Encodable>(_ value: T, _ key: String) -> Bool {
    let data = try! JSONEncoder().encode(value)
    let object = try! JSONSerialization.jsonObject(with: data) as! [String: Any]
    return object[key] is NSNull
}

// ── Work ─────────────────────────────────────────────────────────

var draft = WorkDraft()
draft.typeCode = "project"
draft.name = "  26-BP-07-02 | ปรับปรุงแอป Umay+ ระยะที่หนึ่ง  "
draft.detail = ""
draft.requestedBy = "   "

let update = WorkPayload(updateFrom: draft)
check("PATCH ไม่เอ่ยถึง user_id เลย", !keys(update).contains("user_id"))
check("PATCH ส่งคีย์ description มาด้วยเสมอ", keys(update).contains("description"))
check("คำอธิบายที่ถูกล้างเป็น null ไม่ใช่หายไป", isNull(update, "description"))
check("ผู้ขอที่มีแต่ช่องว่างเป็น null", isNull(update, "requested_by"))
check("รหัสงานอยู่ในชื่อ ไม่มีคอลัมน์แยก",
      json(update).contains("26-BP-07-02 | ปรับปรุงแอป Umay+ ระยะที่หนึ่ง\"") && !keys(update).contains("code"))

let owner = UUID()
let insert = WorkPayload(insertFor: owner, draft: draft)
check("INSERT ส่ง user_id ไปด้วย", keys(insert).contains("user_id"))
check("INSERT ยังส่ง null ให้ช่องที่ว่าง", isNull(insert, "description"))

draft.detail = "ปรับปรุงหน้าแรกและหน้าโปรไฟล์"
let filled = WorkPayload(updateFrom: draft)
check("คำอธิบายที่มีค่าไม่ใช่ null", !isNull(filled, "description"))
check("ข้อความไทยเดินทางครบไม่ถูกแปลงเป็น escape", json(filled).contains("ปรับปรุงหน้าแรกและหน้าโปรไฟล์"))

// ── stage ────────────────────────────────────────────────────────

let sit = WorkStageTypeRow(code: "SIT", label: "System Integration Test", position: 4)
var stage = WorkStageDraft(type: sit, startingFrom: day("2026-09-01"), calendar: cal)
stage.plannedEnd = day("2026-09-30")

let stageID = UUID()
let stageUpdate = WorkStagePayload(updateFor: stageID, draft: stage, position: 2)
check("แถวที่มีอยู่แล้วไม่ย้ายเจ้าของ — ไม่มี work_id", !keys(stageUpdate).contains("work_id"))
check("แถวที่มีอยู่แล้วบอกว่าจะแก้แถวไหน", keys(stageUpdate).contains("id"))
check("ยังไม่เริ่มจริง ส่ง actual_start เป็น null", isNull(stageUpdate, "actual_start"))
check("ยังไม่จบจริง ส่ง actual_end เป็น null", isNull(stageUpdate, "actual_end"))
check("รหัสมาจากรายการ", json(stageUpdate).contains("\"code\":\"SIT\""))
check("ลำดับมาจากผู้เรียก ไม่ใช่จาก draft", json(stageUpdate).contains("\"position\":2"))

let workID = UUID()
let stageInsert = WorkStagePayload(insertFor: workID, draft: stage, position: 1)
check("แถวใหม่บอกว่าแขวนใต้ Work ไหน", keys(stageInsert).contains("work_id"))
check("แถวใหม่ไม่ส่ง id — ฐานข้อมูลเป็นคนแจก", !keys(stageInsert).contains("id"))
check("วันเริ่มตามแผนเขียนเป็น yyyy-MM-dd ตรงวัน", json(stageInsert).contains("\"planned_start\":\"2026-09-01\""))
check("วันจบตามแผนเขียนเป็น yyyy-MM-dd ตรงวัน", json(stageInsert).contains("\"planned_end\":\"2026-09-30\""))

var custom = WorkStageDraft(custom: day("2026-11-01"), calendar: cal)
custom.code = " TRN "
custom.name = " อบรมผู้ใช้ก่อนเปิดระบบ "
let customPayload = WorkStagePayload(insertFor: workID, draft: custom, position: 9)
check("stage แบบ Other ส่งรหัสที่ตัดช่องว่างแล้ว", json(customPayload).contains("\"code\":\"TRN\""))
check("stage แบบ Other ส่งชื่อไทยที่ตัดช่องว่างแล้ว", json(customPayload).contains("\"name\":\"อบรมผู้ใช้ก่อนเปิดระบบ\""))

// จำลองสิ่งที่ DatePicker ของเครื่องที่ตั้งโซนกรุงเทพฯ ส่งมา ถ้าไม่ได้ตรึงโซนไว้:
// เที่ยงคืนของวันที่ 1 กันยายน ตามเวลากรุงเทพฯ คือ 2026-08-31T17:00Z
var bangkok = Calendar(identifier: .gregorian)
bangkok.timeZone = TimeZone(identifier: "Asia/Bangkok")!
var drifted = stage
drifted.plannedStart = bangkok.date(from: DateComponents(year: 2026, month: 9, day: 1))!
check("รู้ตัวว่าโซนเวลาที่ไม่ถูกตรึงทำให้คลาดไปหนึ่งวัน — view ต้องใส่ .environment(\\.timeZone) เสมอ",
      json(WorkStagePayload(insertFor: workID, draft: drifted, position: 1))
          .contains("\"planned_start\":\"2026-08-31\""))

var closed = stage
closed.actualStart = day("2026-09-03")
closed.actualEnd = day("2026-09-28")
let closedPayload = WorkStagePayload(updateFor: stageID, draft: closed, position: 2)
check("วันจริงที่มีค่าไม่ใช่ null", !isNull(closedPayload, "actual_start"))
check("วันจบจริงเขียนตรงวัน", json(closedPayload).contains("\"actual_end\":\"2026-09-28\""))

var reopened = closed
reopened.actualEnd = nil
let reopenedPayload = WorkStagePayload(updateFor: stageID, draft: reopened, position: 2)
check("ถอนวันจบจริงออกแล้วต้องส่ง null ไม่ใช่เงียบ", isNull(reopenedPayload, "actual_end"))
check("ถอนวันจบแล้ววันเริ่มจริงยังอยู่", !isNull(reopenedPayload, "actual_start"))

print(failures == 0 ? "ALL PASS" : "\(failures) FAILED")
exit(failures == 0 ? 0 : 1)
```

ข้อ "รู้ตัวว่าโซนเวลา…" **ยืนยันพฤติกรรมที่ผิด** โดยตั้งใจ มันคือหลักฐานว่าอันตรายนี้มีจริง
และเป็นเหตุผลที่งานที่ 8 ต้องใส่ `.environment(\.timeZone, ...)` ถ้าวันหนึ่งข้อนี้ FAIL
แปลว่ามีคนแก้ formatter หรือแก้ปฏิทินให้ไปตามโซนเครื่อง — ต้องหยุดอ่านว่าเกิดอะไรขึ้น ไม่ใช่ลบ assertion ทิ้ง

- [ ] **Step 3: รัน harness ให้ผ่านทุกข้อ**

```bash
swiftc -o "$SCRATCH/run" \
  null-app/Features/work/WorkRows.swift \
  null-app/Features/work/WorkFilter.swift \
  null-app/Features/work/WorkDraft.swift \
  null-app/Features/work/WorkPayload.swift \
  "$SCRATCH/main.swift" && "$SCRATCH/run"
```

Expected: ทุกบรรทัดขึ้นต้นด้วย `ok` และปิดท้ายด้วย `ALL PASS`

- [ ] **Step 4: build ทั้งสองแพลตฟอร์ม แล้ว commit**

```bash
xcodebuild -scheme null-app -destination 'generic/platform=iOS Simulator' build 2>&1 | tail -3
```

Expected: `** BUILD SUCCEEDED **`

```bash
xcodebuild -scheme null-app -destination 'platform=macOS' build 2>&1 | tail -3
```

Expected: `** BUILD SUCCEEDED **`

```bash
git add null-app/Features/work/WorkPayload.swift
git commit -m "Make clearing a field send null, not silence"
```

---

### Task 6: `WorkStore` อ่านรายการ Stage Type และเขียนได้

**Files:**
- Modify: `null-app/Features/work/WorkStore.swift`

**Interfaces:**
- Consumes: `WorkPayload`, `WorkStagePayload` จากงานที่ 5
- Produces:
  - `WorkStore.stageTypes: [WorkStageTypeRow]`
  - `WorkStore.saveError: String?` และ `func clearSaveError()`
  - `func createWork(_ draft: WorkDraft) async -> UUID?`
  - `func updateWork(_ id: UUID, draft: WorkDraft) async -> Bool`
  - `func deleteWork(_ id: UUID) async -> Bool`
  - `func saveStages(_ drafts: [WorkStageDraft], for workID: UUID, existing: [WorkStageRow]) async -> Bool`
  - งานที่ 7 และ 8 เรียกเฉพาะชุดนี้ ไม่แตะ `Backend.client` เอง

- [ ] **Step 1: ดึงสตริง select ออกมาไว้ที่เดียว และเพิ่ม state**

ใต้ `private let userID: UUID` เติม:

```swift
    /// คอลัมน์ที่หน้าจอต้องใช้ เขียนไว้ที่เดียวเพราะทั้ง `load()` และ `createWork(_:)` ใช้ชุดเดียวกัน
    /// สองสำเนาที่ต้องแก้พร้อมกันคือสองสำเนาที่วันหนึ่งจะไม่ตรงกัน
    private static let workSelect = """
        id,type_code,name,description,requested_by,updated_at,\
        stage(id,code,name,position,planned_start,planned_end,actual_start,actual_end)
        """
```

แก้ `load()` ให้ใช้ `.select(Self.workSelect)` และเติม property สองตัวใต้ `private(set) var types`:

```swift
    /// รายการ stage มาตรฐาน — หน้าแบ่ง stage วนจากตัวนี้ ไม่มีชื่อขั้นตอนเขียนตายในโค้ดเลย
    private(set) var stageTypes: [WorkStageTypeRow] = []

    /// แยก "บันทึกไม่สำเร็จ" ออกจาก "โหลดไม่สำเร็จ" — สองอย่างนี้พูดคนละประโยคกับผู้ใช้
    /// และอยู่คนละที่บนจอ อันหนึ่งแทนที่รายการทั้งหน้า อีกอันอยู่ใต้ปุ่ม Save ที่กดไปแล้ว
    private(set) var saveError: String?

    func clearSaveError() { saveError = nil }

    /// `error.localizedDescription` ของ PostgrestError คือข้อความจาก Postgres ทั้งดุ้น
    /// รวมชื่อ constraint ที่ผู้ใช้ไม่มีทางรู้จัก จึงแปลงเป็นประโยคเดียวที่พาไปทำอะไรต่อได้
    /// ส่วนตัวเต็มพิมพ์ลง console ไว้ตอน debug — การซ่อนทั้งหมดทำให้ไล่ปัญหาไม่ได้เลย
    private func fail(_ error: Error) {
        #if DEBUG
        print("[WorkStore] \(error)")
        #endif
        saveError = "Couldn't save. Check your connection and try again."
    }
```

- [ ] **Step 2: ดึงรายการ Stage Type มาพร้อมกับที่เหลือ**

ใน `load()` เติมคำขอที่สามคู่กับสองอันเดิม แล้วรับผลพร้อมกัน:

```swift
            async let stageTypesTask: [WorkStageTypeRow] = Backend.client
                .schema("f_work")
                .from("stage_type")
                .select("code,label,position")
                .eq("is_active", value: true)
                .order("position")
                .execute()
                .value

            let (loadedWorks, loadedTypes, loadedStageTypes) =
                try await (worksTask, typesTask, stageTypesTask)
```

แล้วเติม `stageTypes = loadedStageTypes` ข้าง ๆ `types = loadedTypes`

คำขอที่สามยิงขนานกับสองอันเดิมด้วย `async let` ไม่ใช่ต่อคิว — สามคำขอที่รอกันเป็นทอด ๆ
ทำให้หน้าจอช้ากว่าที่ควรสามเท่าโดยไม่ได้อะไรกลับมา

- [ ] **Step 3: เขียนสามฟังก์ชันของ Work**

เติมท้ายคลาส:

```swift
    /// สร้าง Work ใหม่ คืน id ของแถวที่เกิดขึ้นจริง
    ///
    /// ต้องได้ id กลับมา ไม่ใช่แค่ true/false เพราะ id คือสิ่งที่พาผู้ใช้ไปหน้าแบ่ง stage
    /// ต่อได้ทันที และ **id เป็นของที่ฐานข้อมูลแจก** แอปห้ามมินต์เอง กติกาเดียวกับ `stable_suffix`
    func createWork(_ draft: WorkDraft) async -> UUID? {
        do {
            let created: [WorkRow] = try await Backend.client
                .schema("f_work")
                .from("work")
                .insert(WorkPayload(insertFor: userID, draft: draft))
                .select(Self.workSelect)
                .execute()
                .value
            saveError = nil
            await load()
            return created.first?.id
        } catch {
            fail(error)
            return nil
        }
    }

    func updateWork(_ id: UUID, draft: WorkDraft) async -> Bool {
        do {
            try await Backend.client
                .schema("f_work")
                .from("work")
                .update(WorkPayload(updateFrom: draft))
                .eq("id", value: id)
                .execute()
            saveError = nil
            await load()
            return true
        } catch {
            fail(error)
            return false
        }
    }

    /// ลบถาวร — stage ตามไปด้วยผ่าน `on delete cascade` ที่ประกาศไว้ตั้งแต่รอบ 2
    /// นี่คือทางเดียวในแอปที่ข้อมูลหายจริง และมันต้องมาจากการกดยืนยันของผู้ใช้เท่านั้น
    func deleteWork(_ id: UUID) async -> Bool {
        do {
            try await Backend.client
                .schema("f_work")
                .from("work")
                .delete()
                .eq("id", value: id)
                .execute()
            saveError = nil
            await load()
            return true
        } catch {
            fail(error)
            return false
        }
    }
```

- [ ] **Step 4: เขียนการบันทึก stage แบบเทียบทีละแถว**

```swift
    /// บันทึก stage ทั้งชุดของ Work หนึ่งชิ้น โดยเทียบของเก่ากับของใหม่ทีละแถว
    ///
    /// **ห้ามลบทั้งชุดแล้วใส่ใหม่** ถึงโค้ดจะสั้นกว่ามาก — รอบ 4 จะมีตาราง `task`
    /// แขวนใต้ `stage` ด้วย `on delete cascade` การลบ stage เพื่อเขียนทับจะพา task
    /// ที่ผู้ใช้ติ๊กสะสมมาทั้งเดือนหายไปด้วย และหน้าจอจะบอกว่าบันทึกสำเร็จทุกอย่าง
    ///
    /// `position` มาจากตำแหน่งในรายการที่ผู้ใช้เห็น ไม่ใช่ช่องให้พิมพ์เลขเอง —
    /// เลขที่พิมพ์เองคือเลขที่ซ้ำกันได้และไม่มีใครสังเกต
    func saveStages(
        _ drafts: [WorkStageDraft],
        for workID: UUID,
        existing: [WorkStageRow]
    ) async -> Bool {
        let keptIDs = Set(drafts.filter { !$0.isNew }.map(\.id))
        let removed = existing.map(\.id).filter { !keptIDs.contains($0) }

        do {
            if !removed.isEmpty {
                try await Backend.client
                    .schema("f_work").from("stage")
                    .delete()
                    .in("id", values: removed)
                    .execute()
            }

            for (index, draft) in drafts.enumerated() where !draft.isNew {
                try await Backend.client
                    .schema("f_work").from("stage")
                    .update(WorkStagePayload(updateFor: draft.id, draft: draft, position: index + 1))
                    .eq("id", value: draft.id)
                    .execute()
            }

            let inserts = drafts.enumerated()
                .filter(\.element.isNew)
                .map { WorkStagePayload(insertFor: workID, draft: $0.element, position: $0.offset + 1) }
            if !inserts.isEmpty {
                try await Backend.client
                    .schema("f_work").from("stage")
                    .insert(inserts)
                    .execute()
            }

            saveError = nil
            await load()
            return true
        } catch {
            fail(error)
            return false
        }
    }
```

- [ ] **Step 5: build ทั้งสองแพลตฟอร์ม แล้ว commit**

```bash
xcodebuild -scheme null-app -destination 'generic/platform=iOS Simulator' build 2>&1 | tail -3
```

Expected: `** BUILD SUCCEEDED **`

```bash
xcodebuild -scheme null-app -destination 'platform=macOS' build 2>&1 | tail -3
```

Expected: `** BUILD SUCCEEDED **`

```bash
git add null-app/Features/work/WorkStore.swift
git commit -m "Let the store write, and let it write nulls"
```

---

### Task 7: หน้าสร้าง Work และหน้าแก้ Work

**Files:**
- Create: `null-app/Features/work/WorkFormView.swift`
- Modify: `null-app/Features/work/WorkRootView.swift`

**Interfaces:**
- Consumes: `WorkStore.createWork/updateWork/deleteWork/saveError` จากงานที่ 6 · `WorkDraft` จากงานที่ 4
- Produces: `WorkRoute { let id: UUID }` — ค่าที่การ์ดผลักเข้า `NavigationStack`

- [ ] **Step 1: เขียน `WorkFormView.swift`**

```swift
//
//  WorkFormView.swift
//  null-app
//

import SwiftUI

/// ปลายทางของการกดการ์ดหนึ่งใบ
///
/// เป็น struct ห่อ UUID ไม่ใช่ UUID เปล่า ๆ เพราะหน้าถัดไปก็ใช้ UUID เหมือนกัน
/// `navigationDestination(for: UUID.self)` สองตัวในสแตกเดียวกันจะแย่งกันเงียบ ๆ
nonisolated struct WorkRoute: Hashable {
    let id: UUID
}

/// หน้าสร้าง Work และหน้าแก้ Work — view เดียว สองโหมด
///
/// `workID == nil` คือสร้างใหม่ (เปิดเป็น sheet) มีค่าคือแก้ของเดิม (ผลักเข้าสแตก)
/// ทั้งสองโหมดใช้ฟอร์มชุดเดียวกัน เพราะช่องเหมือนกันทุกช่อง — สอง view จะกลายเป็น
/// สองชุดกติกาที่ค่อย ๆ ห่างกันขึ้นทุกครั้งที่มีการแก้ข้างเดียว
struct WorkFormView: View {
    /// nil = สร้าง Work ใหม่
    let workID: UUID?
    let store: WorkStore

    @State private var draft = WorkDraft()
    @State private var didLoadDraft = false
    @State private var isSaving = false
    @State private var confirmingDelete = false

    @Environment(\.dismiss) private var dismiss

    private var work: WorkRow? {
        guard let workID else { return nil }
        return store.works.first { $0.id == workID }
    }

    var body: some View {
        Form {
            Section {
                // ตัวเลือกวนจาก work_type ที่เซิร์ฟเวอร์ให้มา
                // ไม่มีชื่อ Work Type เขียนตายอยู่ในไฟล์นี้แม้แต่ตัวเดียว
                Picker("Type", selection: $draft.typeCode) {
                    Text("Choose…").tag("")
                    ForEach(store.types) { type in
                        Text(type.label).tag(type.code)
                    }
                }

                TextField("Name", text: $draft.name, axis: .vertical)
                    .lineLimit(1...4)
            } footer: {
                // รหัสงานอยู่ในชื่อ ไม่มีคอลัมน์แยก — สเปกตัดสินไว้แล้วว่าการแยกออกมา
                // จะได้ช่องบังคับเพิ่มมาหนึ่งช่อง แลกกับความสามารถที่ยังไม่มีใครขอ
                Text("Put the work code in the name, like 26-BP-07-02 | …")
            }

            Section("Optional") {
                TextField("What this covers", text: $draft.detail, axis: .vertical)
                    .lineLimit(2...6)
                TextField("Requested by", text: $draft.requestedBy)
            }

            if let work {
                Section {
                    NavigationLink(value: WorkStageRoute(workID: work.id)) {
                        LabeledContent("Stages") {
                            Text(work.stage.isEmpty
                                 ? "None yet"
                                 : "\(work.stage.count) stage\(work.stage.count == 1 ? "" : "s")")
                        }
                    }
                }

                Section {
                    Button("Delete this work", role: .destructive) {
                        confirmingDelete = true
                    }
                } footer: {
                    Text("Deleting removes its stages too. This can't be undone.")
                }
            } else {
                Section {
                    // stage ไม่บังคับตอนสร้างตามสเปก — บังคับติ๊กแปดขั้นพร้อมวันที่ก่อนกด Save
                    // ครั้งแรกได้ คือแบบฟอร์มที่คนกดยกเลิกกลางทาง
                    Text("You can pick the stages after saving.")
                        .foregroundStyle(.secondary)
                }
            }

            if let message = store.saveError {
                Section {
                    Label(message, systemImage: "exclamationmark.triangle")
                        .foregroundStyle(.red)
                }
            }
        }
        .navigationTitle(workID == nil ? "New work" : "Edit work")
        #if !os(macOS)
        .navigationBarTitleDisplayMode(.inline)
        #endif
        .toolbar {
            ToolbarItem(placement: .confirmationAction) {
                Button("Save", action: save)
                    .disabled(!draft.isValid || isSaving)
            }
            if workID == nil {
                ToolbarItem(placement: .cancellationAction) {
                    Button("Cancel") { dismiss() }
                }
            }
        }
        .confirmationDialog(
            "Delete this work?",
            isPresented: $confirmingDelete,
            titleVisibility: .visible
        ) {
            Button("Delete", role: .destructive, action: delete)
        } message: {
            Text("Its stages go too. This can't be undone.")
        }
        .task {
            // โหลดค่าเข้าฟอร์มครั้งเดียว — `store.works` เปลี่ยนทุกครั้งที่บันทึกสำเร็จ
            // ถ้าเติมค่าใหม่ทุกครั้งที่ store ขยับ ผู้ใช้ที่กำลังพิมพ์อยู่จะโดนเขียนทับกลางคัน
            guard !didLoadDraft else { return }
            if let work { draft = WorkDraft(work) }
            didLoadDraft = true
        }
        .onDisappear { store.clearSaveError() }
    }

    /// งานเขียนอยู่ใน `Task` ที่ไม่ผูกกับอายุของ view โดยตั้งใจ
    /// `.task` จะถูกยกเลิกเมื่อ view หายไป และคำขอที่ถูกยกเลิกกลางทางคือคำขอที่
    /// ไม่มีใครรู้ว่าแถวเข้าไปแล้วหรือยัง — สภาพที่แย่กว่าทั้งสำเร็จและล้มเหลว
    private func save() {
        isSaving = true
        Task {
            let ok: Bool
            if let workID {
                ok = await store.updateWork(workID, draft: draft)
            } else {
                ok = await store.createWork(draft) != nil
            }
            isSaving = false
            if ok { dismiss() }
        }
    }

    private func delete() {
        guard let workID else { return }
        isSaving = true
        Task {
            let ok = await store.deleteWork(workID)
            isSaving = false
            if ok { dismiss() }
        }
    }
}
```

- [ ] **Step 2: ต่อปุ่ม `+` และการกดการ์ดใน `WorkRootView`**

1. เติม state ใต้ `@State private var today = Date()`:

```swift
    @State private var isCreating = false
```

2. แทนที่ปุ่มใน `.toolbar` ที่ยัง `.disabled(true)` อยู่:

```swift
            ToolbarItem(placement: .primaryAction) {
                Button {
                    isCreating = true
                } label: {
                    Image(systemName: "plus")
                }
                // ยังไม่มี Work Type ให้เลือกก็สร้างไม่ได้ เพราะเป็นช่องบังคับ
                // ปิดปุ่มไว้ตรงไปตรงมากว่าเปิดฟอร์มที่กด Save ไม่ได้
                .disabled(store.types.isEmpty)
                .accessibilityLabel("Add work")
            }
```

3. เติมท้าย modifier chain ของ `ScrollView` (หลัง `.task { … }`):

```swift
        .sheet(isPresented: $isCreating) {
            NavigationStack {
                WorkFormView(workID: nil, store: store)
            }
        }
        .navigationDestination(for: WorkRoute.self) { route in
            WorkFormView(workID: route.id, store: store)
        }
        .navigationDestination(for: WorkStageRoute.self) { route in
            WorkStageEditorView(workID: route.workID, store: store)
        }
```

4. ห่อการ์ดใน `list` ด้วย `NavigationLink(value:)`:

```swift
            LazyVStack(spacing: 10) {
                ForEach(visibleWorks) { work in
                    // `NavigationLink(value:)` ไม่สร้างปลายทางจนกว่าจะกดจริง
                    // ต่างจาก `NavigationLink(destination:)` ที่สร้างทันทีที่วาดทุกใบ
                    NavigationLink(value: WorkRoute(id: work.id)) {
                        WorkCard(work: work, today: today)
                    }
                    .buttonStyle(.plain)
                }
            }
```

- [ ] **Step 3: ยังไม่ commit**

จะยัง build ไม่ผ่านจนกว่างานที่ 8 จะมี `WorkStageEditorView` — **นี่คือสิ่งที่คาดไว้**

```bash
xcodebuild -scheme null-app -destination 'generic/platform=iOS Simulator' build 2>&1 | grep -E "error:" | head -5
```

Expected: มีแต่บรรทัดที่พูดถึง `WorkStageEditorView` หรือ `WorkStageRoute`
ถ้า error พูดถึงอย่างอื่น ให้แก้ก่อนเดินต่อ

---

### Task 8: หน้าแบ่ง Stage

หัวใจของรอบนี้ **ติ๊กเลือกจากรายการ ไม่ใช่พิมพ์เอง** — ตัวอย่างจริงคือแปดขั้น
ถ้าต้องพิมพ์รหัสกับชื่อเองทุกขั้นจะเป็น 32 ช่องกรอก พอเลือกจากรายการเหลือ 16
และไม่มีทางเกิด `Dev` กับ `Development` ปนกันในระบบเดียวกัน

**Files:**
- Create: `null-app/Features/work/WorkStageEditorView.swift`

**Interfaces:**
- Consumes: `WorkStore.saveStages/stageTypes` จากงานที่ 6 · `WorkStageDraft` จากงานที่ 4 · `WorkFilter.calendar`
- Produces: `WorkStageRoute { let workID: UUID }`

- [ ] **Step 1: เขียน `WorkStageEditorView.swift`**

```swift
//
//  WorkStageEditorView.swift
//  null-app
//

import SwiftUI

/// ปลายทางของ "Stages" บนหน้าแก้ Work
nonisolated struct WorkStageRoute: Hashable {
    let workID: UUID
}

/// แบ่ง Stage ของ Work หนึ่งชิ้น
///
/// รายการ `stage_type` เป็น **เมนูให้ติ๊ก ไม่ใช่ลำดับบังคับ** — Work ที่ข้าม SIT ก็ไม่ติ๊ก
/// Work ที่รวม SIT กับ UAT ก็ติ๊ก `SU` และยังตั้ง stage นอกรายการเองได้ผ่าน "Other"
///
/// การเรียงลำดับใช้ปุ่มขึ้น/ลง ไม่ใช่ `.onMove` กับ `EditButton` — `EditButton` ไม่มีบน macOS
/// และแอปนี้เป็นเป้าหมายเดียวที่ลงสี่แพลตฟอร์ม ปุ่มที่หน้าตาเหมือนกันทุกที่
/// ชนะการมีท่าเรียงลำดับที่ใช้ได้เฉพาะ iPhone
struct WorkStageEditorView: View {
    let workID: UUID
    let store: WorkStore

    @State private var drafts: [WorkStageDraft] = []
    @State private var didLoad = false
    @State private var isSaving = false
    @State private var validationMessage: String?

    @Environment(\.dismiss) private var dismiss

    private var work: WorkRow? { store.works.first { $0.id == workID } }

    /// ขั้นที่ยังไม่ถูกติ๊ก — เมนู "Add a stage" แสดงเฉพาะพวกนี้
    /// ขั้นที่ติ๊กแล้วหายจากเมนู เพราะปุ่มที่กดแล้วไม่เกิดอะไรคือความสับสน
    private var unusedTypes: [WorkStageTypeRow] {
        let used = Set(drafts.map(\.code))
        return store.stageTypes.filter { !used.contains($0.code) }
    }

    /// stage ใหม่เริ่มวันถัดจากวันจบของอันสุดท้าย เพราะนั่นคือรูปแบบที่พบบ่อยที่สุด
    /// stage ทับกันได้ก็จริง (PT คาบ UAT, IMP คาบ PVT) แต่ให้ผู้ใช้เลื่อนให้ทับเอง
    /// ง่ายกว่าให้แอปเดาว่าจะทับตรงไหน
    private var nextStart: Date {
        guard let last = drafts.last else { return Date() }
        return WorkFilter.calendar.date(byAdding: .day, value: 1, to: last.plannedEnd)
            ?? last.plannedEnd
    }

    /// `Date.formatted()` **ไม่ได้** อ่านโซนเวลาจาก environment เหมือน `DatePicker`
    /// จึงต้องบอกที่นี่เอง ไม่งั้นหัวข้อจะโชว์วันคลาดจากที่ DatePicker ข้างในแสดงหนึ่งวัน
    private static let summaryStyle: Date.FormatStyle = {
        var style = Date.FormatStyle.dateTime.day().month(.abbreviated).year()
        style.timeZone = TimeZone(secondsFromGMT: 0)!
        return style
    }()

    var body: some View {
        List {
            if drafts.isEmpty {
                Section {
                    Text("Pick the stages this work goes through. The counters and the lateness number both come from their dates.")
                        .foregroundStyle(.secondary)
                }
            }

            ForEach($drafts) { $draft in
                Section {
                    DisclosureGroup {
                        // ช่องพิมพ์รหัสกับชื่อโผล่เฉพาะ stage แบบ Other
                        // ขั้นที่ติ๊กจากรายการได้ชื่อมาจากหลังบ้านแล้ว แก้ที่นี่ไม่ได้โดยตั้งใจ
                        if draft.isCustom {
                            TextField("Code", text: $draft.code)
                            TextField("Stage name", text: $draft.name, axis: .vertical)
                                .lineLimit(1...3)
                        }

                        DatePicker("Planned start", selection: $draft.plannedStart, displayedComponents: .date)
                        DatePicker("Planned end", selection: $draft.plannedEnd, displayedComponents: .date)

                        optionalDate("Started", date: $draft.actualStart, fallback: draft.plannedStart)
                        optionalDate(
                            "Finished",
                            date: $draft.actualEnd,
                            fallback: draft.actualStart ?? draft.plannedEnd
                        )

                        HStack {
                            Button {
                                move(draft.id, by: -1)
                            } label: {
                                Label("Earlier", systemImage: "arrow.up")
                            }
                            .disabled(index(of: draft.id) == 0)

                            Spacer()

                            Button {
                                move(draft.id, by: 1)
                            } label: {
                                Label("Later", systemImage: "arrow.down")
                            }
                            .disabled(index(of: draft.id) == drafts.count - 1)
                        }
                        .buttonStyle(.borderless)

                        Button("Remove this stage", role: .destructive) {
                            drafts.removeAll { $0.id == draft.id }
                        }
                    } label: {
                        // หัวข้อเป็นข้อความล้วน ไม่มีปุ่ม — ปุ่มในหัวข้อของ DisclosureGroup
                        // แย่งการแตะกับตัวพับเอง ทำให้กดปุ่มแล้วกลุ่มพับ/กางแทน
                        VStack(alignment: .leading, spacing: 2) {
                            Text(draft.code.isEmpty ? "New stage" : draft.code)
                                .font(.subheadline)
                                .fontWeight(.medium)

                            Text(draft.name.isEmpty ? "Needs a name" : draft.name)
                                .font(.caption)
                                .foregroundStyle(.secondary)
                                // ชื่อขั้นภาษาไทยไม่มีช่องว่าง ปล่อยให้ระบบตัดบรรทัดเอง
                                .fixedSize(horizontal: false, vertical: true)

                            Text("\(draft.plannedStart.formatted(Self.summaryStyle)) – \(draft.plannedEnd.formatted(Self.summaryStyle))")
                                .font(.caption2)
                                .foregroundStyle(.tertiary)
                        }
                    }
                } footer: {
                    if let message = draft.validationError(calendar: WorkFilter.calendar) {
                        Text(message).foregroundStyle(.red)
                    }
                }
            }

            Section("Add a stage") {
                // วนจากรายการที่เซิร์ฟเวอร์ให้มา ไม่มีชื่อขั้นตอนเขียนตายในไฟล์นี้เลย
                // วันที่มีขั้นที่สิบ หน้านี้ไม่ต้องแก้สักบรรทัด
                ForEach(unusedTypes) { type in
                    Button {
                        drafts.append(WorkStageDraft(
                            type: type,
                            startingFrom: nextStart,
                            calendar: WorkFilter.calendar
                        ))
                    } label: {
                        LabeledContent(type.label) {
                            Text(type.code).foregroundStyle(.secondary)
                        }
                    }
                }

                Button {
                    drafts.append(WorkStageDraft(custom: nextStart, calendar: WorkFilter.calendar))
                } label: {
                    Label("Other…", systemImage: "plus")
                }
            }

            if validationMessage != nil || store.saveError != nil {
                Section {
                    if let validationMessage {
                        Label(validationMessage, systemImage: "exclamationmark.triangle")
                            .foregroundStyle(.red)
                    }
                    if let message = store.saveError {
                        Label(message, systemImage: "exclamationmark.triangle")
                            .foregroundStyle(.red)
                    }
                }
            }
        }
        // `DatePicker` อ่านปฏิทินและโซนเวลาจาก environment สองบรรทัดนี้คือสิ่งที่ทำให้
        // วันที่ผู้ใช้เลือกกับวันที่เซิร์ฟเวอร์ได้รับเป็นวันเดียวกัน — ดูข้อพิสูจน์ในงานที่ 5
        // ถ้าลบสองบรรทัดนี้ ทุกวันที่จะคลาดไปหนึ่งวันในโซนที่นำหน้า UTC โดยไม่มี error ใด ๆ
        .environment(\.calendar, WorkFilter.calendar)
        .environment(\.timeZone, WorkFilter.calendar.timeZone)
        .navigationTitle("Stages")
        #if !os(macOS)
        .navigationBarTitleDisplayMode(.inline)
        #endif
        .toolbar {
            ToolbarItem(placement: .confirmationAction) {
                Button("Save", action: save).disabled(isSaving)
            }
        }
        .task {
            guard !didLoad else { return }
            let knownCodes = Set(store.stageTypes.map(\.code))
            drafts = (work?.stage ?? []).map { WorkStageDraft($0, knownCodes: knownCodes) }
            didLoad = true
        }
        .onDisappear { store.clearSaveError() }
    }

    /// toggle ที่แทน `Date?` ได้จริง — ปิดแล้วค่าเป็น nil ซึ่งจะกลายเป็น `null` ในสาย
    /// ไม่ใช่คีย์ที่หายไปจาก JSON ดู `WorkPayload.swift`
    @ViewBuilder
    private func optionalDate(
        _ label: String,
        date: Binding<Date?>,
        fallback: Date
    ) -> some View {
        Toggle(label, isOn: Binding(
            get: { date.wrappedValue != nil },
            set: { isOn in date.wrappedValue = isOn ? (date.wrappedValue ?? fallback) : nil }
        ))

        if let value = date.wrappedValue {
            DatePicker(
                "\(label) on",
                selection: Binding(get: { value }, set: { date.wrappedValue = $0 }),
                displayedComponents: .date
            )
        }
    }

    private func index(of id: UUID) -> Int {
        drafts.firstIndex { $0.id == id } ?? 0
    }

    private func move(_ id: UUID, by offset: Int) {
        let from = index(of: id)
        let to = from + offset
        guard drafts.indices.contains(to) else { return }
        drafts.swapAt(from, to)
    }

    private func save() {
        guard let work else { return }

        // ตรวจทั้งชุดก่อนยิง เพราะ error จากฐานข้อมูลจะบอกแค่ชื่อ constraint
        // ไม่บอกว่าเป็นของ stage ไหนในแปดอัน
        if let message = WorkStageDraft.firstError(in: drafts, calendar: WorkFilter.calendar) {
            validationMessage = message
            return
        }
        validationMessage = nil

        isSaving = true
        Task {
            let ok = await store.saveStages(drafts, for: workID, existing: work.stage)
            isSaving = false
            if ok { dismiss() }
        }
    }
}
```

- [ ] **Step 2: build ทั้งสองแพลตฟอร์ม**

```bash
xcodebuild -scheme null-app -destination 'generic/platform=iOS Simulator' build 2>&1 | tail -3
```

Expected: `** BUILD SUCCEEDED **`

```bash
xcodebuild -scheme null-app -destination 'platform=macOS' build 2>&1 | tail -3
```

Expected: `** BUILD SUCCEEDED **`

- [ ] **Step 3: Commit งานที่ 7 กับ 8 พร้อมกัน**

```bash
git add null-app/Features/work/
git commit -m "Create work in the app, and pick its stages from a menu"
```

---

### Task 9: พิสูจน์กับของจริง

CLAUDE.md เขียนไว้ว่า **"หน้าจอที่ดูถูกต้องไม่ใช่หลักฐาน"** — บั๊กหลายตัวในโปรเจกต์นี้
แสดงตัวเป็นหน้าจอที่ถูกต้องทับสถานะบนเซิร์ฟเวอร์ที่ผิด งานนี้จึงอ่านแถวกลับมาเทียบทุกครั้ง

**Files:** ไม่มีไฟล์ใน repo

**Interfaces:**
- Consumes: ทุกอย่างจากงานที่ 1 ถึง 8

- [ ] **Step 1: ติดตั้งและเปิดแอป**

```bash
xcrun simctl list devices booted
```

จด UDID เป็น `SIM` แล้ว:

```bash
xcrun simctl install "$SIM" "$(xcodebuild -scheme null-app -destination 'generic/platform=iOS Simulator' -showBuildSettings 2>/dev/null | awk -F' = ' '/ BUILT_PRODUCTS_DIR /{print $2"/null-app.app"}')"
xcrun simctl launch "$SIM" purin.null-app
```

**ห้าม** `simctl keychain reset` — ต้องเข้าเป็นเจ้าของข้อมูลจริง

- [ ] **Step 2: สร้าง Work ใหม่ด้วยชื่อภาษาไทยจริง**

กด `+` แล้วกรอก:

- Type: `Project`
- Name: `26-BP-99-01 | ทดสอบการสร้างงานจากในแอปรอบที่สาม`
- What this covers: เว้นว่าง
- Requested by: `ทีมทดสอบ`

กด Save ถ่ายภาพหน้าจอ ยืนยันว่าการ์ดใบใหม่โผล่พร้อมคำเชิญ `Add a timeline`

- [ ] **Step 3: อ่านแถวกลับมาเทียบ**

```sql
select id, type_code, name, description, requested_by, archived_at
from f_work.work where name like '26-BP-99-01%';
```

Expected:
- `name` ตรงตัวอักษรต่อตัวอักษร รวมสระและวรรณยุกต์ไทย และ**รหัสงานอยู่ในชื่อ** ไม่ได้แยกไปไหน
- `description` เป็น **null** ไม่ใช่ `""` — ถ้าเป็นสตริงว่างแปลว่า `emptyToNil` ไม่ถูกเรียก
- `requested_by` = `ทีมทดสอบ`

จด `id` เป็น `TEST_WORK`

- [ ] **Step 4: พิสูจน์ว่าการล้างค่าล้างได้จริง**

กดการ์ดเข้าหน้าแก้ ลบข้อความในช่อง `Requested by` ให้ว่าง แล้ว Save

```sql
select requested_by from f_work.work where id = 'TEST_WORK';
```

Expected: **null**
ถ้ายังเป็น `ทีมทดสอบ` แปลว่าคีย์หายไปจาก PATCH — กลับไปดู `encode(to:)` ใน `WorkPayload.swift`
**นี่คือขั้นที่กับดักเดิมจะโผล่ ถ้าโผล่จริงห้ามเดินต่อ**

- [ ] **Step 5: แบ่ง Stage จากเมนู แล้วเทียบวันที่ทีละวัน**

เข้าหน้าแก้ → Stages → ติ๊กจากเมนูสี่ขั้นตามลำดับ: `RU`, `RI`, `DEV`, `SU`
จากนั้นกด `Other…` อีกหนึ่งขั้น กรอก Code `TRN` ชื่อ `อบรมผู้ใช้ก่อนเปิดระบบ`

แก้วันของขั้นแรกเป็น 1 ก.ย. 2026 – 30 ก.ย. 2026 แล้ว Save

```sql
select code, name, position, planned_start, planned_end, actual_start, actual_end
from f_work.stage where work_id = 'TEST_WORK' order by position;
```

Expected:
- ห้าแถว `position` เป็น 1–5 เรียงตามที่เห็นบนจอ
- แถวแรก `code = RU`, `name = Requirement · User`, `planned_start = 2026-09-01`, `planned_end = 2026-09-30`
- แถวที่สี่ `code = SU`, `name = SIT & UAT`
- แถวที่ห้า `code = TRN`, `name = อบรมผู้ใช้ก่อนเปิดระบบ` — ขั้นแบบ Other เข้าไปได้ทั้งที่ไม่อยู่ในรายการ
- `actual_start` และ `actual_end` เป็น null ทุกแถว

**ถ้าวันที่คลาดไปหนึ่งวัน** แปลว่า `.environment(\.timeZone, …)` หลุดไปจาก `WorkStageEditorView`
— กับดักที่ harness ในงานที่ 5 ยืนยันไว้แล้วว่ามีจริง

- [ ] **Step 6: ยืนยันว่าเมนูไม่โชว์ขั้นที่ติ๊กไปแล้ว**

กลับเข้าหน้า Stages อีกครั้ง ถ่ายภาพหน้าจอส่วน `Add a stage`

Expected: เหลือ `SIT`, `UAT`, `PT`, `PVT`, `IMP` และ `Other…` — ไม่มี `RU`, `RI`, `DEV`, `SU`
(`SIT` กับ `UAT` ยังอยู่ในเมนูแม้จะติ๊ก `SU` ไปแล้ว ซึ่งถูกต้อง — สามตัวนี้เป็นทางเลือกคนละแบบ ไม่ใช่ตัวเดียวกัน)

- [ ] **Step 7: พิสูจน์ว่าถอนวันจริงออกได้**

กางขั้น `RU` เปิด toggle `Started` เลือก 3 ก.ย. 2026 แล้ว Save

```sql
select code, actual_start from f_work.stage where work_id = 'TEST_WORK' order by position;
```

Expected: แถวแรก `actual_start = 2026-09-03`

กลับเข้าไป **ปิด** toggle `Started` แล้ว Save อีกครั้ง แล้วรัน query เดิม

Expected: แถวแรกกลับเป็น **null**
ถ้ายังเป็น `2026-09-03` แปลว่า `encodeNil` ไม่ทำงานบนเส้นทางของ stage

- [ ] **Step 8: พิสูจน์ว่า trigger ดัน `updated_at` ของ Work**

```sql
select w.updated_at as work_updated, max(s.updated_at) as stage_updated
from f_work.work w join f_work.stage s on s.work_id = w.id
where w.id = 'TEST_WORK' group by w.updated_at;
```

Expected: สองค่าห่างกันไม่เกินไม่กี่วินาที — แก้ stage แล้ว Work ถือว่าขยับ

- [ ] **Step 9: ตรวจว่าหน้าจอตรงกับแถว**

ถ่ายภาพหน้าจอ Work แล้วยืนยัน:
- การ์ดของ Work ทดสอบมีแถบ stage ห้าช่อง ไม่ใช่คำเชิญ `Add a timeline` แล้ว
- ชื่อ stage ปัจจุบันใต้แถบ **ไม่มี** chip ต่อท้าย (badge ถูกถอดออกในงานที่ 1)
- รหัสบนแถบอ่านออกทั้งห้าช่อง รวม `TRN` ที่ยาวสามตัวอักษรในช่องแคบ

- [ ] **Step 10: ลบ Work ทดสอบ แล้วยืนยันว่า stage ตามไปด้วย**

ในแอป: หน้าแก้ → `Delete this work` → ยืนยัน

```sql
select count(*) as works from f_work.work where id = 'TEST_WORK';
select count(*) as stages from f_work.stage where work_id = 'TEST_WORK';
```

Expected: `0` ทั้งสองบรรทัด

- [ ] **Step 11: ตรวจว่าข้อมูลจริงไม่ถูกกระทบ**

```sql
select w.name, count(s.id) as stages
from f_work.work w left join f_work.stage s on s.work_id = w.id
group by w.name order by w.name;
```

Expected: Work สามชิ้นเดิมยังอยู่ครบ และ `26-BP-07-02` ยังมี 7 stage เท่าเดิม

- [ ] **Step 12: build ปิดท้ายทั้งสองแพลตฟอร์ม**

```bash
xcodebuild -scheme null-app -destination 'generic/platform=iOS Simulator' build 2>&1 | tail -3
```

Expected: `** BUILD SUCCEEDED **`

```bash
xcodebuild -scheme null-app -destination 'platform=macOS' build 2>&1 | tail -3
```

Expected: `** BUILD SUCCEEDED **`

- [ ] **Step 13: บันทึกสิ่งที่พิสูจน์แล้ว**

เติมผลลงใน `.superpowers/sdd/2026-08-18-work-round-3/progress.md` (สร้างโฟลเดอร์ถ้ายังไม่มี —
ไฟล์นี้ไม่ถูก track โดยตั้งใจ) อย่างน้อยต้องมี: ข้อไหนผ่าน ข้อไหนต้องแก้ก่อนผ่าน
และอะไรที่ยังไม่ได้พิสูจน์

---

## Self-Review

**Spec coverage**

| สเปกกำหนด | งานที่ครอบคลุม |
|---|---|
| `badge` ไม่มีคอลัมน์นี้ | งานที่ 1 (แอป) + งานที่ 3 Step 4 (ฐานข้อมูล) |
| **ไม่มีคอลัมน์ `code` แยก รหัสงานพิมพ์นำหน้าชื่อ** | งานที่ 5 `WorkPayload` ไม่มีคีย์ `code`, งานที่ 7 footer บอกวิธี, พิสูจน์งานที่ 9 Step 3 |
| ช่องบังคับมีสองช่อง: `type_code` และ `name` | งานที่ 4 `WorkDraft.validationError` |
| stage ทั้งชุดไม่บังคับตอนสร้าง | งานที่ 7 — โหมดสร้างไม่มีส่วน stage เลย |
| งานที่ยังไม่มี stage ขึ้นคำเชิญใส่ timeline | มีอยู่แล้วจากรอบ 2 พิสูจน์ซ้ำงานที่ 9 Step 2 |
| ไม่บังคับ template แต่ละโปรเจกต์กำหนด stage เอง | งานที่ 3 Step 1 — `stage.code` ไม่เป็น FK, มี "Other", ไม่มีขั้นไหนถูกเติมให้อัตโนมัติ |
| ห้ามเขียนชื่อ Work Type / Stage Type ตายตัวใน view | งานที่ 7 `Picker` วนจาก `store.types` · งานที่ 8 เมนูวนจาก `store.stageTypes` |
| `work_type` / `stage_type` เป็น reference data อ่านอย่างเดียว | งานที่ 3 Step 1–2, พิสูจน์ Step 9 |
| stage ผูกกับ work เจ้าของสืบผ่าน work | งานที่ 2 Step 2 พิสูจน์ว่า RLS ที่เขียนใหม่ยังปฏิเสธ |
| `position` เป็นลำดับใน timeline | งานที่ 6 Step 4 — มาจากตำแหน่งในรายการ ไม่ใช่ช่องกรอก |
| สาม state ของ stage อนุมานจากวันจริง | งานที่ 8 `optionalDate` เป็นทางเดียวที่ตั้งวันจริงได้ |
| stage ทับกันได้ | งานที่ 4 ไม่มีกติกาห้ามทับ · งานที่ 8 ตั้งค่าเริ่มต้นเป็นต่อกัน แต่เลื่อนให้ทับได้ |
| หน่วยเวลาละเอียดกว่าเดือน | `DatePicker` แบบ `.date` เก็บระดับวัน คอลัมน์เป็น `date` |
| ลบถาวรเป็นการกระทำแยกที่ต้องตั้งใจ | งานที่ 7 — ปุ่มอยู่ท้ายฟอร์มแก้ และมี `confirmationDialog` |
| ทดสอบด้วยข้อความไทยจริง | งานที่ 4, 5 (harness) และงานที่ 9 Step 2, 5 |
| `updated_at` ไม่โกหก | งานที่ 3 Step 4 trigger `stage_touch_work`, พิสูจน์ Step 8 และงานที่ 9 Step 8 |
| PATCH ที่ล้างค่าต้องส่ง null | งานที่ 5 ทั้งงาน, พิสูจน์กับของจริงงานที่ 9 Step 4 และ 7 |

**ยังไม่อยู่ในแผนนี้โดยตั้งใจ:** ตาราง `task` และ progress % (รอบ 4) · หน้า Timeline เต็ม (รอบ 5) ·
บล็อก "ช่วงนี้ควรโฟกัส" และ "เพิ่งขยับ" · การเข้ากรุและหน้ารายการที่เข้ากรุ

**Type consistency:** `WorkRow` ตั้งชื่อในงานที่ 2 และใช้ชื่อนี้ในงานที่ 4 (`WorkDraft.init(_ row: WorkRow)`),
6 (`createWork` คืน `[WorkRow]`), 7 (`store.works.first`) · `WorkStageTypeRow` ประกาศในงานที่ 4 Step 4
ถูกใช้ใน `WorkStageDraft.init(type:startingFrom:calendar:)` (งานที่ 4), harness (งานที่ 5),
`WorkStore.stageTypes` (งานที่ 6) และเมนู (งานที่ 8) · `WorkStageDraft.validationError(calendar:)`
รับ `Calendar` ทั้งใน harness, `firstError` และงานที่ 8 · `WorkFilter.calendar` ประกาศในงานที่ 4 Step 1
ถูกใช้ในงานที่ 4 Step 2, harness งานที่ 5 และงานที่ 8 · `WorkRoute` ประกาศใน `WorkFormView.swift` (งานที่ 7)
ส่วน `WorkStageRoute` ประกาศใน `WorkStageEditorView.swift` (งานที่ 8) และอ้างข้ามกันสองทาง —
เป็นเหตุผลที่งานที่ 7 ไม่ commit เดี่ยว · `WorkStore.workSelect` ประกาศงานที่ 6 Step 1 ใช้ใน `load()` และ `createWork(_:)`

**หนี้ที่แผนนี้ทิ้งไว้:**

1. `saveStages` ยิง update ทีละแถวในลูป — 8 stage คือ 8 คำขอ ที่จำนวนงานระดับนี้ยังไม่คุ้มกับการเขียน
   `upsert` ที่ต้องจัดการ conflict target เอง ถ้าวันหนึ่ง timeline ยาวเกิน ~15 stage ค่อยรวบเป็นคำขอเดียว
2. `saveStages` ไม่ใช่ transaction — เน็ตหลุดกลางทางแล้วบาง stage เข้าบางอันไม่เข้า หน้าจอจะโหลดใหม่
   แล้วแสดงสภาพจริงซึ่งผู้ใช้แก้ต่อได้ ถ้าวันหนึ่งต้องการอะตอมมิกจริง ๆ ต้องย้ายไปเป็น Postgres function
3. ไม่มีอะไรกันรหัสซ้ำใน Work เดียวกัน — เมนูกันซ้ำให้แล้วสำหรับขั้นที่มาจากรายการ (`unusedTypes`)
   แต่ stage แบบ Other พิมพ์รหัสซ้ำกันเองได้ จงใจไม่ใส่ unique index เพราะจะไปปิดกรณีที่เป็นไปได้จริง
   เช่น UAT สองรอบในโปรเจกต์เดียว
4. ชื่อของ stage ถูกคัดลอกจาก `stage_type.label` ตอนติ๊ก ไม่ได้ join ตอนอ่าน — แก้ label ที่หลังบ้าน
   แล้วแถวเก่าจะยังเป็นชื่อเดิม ตั้งใจให้เป็นแบบนี้เพราะ stage แบบ Other ต้องมีชื่อของตัวเองอยู่แล้ว
   และแถบบนการ์ดแสดง `code` ไม่ใช่ `name`
