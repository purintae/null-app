# Work tracker รอบ 2 — ข้อมูลจริงขึ้นหน้าจอ

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** ฟีเจอร์ `work` อ่านงานจริงจาก Supabase มาแสดงเป็นการ์ดพร้อมแถบ stage และตัวนับสี่ใบที่กดกรองได้

**Architecture:** สร้าง schema `f_work` สามตาราง (`work_type`, `item`, `stage`) โดย `item` เป็นเจ้าของแถวผ่าน `user_id` ส่วน `stage` สืบสิทธิ์ผ่าน `item` และ `work_type` เป็น reference data ที่อ่านได้อย่างเดียว ฝั่งแอปมี `WorkStore` เป็นเจ้าของ state คนเดียวตามแบบ `ProfileStore` และแยกตรรกะการนับทั้งหมดออกเป็น `WorkFilter` ที่ไม่ import Supabase เพื่อให้ทดสอบด้วย `swiftc` ได้จริง

**Tech Stack:** Swift 5 / SwiftUI (`SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor`), supabase-swift 2.x, Postgres 15 + RLS, deployment target 26.5

**Spec:** `docs/superpowers/specs/2026-08-14-work-tracker-design.md`

## Global Constraints

- feature id คือ `work` schema คือ `f_work` — ตรง `^[a-z][a-z0-9_]*$`
- ชื่อ type ทุกตัวขึ้นต้นด้วย `Work` (ทุกฟีเจอร์อยู่โมดูลเดียวกัน ชื่อไม่มี prefix จะชนกัน)
- แตะตารางผ่าน `Backend.client.schema("f_work").from(...)` เท่านั้น
- **ห้ามเขียนชื่อประเภทงานตายตัวใน view** — ตัวกรองวนจาก `work_type` ที่ดึงมา
- **ห้ามใช้ segmented picker กับตัวกรองประเภท** — ใช้ chip เรียงแนวนอนที่เลื่อนได้
- ป้ายกำกับของแอปเป็นอังกฤษ ข้อความที่ผู้ใช้พิมพ์เป็นไทยได้ — **ทดสอบด้วยข้อความไทยจริงเสมอ ไม่ใช่ placeholder อังกฤษ** (ไทยไม่มีช่องว่างระหว่างคำ การตัดบรรทัดจึงพึ่งพจนานุกรมของระบบ)
- Overdue **ไม่มีระยะผ่อนผัน** เลยกำหนดมาวันเดียวก็นับ
- ความช้ามีนิยามเดียวทั้งแอป — ตัวเลขบนการ์ดและเกณฑ์ของตัวนับต้องมาจากฟังก์ชันเดียวกัน
- `makeRoot` ถูกเรียกตอนวาด Home ทุกไอคอน ไม่ใช่ตอนกด — ห้ามทำงานจริงใน `makeRoot` และใน `init` ของ view ราก ให้อยู่ใน `.task`
- สร้างไฟล์ด้วย Write ตรง ๆ (synchronized file group) **ห้ามแก้ `project.pbxproj`**
- build ผ่านทั้ง iOS และ macOS ก่อนถือว่าเสร็จ
- ใช้ `apply_migration` สำหรับทุกการเปลี่ยน schema ห้ามพิมพ์ใน Dashboard

## File Structure

| ไฟล์ | หน้าที่ |
|---|---|
| `null-app/Features/work/WorkRow.swift` | รูปร่างของแถวทั้งสามตาราง — Foundation ล้วน ไม่ import Supabase |
| `null-app/Features/work/WorkFilter.swift` | ตรรกะการนับและความช้าทั้งหมด — Foundation ล้วน ทดสอบด้วย `swiftc` ได้ |
| `null-app/Features/work/WorkStore.swift` | เจ้าของ state คนเดียว คุยกับ Supabase |
| `null-app/Features/work/WorkStageBar.swift` | แถบ chip ของ stage บนการ์ด |
| `null-app/Features/work/WorkCard.swift` | การ์ดงานหนึ่งใบ |
| `null-app/Features/work/WorkRootView.swift` | **แก้** — ต่อ store, ตัวนับจริง, ตัวกรอง, รายการการ์ด |
| `null-app/Features/work/WorkFeature.swift` | **แก้** — ส่ง `userID` ต่อให้ root view |
| `null-app/Features/work/UNINSTALL.md` | **แก้** — เติมคำสั่ง export ตอนนี้ที่มีข้อมูลจริงแล้ว |

`WorkRow.swift` และ `WorkFilter.swift` ห้าม `import Supabase` เด็ดขาด — นั่นคือสิ่งเดียวที่ทำให้ชุดทดสอบด้วย `swiftc` ในงานที่ 3 เป็นไปได้

---

### Task 1: schema, RLS และการพิสูจน์ว่ามันปฏิเสธ

**Files:**
- Migration: `f_work_initial` (ผ่าน `apply_migration` ไม่มีไฟล์ใน repo)
- Modify: `null-app/Features/work/UNINSTALL.md`

**Interfaces:**
- Consumes: ไม่มี
- Produces: `f_work.work_type(code, label, position, is_active)`, `f_work.item(id, user_id, type_code, name, description, requested_by, badge, archived_at, created_at, updated_at)`, `f_work.stage(id, item_id, code, name, position, planned_start, planned_end, actual_start, actual_end, created_at, updated_at)` — ชื่อคอลัมน์เหล่านี้คืออะไรที่ `WorkRow.swift` ในงานที่ 2 ต้อง map ตรงตัว

- [ ] **Step 1: หา user id จริงสองคนไว้ทดสอบ**

ใช้ `execute_sql`:

```sql
select id, created_at from auth.users order by created_at limit 2;
```

จดไว้เป็น `USER_A` (เจ้าของแถว) และ `USER_B` (คนแปลกหน้า) ถ้ามีน้อยกว่าสองคน **หยุด** แล้วบอกผู้ใช้ — การพิสูจน์ RLS ต้องมีคนที่สองจริง ไม่ใช่ uuid สุ่ม เพราะ FK จะไม่ผ่าน

- [ ] **Step 2: สร้าง schema และตาราง**

`apply_migration` ชื่อ `f_work_initial`:

```sql
create schema if not exists f_work;

-- reference data ไม่ได้เป็นของใครคนใดคนหนึ่ง จึงไม่มี user_id
create table f_work.work_type (
    code text primary key,
    label text not null,
    position int not null,
    is_active boolean not null default true
);

insert into f_work.work_type (code, label, position) values
    ('project', 'Project', 1),
    ('request', 'Request', 2);

create table f_work.item (
    id uuid primary key default gen_random_uuid(),
    user_id uuid not null references auth.users(id) on delete cascade,
    type_code text not null references f_work.work_type(code),
    name text not null,
    description text,
    requested_by text,
    badge text,
    archived_at timestamptz,
    created_at timestamptz not null default now(),
    updated_at timestamptz not null default now()
);

create index item_user_id_idx on f_work.item (user_id);

-- stage ไม่มี user_id ของตัวเอง เจ้าของสืบผ่าน item
-- ถ้าใส่ user_id ซ้ำที่นี่ วันหนึ่งมันจะไม่ตรงกับ item แล้วไม่มีใครรู้ว่าอันไหนถูก
create table f_work.stage (
    id uuid primary key default gen_random_uuid(),
    item_id uuid not null references f_work.item(id) on delete cascade,
    code text not null,
    name text not null,
    position int not null,
    planned_start date not null,
    planned_end date not null,
    actual_start date,
    actual_end date,
    created_at timestamptz not null default now(),
    updated_at timestamptz not null default now()
);

create index stage_item_id_idx on f_work.stage (item_id);
```

- [ ] **Step 3: เปิด RLS และเขียน policy**

`apply_migration` ชื่อ `f_work_rls`:

```sql
alter table f_work.work_type enable row level security;
alter table f_work.item enable row level security;
alter table f_work.stage enable row level security;

-- work_type อ่านได้อย่างเดียว ไม่มี policy สำหรับ insert/update/delete เลย
-- ความปลอดภัยมาจากการไม่มี policy ไม่ใช่จากการเช็คในโค้ดแอป
create policy "read active types" on f_work.work_type
    for select using (true);

create policy "own item select" on f_work.item
    for select using (user_id = auth.uid());

create policy "own item insert" on f_work.item
    for insert with check (user_id = auth.uid());

create policy "own item update" on f_work.item
    for update using (user_id = auth.uid()) with check (user_id = auth.uid());

create policy "own item delete" on f_work.item
    for delete using (user_id = auth.uid());

-- with check ต้องมีด้วย ไม่งั้นย้าย stage ไปแขวนใต้ item ของคนอื่นได้
create policy "own stage select" on f_work.stage
    for select using (
        exists (select 1 from f_work.item i where i.id = stage.item_id and i.user_id = auth.uid())
    );

create policy "own stage insert" on f_work.stage
    for insert with check (
        exists (select 1 from f_work.item i where i.id = stage.item_id and i.user_id = auth.uid())
    );

create policy "own stage update" on f_work.stage
    for update using (
        exists (select 1 from f_work.item i where i.id = stage.item_id and i.user_id = auth.uid())
    ) with check (
        exists (select 1 from f_work.item i where i.id = stage.item_id and i.user_id = auth.uid())
    );

create policy "own stage delete" on f_work.stage
    for delete using (
        exists (select 1 from f_work.item i where i.id = stage.item_id and i.user_id = auth.uid())
    );
```

- [ ] **Step 4: trigger ให้ `updated_at` ไม่โกหก**

`apply_migration` ชื่อ `f_work_touch_updated_at`:

```sql
create function f_work.touch_updated_at() returns trigger
    language plpgsql
    set search_path = ''
as $$
begin
    new.updated_at = now();
    return new;
end;
$$;

create trigger touch_updated_at
    before update on f_work.item
    for each row execute function f_work.touch_updated_at();

create trigger touch_updated_at
    before update on f_work.stage
    for each row execute function f_work.touch_updated_at();
```

- [ ] **Step 5: grant ให้ `authenticated` เท่านั้น**

`apply_migration` ชื่อ `f_work_grants`:

```sql
grant usage on schema f_work to authenticated;
grant select on f_work.work_type to authenticated;
grant select, insert, update, delete on f_work.item to authenticated;
grant select, insert, update, delete on f_work.stage to authenticated;
```

ไม่ให้ `anon` แม้แต่ select — `auth.uid()` เป็น NULL อยู่แล้วจึงไม่ผ่าน policy แต่สิทธิ์ที่ไม่มีใครใช้คือสิทธิ์ที่ไม่มีใครตรวจ

- [ ] **Step 6: ใส่ข้อมูลทดสอบของ USER_A**

`execute_sql` แทน `<USER_A>` ด้วยค่าจริง ใช้ **ข้อความไทยจริง** ตาม Global Constraints:

```sql
insert into f_work.item (user_id, type_code, name, description, badge)
values ('<USER_A>', 'project', '26-BP-07-02 | ปรับปรุงแอป Umay+ ระยะที่หนึ่ง',
        'ออกแบบใหม่ทั้งแอปและเพิ่มฟังก์ชันตามแผนงานประจำปี', 'Get Requirement #2')
returning id;
```

จด id เป็น `ITEM_1` แล้วใส่ stage เจ็ดอันตามตัวอย่างจริงในสเปก (สังเกตว่า PT คาบกับ U และ PVT ยาวหนึ่งสัปดาห์คาบกับ I — **ต้องใส่ให้ทับกันจริงเพื่อทดสอบว่า UI รับกรณีนี้ได้**):

```sql
insert into f_work.stage (item_id, code, name, position, planned_start, planned_end, actual_start, actual_end) values
    ('<ITEM_1>', 'RU',  'Requirement · User',           1, '2026-06-01', '2026-07-31', '2026-06-01', '2026-07-28'),
    ('<ITEM_1>', 'RI',  'Requirement · IT',             2, '2026-08-01', '2026-09-30', '2026-08-03', null),
    ('<ITEM_1>', 'D',   'Development',                  3, '2026-10-01', '2026-12-31', null, null),
    ('<ITEM_1>', 'U',   'SIT & UAT',                    4, '2027-01-01', '2027-02-28', null, null),
    ('<ITEM_1>', 'PT',  'Penetration Test',             5, '2027-02-01', '2027-02-28', null, null),
    ('<ITEM_1>', 'PVT', 'Production Verification Test', 6, '2027-03-01', '2027-03-07', null, null),
    ('<ITEM_1>', 'I',   'Go-live',                      7, '2027-03-01', '2027-03-31', null, null);
```

แล้วเพิ่มงานอีกสองใบเพื่อให้ตัวนับมีค่าต่างกัน — **หนึ่งใบต้องเลยกำหนดจริง** และ **หนึ่งใบต้องไม่มี stage เลย**:

```sql
insert into f_work.item (user_id, type_code, name, badge)
values ('<USER_A>', 'request', '26-BP-09-01 | แก้ปุ่มยืนยันบนหน้าชำระเงิน', 'รอ UAT')
returning id;
```

จดเป็น `ITEM_2` แล้ว:

```sql
insert into f_work.stage (item_id, code, name, position, planned_start, planned_end, actual_start, actual_end) values
    ('<ITEM_2>', 'D', 'Development', 1, '2026-07-01', '2026-07-31', '2026-07-01', '2026-07-30'),
    ('<ITEM_2>', 'U', 'SIT & UAT',   2, '2026-08-01', '2026-08-10', '2026-08-04', null);
```

`ITEM_2` จะเป็น Overdue เพราะ stage `U` เลย `2026-08-10` มาแล้วและยังไม่ปิด

```sql
insert into f_work.item (user_id, type_code, name)
values ('<USER_A>', 'project', '27-BP-01-01 | งานที่ยังไม่ได้วางไทม์ไลน์');
```

ใบนี้ไม่มี stage เลย ใช้ทดสอบว่าการ์ดขึ้นคำเชิญให้ใส่ timeline และไม่ถูกนับในตัวนับใด

- [ ] **Step 7: พิสูจน์ว่า RLS ปฏิเสธ USER_B**

`execute_sql` — ต้องอยู่ใน transaction เดียวกันเพราะ `set local` มีผลแค่ใน transaction:

```sql
begin;
select set_config('request.jwt.claims',
    json_build_object('sub', '<USER_B>', 'role', 'authenticated')::text, true);
set local role authenticated;

select count(*) as items_visible_to_stranger from f_work.item;
select count(*) as stages_visible_to_stranger from f_work.stage;
select count(*) as types_visible from f_work.work_type;

rollback;
```

Expected: `items_visible_to_stranger = 0`, `stages_visible_to_stranger = 0`, `types_visible = 2`
(work_type เห็นได้ทุกคนโดยตั้งใจ — เป็น reference data)

- [ ] **Step 8: พิสูจน์ว่า USER_B ปลอม `user_id` ไม่ได้**

```sql
begin;
select set_config('request.jwt.claims',
    json_build_object('sub', '<USER_B>', 'role', 'authenticated')::text, true);
set local role authenticated;

insert into f_work.item (user_id, type_code, name)
values ('<USER_A>', 'project', 'แถวปลอม');

rollback;
```

Expected: **ต้อง error** ด้วย `new row violates row-level security policy for table "item"`
ถ้า insert สำเร็จ policy พัง — **หยุด อย่าเดินต่อ**

- [ ] **Step 9: พิสูจน์ว่า USER_B แขวน stage ใต้ item ของ USER_A ไม่ได้**

```sql
begin;
select set_config('request.jwt.claims',
    json_build_object('sub', '<USER_B>', 'role', 'authenticated')::text, true);
set local role authenticated;

insert into f_work.stage (item_id, code, name, position, planned_start, planned_end)
values ('<ITEM_1>', 'X', 'stage ปลอม', 99, '2026-01-01', '2026-01-31');

rollback;
```

Expected: **ต้อง error** ด้วย `new row violates row-level security policy for table "stage"`
นี่คือข้อที่ policy แบบ `using` อย่างเดียวจะปล่อยผ่าน — เป็นเหตุผลที่ทุก insert/update policy ต้องมี `with check`

- [ ] **Step 10: พิสูจน์ว่า `work_type` เขียนไม่ได้แม้แต่เจ้าของ**

```sql
begin;
select set_config('request.jwt.claims',
    json_build_object('sub', '<USER_A>', 'role', 'authenticated')::text, true);
set local role authenticated;

insert into f_work.work_type (code, label, position) values ('hack', 'Hack', 9);

rollback;
```

Expected: **ต้อง error** — ทั้งเพราะไม่มี grant insert และเพราะไม่มี policy
ถ้าผ่าน แปลว่า grant ในข้อ 5 ให้เกินมา

- [ ] **Step 11: พิสูจน์ว่า trigger ทำงาน**

```sql
select updated_at from f_work.item where id = '<ITEM_1>';
update f_work.item set badge = 'Get Requirement #3' where id = '<ITEM_1>';
select updated_at from f_work.item where id = '<ITEM_1>';
```

Expected: ค่าที่สองมากกว่าค่าแรก

- [ ] **Step 12: เติมคำสั่ง export ลง UNINSTALL.md**

แทนข้อ 4 เดิมใน `null-app/Features/work/UNINSTALL.md` ด้วยคำสั่งที่ใช้ได้จริง:

```markdown
4. งานของผู้ใช้อยู่ใน `f_work.item` และ `f_work.stage` และกู้ไม่ได้หลังข้อ 7 —
   export ก่อนด้วย `execute_sql` (`f_work.work_type` เป็น reference data ไม่ต้อง export):

   ```sql
   select i.name, i.description, i.requested_by, i.badge, i.archived_at,
          s.code, s.name as stage_name, s.position,
          s.planned_start, s.planned_end, s.actual_start, s.actual_end
   from f_work.item i
   left join f_work.stage s on s.item_id = i.id
   order by i.created_at, s.position;
   ```
```

- [ ] **Step 13: Commit**

```bash
git add null-app/Features/work/UNINSTALL.md
git commit -m "Give the work feature its schema and prove the policies refuse"
```

(migration ไม่มีไฟล์ใน repo — ประวัติอยู่ใน Supabase ตามธรรมเนียมโปรเจกต์)

---

### Task 2: เปิด schema ให้ Data API เห็น

**Files:** ไม่มีไฟล์ใน repo — ขั้นตอนนี้อยู่ใน Dashboard และการยืนยันด้วย curl

**Interfaces:**
- Consumes: schema `f_work` จากงานที่ 1
- Produces: REST endpoint `/rest/v1/item` ที่ตอบ 200 เมื่อส่ง `Accept-Profile: f_work` — งานที่ 4 พึ่งสิ่งนี้ทั้งหมด

- [ ] **Step 1: หยุดแล้วขอให้ผู้ใช้ทำ**

ขั้นตอนนี้ไม่มีเครื่องมือ MCP ทำแทนได้ บอกผู้ใช้ตามนี้:

> Supabase Dashboard → Project Settings → **Integrations → Data API → แท็บ Settings**
> → **Exposed schemas** → เพิ่ม `f_work` → Save
>
> ⚠️ หน้าเดียวกันมีช่อง **Extra search path** อยู่ใกล้กันและหน้าตาคล้ายมาก **คนละเรื่องกัน**
> ใส่ผิดช่องจะไม่มี error ใด ๆ ทุกอย่างดูเหมือนบันทึกสำเร็จ แต่ REST ยังตอบ `PGRST106`

รอจนผู้ใช้ยืนยันว่าทำแล้ว **ห้ามเดาว่าทำแล้ว**

- [ ] **Step 2: ยืนยันว่ามีผลจริง ก่อนเขียนโค้ดแอปแม้แต่บรรทัดเดียว**

```bash
curl -s -o /dev/null -w '%{http_code}\n' \
  -H "apikey: sb_publishable_TzWBdrFCJzBBL8wlrU-ksg_eJyYZto1" \
  -H "Accept-Profile: f_work" \
  "https://yqeqzplufezlnudsxzql.supabase.co/rest/v1/item?select=id&limit=1"
```

Expected: `200` (body เป็น `[]` เพราะ anon ไม่ผ่าน RLS ซึ่งถูกต้อง)
ถ้าได้ `404` — ข้อ 1 ยังไม่มีผล กลับไปดูว่าใส่ถูกช่องหรือยัง **อย่าเดินต่อ**

- [ ] **Step 3: ยืนยันว่า core ไม่พัง**

การเพิ่ม schema ทำให้ PostgREST สร้าง schema cache ใหม่ ต้องเช็กว่าตารางของ core ยังตอบ:

```bash
curl -s -o /dev/null -w '%{http_code}\n' \
  -H "apikey: sb_publishable_TzWBdrFCJzBBL8wlrU-ksg_eJyYZto1" \
  "https://yqeqzplufezlnudsxzql.supabase.co/rest/v1/profiles?select=user_id&limit=1"
```

Expected: `200` ถ้าได้ `PGRST002` แปลว่า schema cache สร้างไม่สำเร็จ — **หยุดแล้วแจ้งผู้ใช้ทันที** เพราะทั้งโปรเจกต์จะใช้ไม่ได้ รวมถึงการล็อกอิน

- [ ] **Step 4: ยืนยันว่า embed ใช้ได้**

งานที่ 4 จะดึง item พร้อม stage ในคำขอเดียว ต้องรู้ตั้งแต่ตอนนี้ว่า PostgREST มองเห็น FK:

```bash
curl -s -o /dev/null -w '%{http_code}\n' \
  -H "apikey: sb_publishable_TzWBdrFCJzBBL8wlrU-ksg_eJyYZto1" \
  -H "Accept-Profile: f_work" \
  "https://yqeqzplufezlnudsxzql.supabase.co/rest/v1/item?select=id,stage(id)&limit=1"
```

Expected: `200` ถ้าได้ `400` พร้อม `PGRST200` แปลว่าหา relationship ไม่เจอ — กลับไปดูว่า FK `stage.item_id → item.id` ถูกสร้างจริงในงานที่ 1

---

### Task 3: ตรรกะการนับที่ทดสอบได้จริง

**Files:**
- Create: `null-app/Features/work/WorkRow.swift`
- Create: `null-app/Features/work/WorkFilter.swift`
- Test: ชุด harness ชั่วคราวนอก repo (สร้างด้วย `mktemp -d`)

**Interfaces:**
- Consumes: ชื่อคอลัมน์จากงานที่ 1
- Produces:
  - `WorkStageRow` — `id: UUID`, `code: String`, `name: String`, `position: Int`, `plannedStart: String`, `plannedEnd: String`, `actualStart: String?`, `actualEnd: String?`, และ computed `plannedStartDate/plannedEndDate/actualStartDate/actualEndDate: Date?`
  - `WorkItemRow` — `id: UUID`, `typeCode: String`, `name: String`, `description: String?`, `requestedBy: String?`, `badge: String?`, `updatedAt: Date`, `stage: [WorkStageRow]`
  - `WorkTypeRow` — `code: String`, `label: String`, `position: Int`
  - `WorkFilter` — enum `.active/.overdue/.dueThisMonth/.startingSoon`, มี `label: String` และ `func matches(_ stages: [WorkStageRow], today: Date, calendar: Calendar) -> Bool`
  - `WorkFilter.daysLate(_ stages: [WorkStageRow], today: Date, calendar: Calendar) -> Int?`
  - `WorkStageRow.State` — enum `.completed/.current/.ahead` และ `func state(today:) -> State`

- [ ] **Step 1: เขียน `WorkRow.swift`**

```swift
//
//  WorkRow.swift
//  null-app
//

import Foundation

/// รูปร่างของแถวในตาราง f_work.*
///
/// **ห้าม import Supabase ในไฟล์นี้** — ความบริสุทธิ์ตรงนี้คือสิ่งเดียวที่ทำให้
/// คอมไพล์ไฟล์นี้กับ harness ด้วย swiftc เพื่อทดสอบตรรกะการนับได้โดยไม่ต้องมี Xcode
///
/// วันที่ของ stage เก็บเป็น `String` ไม่ใช่ `Date` โดยตั้งใจ — คอลัมน์เป็น Postgres `date`
/// ซึ่งส่งกลับมาเป็น `"2026-06-01"` ส่วน decoder ที่ supabase-swift ตั้งค่าไว้คาดหวัง
/// ISO8601 แบบเต็มพร้อมเวลา การ decode ตรง ๆ เป็น Date จึงล้มทั้งคำขอ
nonisolated struct WorkStageRow: Codable, Sendable, Identifiable {
    let id: UUID
    let code: String
    let name: String
    let position: Int
    let plannedStart: String
    let plannedEnd: String
    let actualStart: String?
    let actualEnd: String?

    enum CodingKeys: String, CodingKey {
        case id, code, name, position
        case plannedStart = "planned_start"
        case plannedEnd = "planned_end"
        case actualStart = "actual_start"
        case actualEnd = "actual_end"
    }

    /// ตัวแปลงตัวเดียวของทั้งฟีเจอร์ — locale คงที่และ UTC เพื่อให้ `"2026-06-01"`
    /// แปลเป็นวันเดียวกันเสมอไม่ว่าเครื่องผู้ใช้ตั้งโซนเวลาอะไร
    static let dayFormatter: DateFormatter = {
        let f = DateFormatter()
        f.locale = Locale(identifier: "en_US_POSIX")
        f.timeZone = TimeZone(secondsFromGMT: 0)
        f.dateFormat = "yyyy-MM-dd"
        return f
    }()

    var plannedStartDate: Date? { Self.dayFormatter.date(from: plannedStart) }
    var plannedEndDate: Date? { Self.dayFormatter.date(from: plannedEnd) }
    var actualStartDate: Date? { actualStart.flatMap(Self.dayFormatter.date(from:)) }
    var actualEndDate: Date? { actualEnd.flatMap(Self.dayFormatter.date(from:)) }

    /// สามสถานะที่อนุมานจากวันจริง ไม่ต้องมีคอลัมน์เก็บ
    enum State: Sendable {
        case completed
        case current
        case ahead
    }

    var state: State {
        if actualEnd != nil { return .completed }
        if actualStart != nil { return .current }
        return .ahead
    }
}

nonisolated struct WorkItemRow: Codable, Sendable, Identifiable {
    let id: UUID
    let typeCode: String
    let name: String
    let description: String?
    let requestedBy: String?
    let badge: String?
    let updatedAt: Date
    let stage: [WorkStageRow]

    enum CodingKeys: String, CodingKey {
        case id, name, description, badge, stage
        case typeCode = "type_code"
        case requestedBy = "requested_by"
        case updatedAt = "updated_at"
    }
}

nonisolated struct WorkTypeRow: Codable, Sendable, Identifiable {
    let code: String
    let label: String
    let position: Int

    var id: String { code }
}
```

- [ ] **Step 2: เขียน harness ที่ยังคอมไพล์ไม่ผ่าน**

```bash
SCRATCH=$(mktemp -d)
cat > "$SCRATCH/main.swift" <<'SWIFT'
import Foundation

var failures = 0

func check(_ name: String, _ condition: Bool) {
    if condition {
        print("ok   \(name)")
    } else {
        print("FAIL \(name)")
        failures += 1
    }
}

let cal: Calendar = {
    var c = Calendar(identifier: .gregorian)
    c.timeZone = TimeZone(secondsFromGMT: 0)!
    return c
}()

let today = WorkStageRow.dayFormatter.date(from: "2026-08-14")!

func stage(
    _ code: String,
    _ plannedStart: String,
    _ plannedEnd: String,
    actualStart: String? = nil,
    actualEnd: String? = nil
) -> WorkStageRow {
    WorkStageRow(
        id: UUID(), code: code, name: code, position: 1,
        plannedStart: plannedStart, plannedEnd: plannedEnd,
        actualStart: actualStart, actualEnd: actualEnd
    )
}

// งานที่เริ่มแล้วและยังไม่จบ
let running = [
    stage("RU", "2026-06-01", "2026-07-31", actualStart: "2026-06-01", actualEnd: "2026-07-28"),
    stage("RI", "2026-08-01", "2026-09-30", actualStart: "2026-08-03"),
]
check("active: เริ่มแล้วยังไม่จบ", WorkFilter.active.matches(running, today: today, calendar: cal))
check("overdue: ยังไม่เลยกำหนด", !WorkFilter.overdue.matches(running, today: today, calendar: cal))
check("daysLate: ไม่มีของสาย", WorkFilter.daysLate(running, today: today, calendar: cal) == nil)

// งานที่ทุก stage ปิดหมดแล้ว
let finished = [
    stage("D", "2026-07-01", "2026-07-31", actualStart: "2026-07-01", actualEnd: "2026-07-30"),
]
check("active: งานที่จบแล้วไม่นับ", !WorkFilter.active.matches(finished, today: today, calendar: cal))

// stage ที่เลยกำหนดจบมาแล้ว 4 วันแต่ยังไม่ปิด
let late = [
    stage("D", "2026-07-01", "2026-07-31", actualStart: "2026-07-01", actualEnd: "2026-07-30"),
    stage("U", "2026-08-01", "2026-08-10", actualStart: "2026-08-04"),
]
check("overdue: เลยกำหนดแล้วยังไม่ปิด", WorkFilter.overdue.matches(late, today: today, calendar: cal))
check("daysLate: นับได้ 4 วัน", WorkFilter.daysLate(late, today: today, calendar: cal) == 4)

// เลยกำหนดมาวันเดียวก็ต้องนับ — spec ระบุว่าไม่มีระยะผ่อนผัน
let barelyLate = [stage("U", "2026-08-01", "2026-08-13", actualStart: "2026-08-01")]
check("overdue: ไม่มีระยะผ่อนผัน", WorkFilter.overdue.matches(barelyLate, today: today, calendar: cal))
check("daysLate: หนึ่งวันคือหนึ่ง", WorkFilter.daysLate(barelyLate, today: today, calendar: cal) == 1)

// วันสุดท้ายพอดียังไม่ถือว่าเลย
let dueToday = [stage("U", "2026-08-01", "2026-08-14", actualStart: "2026-08-01")]
check("overdue: วันครบกำหนดพอดียังไม่นับ", !WorkFilter.overdue.matches(dueToday, today: today, calendar: cal))

// stage สุดท้ายจบเดือนนี้
check("dueThisMonth: จบเดือนนี้", WorkFilter.dueThisMonth.matches(late, today: today, calendar: cal))
check("dueThisMonth: จบปีหน้าไม่นับ", !WorkFilter.dueThisMonth.matches(running, today: today, calendar: cal))

// ยังไม่เริ่ม และเริ่มภายใน 30 วัน
let soon = [stage("RU", "2026-09-01", "2026-09-30")]
check("startingSoon: เริ่มอีก 18 วัน", WorkFilter.startingSoon.matches(soon, today: today, calendar: cal))

let notSoon = [stage("RU", "2026-11-01", "2026-11-30")]
check("startingSoon: เริ่มอีก 2 เดือนไม่นับ", !WorkFilter.startingSoon.matches(notSoon, today: today, calendar: cal))

// ยังไม่เริ่มทั้งที่เลยวันเริ่มตามแผนแล้ว — ต้องยังโผล่ใน startingSoon ไม่ใช่หายไปเฉย ๆ
let shouldHaveStarted = [stage("RU", "2026-08-01", "2026-09-30")]
check("startingSoon: เลยวันเริ่มแล้วยังไม่เริ่ม ต้องยังนับ",
      WorkFilter.startingSoon.matches(shouldHaveStarted, today: today, calendar: cal))

// งานที่ไม่มี stage เลย ต้องไม่เข้าตัวนับใดทั้งสิ้น
for f in WorkFilter.allCases {
    check("ไม่มี stage: ไม่เข้า \(f.label)", !f.matches([], today: today, calendar: cal))
}
check("ไม่มี stage: daysLate เป็น nil", WorkFilter.daysLate([], today: today, calendar: cal) == nil)

// สถานะของ stage
check("state: ปิดแล้วคือ completed", running[0].state == .completed)
check("state: เริ่มแล้วยังไม่ปิดคือ current", running[1].state == .current)
check("state: ยังไม่เริ่มคือ ahead", soon[0].state == .ahead)

print(failures == 0 ? "\nALL PASS" : "\n\(failures) FAILED")
exit(failures == 0 ? 0 : 1)
SWIFT
echo "$SCRATCH"
```

- [ ] **Step 3: รัน harness เพื่อดูว่ามันล้ม**

```bash
swiftc -o "$SCRATCH/run" \
  "null-app/Features/work/WorkRow.swift" \
  "null-app/Features/work/WorkFilter.swift" \
  "$SCRATCH/main.swift" && "$SCRATCH/run"
```

Expected: **คอมไพล์ไม่ผ่าน** ด้วย `no such file or directory: 'null-app/Features/work/WorkFilter.swift'`
ถ้าผ่านตั้งแต่ยังไม่มีไฟล์ แปลว่า harness ไม่ได้ทดสอบโค้ดจริง

- [ ] **Step 4: เขียน `WorkFilter.swift`**

```swift
//
//  WorkFilter.swift
//  null-app
//

import Foundation

/// ตรรกะการนับทั้งหมดของฟีเจอร์อยู่ที่เดียวคือที่นี่
///
/// ตัวนับสี่ใบบนหัวหน้าจอกับการกดกรองรายการข้างล่างใช้ฟังก์ชันเดียวกัน
/// ถ้าแยกกันวันหนึ่งจะเกิดสภาพที่ตัวนับบอกว่ามีสองงาน แต่กดแล้วรายการขึ้นสามงาน
/// แล้วไม่มีใครบอกได้ว่าอันไหนถูก
///
/// **ห้าม import Supabase ในไฟล์นี้** — ดูเหตุผลใน WorkRow.swift
nonisolated enum WorkFilter: String, CaseIterable, Sendable, Identifiable {
    case active
    case overdue
    case dueThisMonth
    case startingSoon

    var id: String { rawValue }

    /// ป้ายกำกับของแอปเป็นอังกฤษตามสเปก
    var label: String {
        switch self {
        case .active: "Active"
        case .overdue: "Overdue"
        case .dueThisMonth: "Due this month"
        case .startingSoon: "Starting soon"
        }
    }

    /// จำนวนวันที่เข้าเกณฑ์ startingSoon
    static let startingSoonWindow = 30

    func matches(_ stages: [WorkStageRow], today: Date, calendar: Calendar) -> Bool {
        // งานที่ยังไม่มี stage ไม่เข้าเกณฑ์ใดเลย — ทั้งสี่ใบวัดจากวันที่ของ stage
        // การ์ดของงานแบบนี้จะขึ้นคำเชิญให้ใส่ timeline แทน
        guard !stages.isEmpty else { return false }

        switch self {
        case .active:
            return stages.contains { $0.actualStart != nil } && !Self.isFinished(stages)

        case .overdue:
            return Self.overdueStages(stages, today: today, calendar: calendar).isEmpty == false

        case .dueThisMonth:
            guard !Self.isFinished(stages) else { return false }
            guard let last = stages.compactMap(\.plannedEndDate).max() else { return false }
            return calendar.isDate(last, equalTo: today, toGranularity: .month)

        case .startingSoon:
            guard stages.allSatisfy({ $0.actualStart == nil }) else { return false }
            guard let first = stages.compactMap(\.plannedStartDate).min() else { return false }
            guard let limit = calendar.date(byAdding: .day, value: Self.startingSoonWindow, to: today)
            else { return false }
            // ไม่มีขอบล่าง — งานที่เลยวันเริ่มตามแผนแล้วแต่ยังไม่เริ่ม ต้องยังโผล่ที่นี่
            // ไม่งั้นมันจะหลุดจากทั้งสี่ใบและหายไปจากสายตาพอดีตอนที่ควรถูกมองที่สุด
            return first <= limit
        }
    }

    /// stage ที่เลยกำหนดจบมาแล้วแต่ยังไม่ปิด
    static func overdueStages(
        _ stages: [WorkStageRow],
        today: Date,
        calendar: Calendar
    ) -> [WorkStageRow] {
        stages.filter { stage in
            guard stage.actualEnd == nil, let end = stage.plannedEndDate else { return false }
            return calendar.startOfDay(for: end) < calendar.startOfDay(for: today)
        }
    }

    /// ช้ากี่วัน นับจาก stage ที่เลยกำหนดมานานที่สุด
    /// คืน nil เมื่อไม่มีอะไรเลยกำหนด — ต่างจาก 0 ซึ่งจะแปลว่า "เลยกำหนดวันนี้พอดี"
    static func daysLate(_ stages: [WorkStageRow], today: Date, calendar: Calendar) -> Int? {
        let overdue = overdueStages(stages, today: today, calendar: calendar)
        guard let earliest = overdue.compactMap(\.plannedEndDate).min() else { return nil }
        return calendar.dateComponents(
            [.day],
            from: calendar.startOfDay(for: earliest),
            to: calendar.startOfDay(for: today)
        ).day
    }

    /// ทุก stage ปิดครบ = งานเสร็จ ไม่ต้องมีคอลัมน์ status
    static func isFinished(_ stages: [WorkStageRow]) -> Bool {
        !stages.isEmpty && stages.allSatisfy { $0.actualEnd != nil }
    }
}
```

- [ ] **Step 5: รัน harness ให้ผ่านทุกข้อ**

```bash
swiftc -o "$SCRATCH/run" \
  "null-app/Features/work/WorkRow.swift" \
  "null-app/Features/work/WorkFilter.swift" \
  "$SCRATCH/main.swift" && "$SCRATCH/run"
```

Expected: ทุกบรรทัดขึ้นต้นด้วย `ok` และปิดท้ายด้วย `ALL PASS`
ถ้ามี `FAIL` ให้แก้ `WorkFilter.swift` ไม่ใช่แก้ harness — harness เขียนจากสเปก

- [ ] **Step 6: Commit**

```bash
git add null-app/Features/work/WorkRow.swift null-app/Features/work/WorkFilter.swift
git commit -m "Give lateness one definition the whole feature can share"
```

---

### Task 4: อ่านข้อมูลจริงเข้ามาในแอป

**Files:**
- Create: `null-app/Features/work/WorkStore.swift`
- Modify: `null-app/Features/work/WorkFeature.swift`

**Interfaces:**
- Consumes: `WorkItemRow`, `WorkTypeRow` จากงานที่ 3 · REST endpoint จากงานที่ 2
- Produces: `WorkStore` — `init(userID: UUID)`, `private(set) var items: [WorkItemRow]`, `private(set) var types: [WorkTypeRow]`, `private(set) var loadFailed: Bool`, `func load() async` — งานที่ 5 และ 6 ใช้ทั้งหมดนี้

- [ ] **Step 1: เขียน `WorkStore.swift`**

```swift
//
//  WorkStore.swift
//  null-app
//

import Foundation
import Observation
import Supabase

/// เจ้าของ state ของฟีเจอร์ work เพียงผู้เดียว ตามแบบเดียวกับ ProfileStore
/// view ไม่รู้ว่าข้อมูลมาจากไหน วันที่แหล่งข้อมูลเปลี่ยนจึงไม่ต้องแก้ view
@Observable
final class WorkStore {
    private(set) var items: [WorkItemRow] = []
    private(set) var types: [WorkTypeRow] = []

    /// แยก "โหลดแล้วไม่มีงาน" ออกจาก "โหลดไม่สำเร็จ" — สองอย่างนี้หน้าตาเหมือนกันบนจอ
    /// ถ้าไม่แยก แล้วผู้ใช้จะเข้าใจว่าข้อมูลหายทั้งที่แค่เน็ตหลุด
    private(set) var loadFailed = false
    private(set) var hasLoaded = false

    private let userID: UUID

    /// ไม่ทำงานใด ๆ ใน init — Home เรียก makeRoot ตอนวาดทุกไอคอน ไม่ใช่ตอนกด
    init(userID: UUID) {
        self.userID = userID
    }

    /// ดึงงานพร้อม stage ในคำขอเดียวผ่าน embed ของ PostgREST
    /// สองคำขอแยกจะทำให้เกิดช่วงที่การ์ดมีงานแต่ยังไม่มีแถบ stage ซึ่งกระพริบ
    func load() async {
        do {
            async let itemsTask: [WorkItemRow] = Backend.client
                .schema("f_work")
                .from("item")
                .select("id,type_code,name,description,requested_by,badge,updated_at,stage(id,code,name,position,planned_start,planned_end,actual_start,actual_end)")
                .eq("user_id", value: userID)
                .is("archived_at", value: nil)
                .order("updated_at", ascending: false)
                .execute()
                .value

            async let typesTask: [WorkTypeRow] = Backend.client
                .schema("f_work")
                .from("work_type")
                .select("code,label,position")
                .eq("is_active", value: true)
                .order("position")
                .execute()
                .value

            let (loadedItems, loadedTypes) = try await (itemsTask, typesTask)

            // stage มาจากเซิร์ฟเวอร์โดยไม่รับประกันลำดับ เรียงที่นี่ครั้งเดียว
            // แทนที่จะให้ทุก view ที่ใช้มันเรียงเอง
            items = loadedItems.map { item in
                var sorted = item
                sorted.stage.sort { $0.position < $1.position }
                return sorted
            }
            types = loadedTypes
            loadFailed = false
        } catch {
            loadFailed = true
        }

        hasLoaded = true
    }
}
```

- [ ] **Step 2: ทำให้ `stage` แก้ไขได้ใน `WorkItemRow`**

`WorkStore.load()` เรียง `stage` ในที่ ต้องเปลี่ยน `let stage` เป็น `var stage` ใน `null-app/Features/work/WorkRow.swift`:

```swift
    var stage: [WorkStageRow]
```

- [ ] **Step 3: รัน harness ของงานที่ 3 ซ้ำ**

```bash
swiftc -o "$SCRATCH/run" \
  "null-app/Features/work/WorkRow.swift" \
  "null-app/Features/work/WorkFilter.swift" \
  "$SCRATCH/main.swift" && "$SCRATCH/run"
```

Expected: `ALL PASS` เหมือนเดิม — การเปลี่ยน `let` เป็น `var` ต้องไม่กระทบตรรกะ

- [ ] **Step 4: ส่ง `userID` ต่อจาก `WorkFeature`**

แก้ `null-app/Features/work/WorkFeature.swift`:

```swift
    func makeRoot(userID: UUID) -> AnyView {
        AnyView(WorkRootView(userID: userID))
    }
```

และลบคอมเมนต์ที่บอกว่า `userID` ยังไม่ถูกใช้ออก

- [ ] **Step 5: รับ `userID` ใน `WorkRootView` โดยยังไม่ใช้**

เพิ่มบรรทัดเดียวบนสุดของ `struct WorkRootView` ใน `null-app/Features/work/WorkRootView.swift` เพื่อให้คอมไพล์ผ่าน (งานที่ 5 จะเอาไปใช้จริง):

```swift
    let userID: UUID
```

และแก้ `#Preview` ให้ส่งค่า:

```swift
#Preview {
    NavigationStack {
        WorkRootView(userID: UUID())
    }
}
```

- [ ] **Step 6: build ทั้งสองแพลตฟอร์ม**

```bash
xcodebuild -scheme null-app -destination 'generic/platform=iOS Simulator' build
xcodebuild -scheme null-app -destination 'platform=macOS' build
```

Expected: `** BUILD SUCCEEDED **` ทั้งคู่ ไม่มี `warning:` ที่ไม่ใช่ AppIntents

- [ ] **Step 7: Commit**

```bash
git add null-app/Features/work
git commit -m "Let the work feature read its own rows"
```

---

### Task 5: การ์ดงานและแถบ stage

**Files:**
- Create: `null-app/Features/work/WorkStageBar.swift`
- Create: `null-app/Features/work/WorkCard.swift`

**Interfaces:**
- Consumes: `WorkItemRow`, `WorkStageRow`, `WorkFilter.daysLate` จากงานก่อนหน้า
- Produces: `WorkStageBar(stages:)` และ `WorkCard(item:today:)` — งานที่ 6 วางสองอันนี้ลงหน้าจอ

- [ ] **Step 1: เขียน `WorkStageBar.swift`**

```swift
//
//  WorkStageBar.swift
//  null-app
//

import SwiftUI

/// แถบ stage ย่อบนการ์ด — ความกว้างของแต่ละช่องคือความยาวตามแผน
/// จึงเป็น timeline ที่ผู้ใช้ตีไว้จริง ไม่ใช่ progress bar ที่แบ่งเท่า ๆ กัน
///
/// stage ทับซ้อนกันได้ตามของจริง แถบนี้จึงเรียงตาม position ไม่ได้พยายามวางตามปฏิทิน
/// (การวางตามปฏิทันจริงอยู่ในหน้า Timeline เต็มซึ่งเลื่อนแนวนอนได้)
struct WorkStageBar: View {
    let stages: [WorkStageRow]

    var body: some View {
        HStack(spacing: 3) {
            ForEach(stages) { stage in
                Text(stage.code)
                    .font(.caption2)
                    .lineLimit(1)
                    .minimumScaleFactor(0.7)
                    .frame(maxWidth: .infinity)
                    .frame(height: 22)
                    .background(background(for: stage.state), in: RoundedRectangle(cornerRadius: 4))
                    .foregroundStyle(foreground(for: stage.state))
                    .layoutPriority(weight(of: stage))
            }
        }
        .accessibilityElement(children: .ignore)
        .accessibilityLabel(accessibilitySummary)
    }

    /// ยิ่ง stage ยาวตามแผน ยิ่งได้พื้นที่มาก
    /// layoutPriority รับ Double จึงใช้จำนวนวันหารด้วย 30 เพื่อให้ค่าอยู่ในระดับที่อ่านง่าย
    private func weight(of stage: WorkStageRow) -> Double {
        guard let start = stage.plannedStartDate, let end = stage.plannedEndDate else { return 1 }
        let days = end.timeIntervalSince(start) / 86_400
        return max(days / 30, 0.25)
    }

    private func background(for state: WorkStageRow.State) -> AnyShapeStyle {
        switch state {
        case .completed: AnyShapeStyle(.quaternary)
        case .current: AnyShapeStyle(Color.accentColor)
        case .ahead: AnyShapeStyle(.quaternary.opacity(0.4))
        }
    }

    private func foreground(for state: WorkStageRow.State) -> Color {
        switch state {
        case .completed: .secondary
        case .current: .white
        case .ahead: .secondary
        }
    }

    /// VoiceOver อ่านชื่อ stage สามสิบตัวติดกันไม่มีประโยชน์ — สรุปให้แทน
    private var accessibilitySummary: String {
        let done = stages.filter { $0.state == .completed }.count
        let current = stages.filter { $0.state == .current }.map(\.name).joined(separator: ", ")
        if current.isEmpty {
            return "\(done) of \(stages.count) stages done"
        }
        return "\(done) of \(stages.count) stages done, now in \(current)"
    }
}
```

- [ ] **Step 2: เขียน `WorkCard.swift`**

```swift
//
//  WorkCard.swift
//  null-app
//

import SwiftUI

/// การ์ดงานหนึ่งใบบนหน้า Overview
///
/// ไม่มีตัวเลข progress % และไม่มี Task x/y — สเปกปัดทั้งสองทิ้ง
/// เพราะแถบ stage บอกได้ละเอียดกว่าและตีความง่ายกว่าตัวเลขโดด ๆ
struct WorkCard: View {
    let item: WorkItemRow
    let today: Date

    private var calendar: Calendar {
        var c = Calendar(identifier: .gregorian)
        c.timeZone = TimeZone(secondsFromGMT: 0)!
        return c
    }

    var body: some View {
        VStack(alignment: .leading, spacing: 10) {
            HStack(alignment: .firstTextBaseline) {
                Text(item.name)
                    .font(.subheadline)
                    .fontWeight(.medium)
                    // ไทยไม่มีช่องว่างระหว่างคำ ปล่อยให้ระบบตัดบรรทัดเองได้เต็มที่
                    .fixedSize(horizontal: false, vertical: true)

                Spacer(minLength: 8)

                if let late = WorkFilter.daysLate(item.stage, today: today, calendar: calendar) {
                    Text("\(late)d late")
                        .font(.caption)
                        .foregroundStyle(.red)
                        .layoutPriority(1)
                }
            }

            if item.stage.isEmpty {
                Label("Add a timeline", systemImage: "calendar.badge.plus")
                    .font(.caption)
                    .foregroundStyle(.secondary)
            } else {
                WorkStageBar(stages: item.stage)
            }

            HStack {
                if let badge = item.badge, !badge.isEmpty {
                    Text(badge)
                        .font(.caption)
                        .foregroundStyle(.secondary)
                        .lineLimit(1)
                }

                Spacer(minLength: 8)

                Text(item.updatedAt, format: .relative(presentation: .numeric))
                    .font(.caption2)
                    .foregroundStyle(.tertiary)
                    .layoutPriority(1)
            }
        }
        .padding(14)
        .frame(maxWidth: .infinity, alignment: .leading)
        .background(.quaternary.opacity(0.4), in: RoundedRectangle(cornerRadius: 12))
    }
}
```

- [ ] **Step 3: build ทั้งสองแพลตฟอร์ม**

```bash
xcodebuild -scheme null-app -destination 'generic/platform=iOS Simulator' build
xcodebuild -scheme null-app -destination 'platform=macOS' build
```

Expected: `** BUILD SUCCEEDED **` ทั้งคู่ (ยังไม่มีใครเรียกใช้สองไฟล์นี้ — คอมไพล์ผ่านคือทั้งหมดที่ตรวจได้ตอนนี้)

- [ ] **Step 4: Commit**

```bash
git add null-app/Features/work/WorkStageBar.swift null-app/Features/work/WorkCard.swift
git commit -m "Draw a work card off the timeline that was actually planned"
```

---

### Task 6: ต่อหน้าจอเข้ากับข้อมูลจริง

**Files:**
- Modify: `null-app/Features/work/WorkRootView.swift`

**Interfaces:**
- Consumes: ทุกอย่างจากงานที่ 3–5
- Produces: หน้าจอที่ใช้งานได้จริง — ไม่มีงานถัดไปในแผนนี้ที่พึ่งพา

- [ ] **Step 1: เขียน `WorkRootView.swift` ใหม่ทั้งไฟล์**

```swift
//
//  WorkRootView.swift
//  null-app
//

import SwiftUI

/// หน้าแรกของฟีเจอร์ Work
///
/// ตัวนับสี่ใบและการกดกรองใช้ WorkFilter ตัวเดียวกัน ตัวเลขบนหัวจึงตรงกับรายการข้างล่างเสมอ
/// โดยไม่ต้องมีใครคอยดูแลให้ตรง
struct WorkRootView: View {
    let userID: UUID

    @State private var store: WorkStore
    @State private var activeFilter: WorkFilter?
    @State private var activeType: String?

    /// วันนี้ถูกตรึงตอนหน้าจอปรากฏ ไม่ใช่คำนวณใหม่ทุกครั้งที่วาด
    /// ไม่งั้นการนับกับตัวเลขบนการ์ดอาจคนละวินาทีกันข้ามเที่ยงคืน
    @State private var today = Date()

    private var calendar: Calendar {
        var c = Calendar(identifier: .gregorian)
        c.timeZone = TimeZone(secondsFromGMT: 0)!
        return c
    }

    init(userID: UUID) {
        self.userID = userID
        _store = State(initialValue: WorkStore(userID: userID))
    }

    var body: some View {
        ScrollView {
            VStack(spacing: 16) {
                summary
                typeFilter
                list
            }
            .padding(.horizontal)
            .padding(.top, 12)
        }
        .navigationTitle("Work")
        #if !os(macOS)
        .navigationBarTitleDisplayMode(.inline)
        #endif
        .toolbar {
            ToolbarItem(placement: .primaryAction) {
                Button {
                    // รอบหน้า: เปิดหน้าเพิ่มงาน
                } label: {
                    Image(systemName: "plus")
                }
                .disabled(true)
                .accessibilityLabel("Add work")
            }
        }
        .refreshable { await store.load() }
        .task {
            today = Date()
            await store.load()
        }
    }

    /// งานที่ผ่านทั้งตัวกรองประเภทและตัวกรองสถานะ
    private var visibleItems: [WorkItemRow] {
        store.items.filter { item in
            if let activeType, item.typeCode != activeType { return false }
            if let activeFilter,
               !activeFilter.matches(item.stage, today: today, calendar: calendar) { return false }
            return true
        }
    }

    private func count(_ filter: WorkFilter) -> Int {
        store.items.filter { filter.matches($0.stage, today: today, calendar: calendar) }.count
    }

    private var summary: some View {
        LazyVGrid(
            columns: [GridItem(.flexible(), spacing: 10), GridItem(.flexible(), spacing: 10)],
            spacing: 10
        ) {
            ForEach(WorkFilter.allCases) { filter in
                Button {
                    activeFilter = activeFilter == filter ? nil : filter
                } label: {
                    VStack(alignment: .leading, spacing: 2) {
                        Text(count(filter), format: .number)
                            .font(.title2)
                            .fontWeight(.medium)

                        Text(filter.label)
                            .font(.subheadline)
                            .foregroundStyle(.secondary)
                            .lineLimit(1)
                    }
                    .frame(maxWidth: .infinity, alignment: .leading)
                    .padding(.vertical, 12)
                    .padding(.horizontal, 14)
                    .background(
                        activeFilter == filter ? AnyShapeStyle(Color.accentColor.opacity(0.15))
                                               : AnyShapeStyle(.quaternary.opacity(0.4)),
                        in: RoundedRectangle(cornerRadius: 12)
                    )
                }
                .buttonStyle(.plain)
                .foregroundStyle(.primary)
                .accessibilityElement(children: .combine)
                .accessibilityAddTraits(activeFilter == filter ? [.isSelected] : [])
            }
        }
    }

    /// chip เลื่อนแนวนอน ไม่ใช่ segmented picker — ประเภทงานเพิ่มได้จากหลังบ้าน
    /// และไม่มีชื่อประเภทใดถูกเขียนตายไว้ในไฟล์นี้
    private var typeFilter: some View {
        ScrollView(.horizontal, showsIndicators: false) {
            HStack(spacing: 8) {
                chip(label: "All", isOn: activeType == nil) { activeType = nil }

                ForEach(store.types) { type in
                    chip(label: type.label, isOn: activeType == type.code) {
                        activeType = activeType == type.code ? nil : type.code
                    }
                }
            }
            .padding(.horizontal, 1)
        }
    }

    private func chip(label: String, isOn: Bool, action: @escaping () -> Void) -> some View {
        Button(action: action) {
            Text(label)
                .font(.subheadline)
                .padding(.horizontal, 14)
                .padding(.vertical, 6)
                .background(
                    isOn ? AnyShapeStyle(Color.accentColor.opacity(0.15))
                         : AnyShapeStyle(.quaternary.opacity(0.4)),
                    in: Capsule()
                )
        }
        .buttonStyle(.plain)
        .foregroundStyle(.primary)
        .accessibilityAddTraits(isOn ? [.isSelected] : [])
    }

    @ViewBuilder
    private var list: some View {
        if !store.hasLoaded {
            ProgressView().padding(.top, 40)
        } else if store.loadFailed {
            ContentUnavailableView(
                "Couldn't load your work",
                systemImage: "exclamationmark.triangle",
                description: Text("Pull down to try again.")
            )
        } else if visibleItems.isEmpty {
            ContentUnavailableView(
                store.items.isEmpty ? "No work yet" : "Nothing matches",
                systemImage: "briefcase",
                description: Text(
                    store.items.isEmpty
                        ? "Projects and requests will show up here."
                        : "Tap the selected filter again to clear it."
                )
            )
            .padding(.top, 24)
        } else {
            LazyVStack(spacing: 10) {
                ForEach(visibleItems) { item in
                    WorkCard(item: item, today: today)
                }
            }
        }
    }
}

#Preview {
    NavigationStack {
        WorkRootView(userID: UUID())
    }
}
```

- [ ] **Step 2: build ทั้งสองแพลตฟอร์ม**

```bash
xcodebuild -scheme null-app -destination 'generic/platform=iOS Simulator' build
xcodebuild -scheme null-app -destination 'platform=macOS' build
```

Expected: `** BUILD SUCCEEDED **` ทั้งคู่

- [ ] **Step 3: ติดตั้งลงซิมูเลเตอร์และเปิด**

```bash
SIM=C7CBA4C6-2050-41B9-8DD1-A5DB6D005FEF
xcrun simctl boot "$SIM" 2>/dev/null
APP="$(xcodebuild -scheme null-app -destination 'generic/platform=iOS Simulator' \
  -showBuildSettings 2>/dev/null | awk -F' = ' '/ BUILT_PRODUCTS_DIR /{print $2"/null-app.app"}')"
xcrun simctl install "$SIM" "$APP"
xcrun simctl launch "$SIM" purin.null-app
```

**สำคัญ:** อย่ารัน `simctl keychain reset` — ต้องเข้าเป็น USER_A คนเดิมที่มีข้อมูล
ถ้าเปิดมาแล้วเป็นคนละบัญชี ให้เช็กว่า session ในเครื่องเป็นของใครก่อน

- [ ] **Step 4: อ่านแถวจริงจากฐานข้อมูลมาเทียบ**

```sql
select i.name, i.badge, i.updated_at,
       count(s.id) as stages,
       count(*) filter (where s.actual_end is not null) as done_stages
from f_work.item i
left join f_work.stage s on s.item_id = i.id
where i.user_id = '<USER_A>' and i.archived_at is null
group by i.id, i.name, i.badge, i.updated_at
order by i.updated_at desc;
```

จดผลไว้ แล้วถ่ายรูปหน้าจอเทียบทีละใบ — **ชื่องานบนจอต้องตรงกับ `name` ในแถวแบบตัวอักษรต่อตัวอักษร**
หน้าจอที่ดูถูกไม่ใช่หลักฐาน

- [ ] **Step 5: ตรวจตัวนับด้วยมือ**

จากข้อมูลทดสอบในงานที่ 1 (โดยถือว่าวันนี้อยู่หลัง 2026-08-10):

| ใบ | ค่าที่ต้องได้ | เพราะ |
|---|---|---|
| Active | 2 | ITEM_1 และ ITEM_2 เริ่มแล้วยังไม่จบ ส่วนใบไม่มี stage ไม่นับ |
| Overdue | 1 | ITEM_2 มี stage `U` เลย 2026-08-10 แล้วยังไม่ปิด |
| Due this month | 1 | ITEM_2 มี stage สุดท้ายจบ 2026-08-10 |
| Starting soon | 0 | ทั้งสองใบเริ่มไปแล้ว ใบที่สามไม่มี stage |

ถ้าเลขไม่ตรง **อย่าแก้ที่หน้าจอ** — กลับไปเพิ่มเคสใน harness ของงานที่ 3 ให้จับข้อผิดพลาดนั้นได้ก่อน แล้วค่อยแก้ `WorkFilter.swift`

- [ ] **Step 6: ตรวจการกดกรอง**

กดการ์ด `Overdue` แล้วถ่ายรูป — รายการข้างล่างต้องเหลือ ITEM_2 ใบเดียว และการ์ดต้องมีสีบอกว่าถูกเลือก
กดซ้ำอีกครั้งต้องกลับมาแสดงทั้งหมด

- [ ] **Step 7: ตรวจตัวกรองประเภท**

chip ต้องมี `All`, `Project`, `Request` โดย **ไม่มีชื่อไหนถูกเขียนไว้ในไฟล์ view** — พิสูจน์ด้วย:

```bash
grep -rn "Project\|Request" null-app/Features/work/*.swift
```

Expected: ไม่เจอเลย (ยกเว้นในคอมเมนต์) ถ้าเจอในโค้ดที่ทำงานจริง แปลว่าเส้นแบ่งรั่ว

กด chip `Request` แล้วต้องเหลือ ITEM_2 ใบเดียว

- [ ] **Step 8: ตรวจว่าข้อความไทยไม่ล้น**

ถ่ายรูปการ์ดทั้งสามใบ ชื่องานเป็นภาษาไทยยาว ๆ ต้องตัดบรรทัดอ่านได้ ไม่โดนตัดกลางคำจนอ่านไม่รู้เรื่อง
และไม่ดันตัวเลข `Nd late` หลุดขอบ

- [ ] **Step 9: ตรวจว่างานที่ไม่มี stage แสดงถูก**

ใบที่สามต้องขึ้น `Add a timeline` แทนแถบ stage และต้องไม่ถูกนับในตัวนับใดเลย

- [ ] **Step 10: build macOS แล้วเปิดจริงหนึ่งครั้ง**

sandbox ของ macOS ปิดเน็ตถ้า entitlement หาย และอาการจะออกมาเป็น
`NSURLErrorCannotFindHost (-1003)` ซึ่งอ่านเหมือนปัญหา DNS ทั้งที่ไม่ใช่
รันแอป macOS แล้วเข้าหน้า Work หนึ่งครั้งเพื่อยืนยันว่าข้อมูลขึ้นเหมือนกัน

- [ ] **Step 11: Commit**

```bash
git add null-app/Features/work/WorkRootView.swift
git commit -m "Put real work on the work screen"
```

---

## Self-Review

**Spec coverage**

| สเปกกำหนด | งานที่ครอบคลุม |
|---|---|
| schema `f_work` สามตาราง | Task 1 Step 2 |
| `work_type` ไม่มี `user_id` อ่านอย่างเดียว | Task 1 Step 3, พิสูจน์ใน Step 10 |
| RLS พิสูจน์ว่าปฏิเสธ | Task 1 Steps 7–9 |
| `updated_at` ไม่โกหก | Task 1 Step 4, พิสูจน์ใน Step 11 |
| Exposed schemas + curl | Task 2 |
| Overdue วัดจากวันที่ ไม่มีระยะผ่อนผัน | Task 3 `WorkFilter.overdue`, harness มีเคส `barelyLate` และ `dueToday` |
| ความช้ามีนิยามเดียว | Task 3 — การ์ดและตัวนับเรียก `WorkFilter` เดียวกัน |
| stage ทับซ้อนกันได้ | Task 1 Step 6 ใส่ PT คาบ U และ PVT คาบ I |
| หน่วยเวลาละเอียดกว่าเดือน | Task 1 Step 6 PVT ยาว 7 วัน |
| สามสถานะของ stage | Task 3 `WorkStageRow.State` |
| การ์ดไม่มี progress % และ Task x/y | Task 5 `WorkCard` |
| งานไม่มี stage ขึ้นคำเชิญ ไม่ถูกนับ | Task 5 Step 2, พิสูจน์ Task 6 Step 9 |
| ตัวนับสี่ใบกดกรองได้ | Task 6 Step 1, พิสูจน์ Step 6 |
| ตัวกรองประเภทเป็น chip เลื่อนได้ ไม่ใช่ segmented | Task 6 `typeFilter` |
| ห้ามเขียนชื่อประเภทตายตัวใน view | Task 6 Step 7 มี grep พิสูจน์ |
| ทดสอบด้วยข้อความไทยจริง | Task 1 Step 6 ใส่ชื่อไทย, Task 6 Step 8 ตรวจการตัดบรรทัด |
| งานที่ archive แล้วไม่โผล่ | Task 4 `.is("archived_at", value: nil)` |

**ยังไม่อยู่ในแผนนี้โดยตั้งใจ** (สเปกจัดไว้รอบหลัง): progress % ถ่วงน้ำหนัก, ตาราง `task`, บล็อก "ช่วงนี้ควรโฟกัส", บล็อก "เพิ่งขยับ", หน้า Timeline เต็ม, หน้าเพิ่ม/แก้งาน, หน้ารายการที่เข้ากรุ

**Type consistency:** `WorkFilter.matches(_:today:calendar:)` ใช้ลายเซ็นเดียวกันทั้งใน harness (Task 3), `count(_:)` และ `visibleItems` (Task 6) · `WorkFilter.daysLate(_:today:calendar:)` ใช้ใน `WorkCard` (Task 5) ตรงกับที่ประกาศใน Task 3 · `WorkStore.load()` `items` `types` `hasLoaded` `loadFailed` ที่ประกาศใน Task 4 ถูกใช้ครบใน Task 6 · `WorkItemRow.stage` เปลี่ยนเป็น `var` ใน Task 4 Step 2 ก่อนที่ `WorkStore.load()` จะเรียง

**หนี้ที่แผนนี้ทิ้งไว้:** `WorkCard` และ `WorkRootView` สร้าง `Calendar` ของตัวเองคนละที่ ถ้ามีที่ที่สามเมื่อไหร่ ควรย้ายไปเป็น `static let` ใน `WorkFilter` — ตอนนี้สองที่ยังไม่คุ้มกับการเพิ่มชั้น
