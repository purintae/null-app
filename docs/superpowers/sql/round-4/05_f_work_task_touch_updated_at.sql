-- migration name: f_work_task_touch_updated_at
-- รันได้ทุกเมื่อหลัง 01 ไม่ขึ้นกับไฟล์อื่น
--
-- `f_work.work` และ `f_work.stage` มี trigger ตัวนี้อยู่แล้วตั้งแต่รอบ 2 แต่ `task` ที่เพิ่ม
-- ในรอบ 4 ไม่มี — `updated_at` ของ task จึงค้างอยู่ที่เวลาที่สร้างตลอดไป กลายเป็นคอลัมน์
-- ที่เก็บค่าซึ่งไม่ตรงกับความจริง ซึ่งเป็นสิ่งเดียวกับที่สเปกรอบนี้ทั้งฉบับพยายามกำจัด
--
-- ใช้ฟังก์ชัน f_work.touch_updated_at() ตัวเดิมที่อีกสองตารางใช้อยู่ ไม่สร้างตัวใหม่
-- รูปทรงของ trigger เหมือนกันเป๊ะ: before update, for each row
create trigger touch_updated_at
    before update on f_work.task
    for each row execute function f_work.touch_updated_at();

-- หมายเหตุ: แถวที่มีอยู่ก่อนหน้านี้ไม่ถูกแก้ย้อนหลัง task ที่เคยถูกติ๊กไปแล้วก่อนรันไฟล์นี้
-- จะยังมี updated_at เท่ากับ created_at ซึ่งไม่ตรงกับความจริง แต่กู้ค่าที่ถูกต้องไม่ได้
-- เพราะไม่มีที่ไหนบันทึกไว้ว่าติ๊กเมื่อไหร่ — ตั้งแต่ไฟล์นี้เป็นต้นไปถึงจะถูก
